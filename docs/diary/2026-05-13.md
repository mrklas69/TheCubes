# 2026-05-13

## Sezení 27

### Diskuse

**(A) `%BEGIN` post-mortem — 7 nepushnutých commitů `feat/terrain` v `main`.**

`%BEGIN` proběhl normálně (git pull no-op, server :8000 spuštěn). Cadence prahy: `%AUDIT:CODE` 8/8 ✓, `%AUDIT:DOCS` 10/10 ✓ — oba dosažené, doporučené pro sez. 27. Performance při 30×30 priorita z Příště sez. 26.

Při shrnutí stavu user upozornil: *„Aha, asi neproběhl merge z feat/terrain."* Diagnostika `git log main..feat/terrain --oneline` potvrdila **7 commitů** od kotvy DD-32 (`d027001`) přes Fáze 1+2+3 (wipe `010d7ac` → MVP `845c0d2` → ramp smoothing `68a1fda`) + 3 docs commity sez. 24/25/26.

Není to nedopatření z `%END` sez. 26 — je to topic-branch workflow stejně jako `feat/factory` (sez. 21–23, mergnuta `e0b5fa3` po dokončení fáze A+B+steady-state). Sez. 26 push šel správně do `origin/feat/terrain`. Co chybělo: explicitní rozhodnutí o **kdy mergnout** topic branch zpět do main.

**(B) Merge varianta — A vs. B vs. C.**

- **(A)** Merge teď. Terrain MVP end-to-end funkční (Fáze 0+1+2+3 hotové, ramp smoothing 92 % pokrytí, UI panel). Performance refactor pak na vlastní `feat/terrain-perf` — clean scope.
- **(B)** Pokračovat na `feat/terrain` přes sez. 27 performance fix, merge až poté („není production-ready bez performance při 30×30").
- **(C)** Merge teď + TODO „aktualizovat PROMPTS.md `%END` o topic-branch workflow."

User vybral **(A)**. Provedeno: `git checkout main && git merge --no-ff feat/terrain` — merge commit `b1295ea`, **12 souborů, +1557/−1709**, nový `src/terrain.js` 411 ř. `git push origin main` ✓.

### Rozhodnutí

- **Merge `feat/terrain` → `main` (--no-ff, b1295ea)** po dokončení Fáze 0+1+2+3 DD-32. Topic branch zůstává (nesmazaná) — sez. 28 použije `feat/terrain-perf` pro performance refactor.
- **Žádný DD.** Operační rozhodnutí, ne arch.

### Kód

Žádný kód v sez. 27. Pouze merge + dokumentace.

### Kudos / Censure

- **Censure! (AI → AI):** `%END` sez. 26 měl explicitně řešit merge strategii topic branche, ne jen pushnout do `origin/feat/terrain` a tiše skončit. User musel sám zachytit že merge chybí — `%BEGIN` sez. 27 neukázal stav `main..feat/terrain` jako standard kontrolu (ukazoval jen současné `git status` + `git log` z výchozího banneru).
- **Censure! (PROMPTS.md):** `%END` text doslova říká *„Commit na větev `main`"* — zastaralé od sez. 21, kdy DD-30 zavedl topic branches (`feat/factory`, později `feat/terrain`). PROMPTS.md `%END` (3.) potřebuje přepis o topic-branch workflow + merge rozhodnutí. **TODO** zapsáno.
- **Kudos! (user):** rychlá detekce stavu po `%BEGIN` shrnutí — předtím než sez. 27 začalo skutečnou práci. Multi-instance discipline (přechod na druhý stroj) by jinak byla riziková (mrkla stroj by pullnul `main` bez terrain MVP).

### Příště (sez. 27 → sez. 28, na stroji mrkla)

**Performance — priorita (přesun ze sez. 26→27):**

- [!] **30×30 vykreslování pomalé** — `%THINK` výběr cesty:
  - `InstancedMesh` per (kind, surface).
  - `BatchedMesh` (Three.js r172+).
  - Mesh merge per cell.
  - Web worker pro `generateTerrain`.
- Větev: **`feat/terrain-perf`** (založit na `main` po pull).

**Audit cadence (přesun ze sez. 26→27, oba stále dosažené):**

- [!] **`%AUDIT:CODE`** — 8/8 sezení od sez. 18. Doporučení: provést **až po** performance refactoru (refactor pravděpodobně přepíše `spawnTerrain`/`createMeshFor`).
- [!] **`%AUDIT:DOCS`** — 10/10 sezení od sez. 16. Může proběhnout kdykoli (nezávislé).

**Otevřené body:**

- [ ] **PROMPTS.md `%END` aktualizovat** o topic-branch workflow (založit větev / merge rozhodnutí na konci milestone). Sub-prah governance.
- [ ] TDRAMP compatibility check, 2-voxel stepy, 4-cell jámy, LIQUID třída, klastrování water cells (z Příště sez. 26).

**Multi-instance discipline:** Sez. 27 single-instance. Přechod stroj hejna → mrkla je sekvenční (po push), ne souběh — žádný worktree potřeba.

## Sezení 28

### Diskuse

**(A) `%BEGIN` zajímavost — pull dvakrát.**

`%BEGIN` `git pull --ff-only` proběhl normálně (9 commitů z hejna). User upozornil, že na druhém stroji vidí jiné UI (`#terrainctrl`) — pre-pull stav byl 9 commitů pozadu (factory toy), post-pull terrain MVP. Po `git fetch` se objevily 2 nové remote větve: `origin/feat/factory` (už mergovaná), `origin/feat/terrain` (NEmergnutá v main na čas %BEGIN). User vysvětlení: hejno mezitím sez. 27 mergnulo `feat/terrain` → main + push, takže druhý `git pull` dotáhl ještě 2 commity (sez. 27 merge + diary). **Lesson:** `gitStatus` banner v systémovém promptu Claude Code se NEaktualizuje za session; vždy `git status` / `git log` přes Bash.

**(B) `%THINK` performance — diagnose first.**

User feedback ze sez. 26: 30×30 vykreslování pomalé (FPS ~15). TODO sez. 27 navrhuje 4 cesty (InstancedMesh / BatchedMesh / mesh merge / web worker). `%THINK` default oponoval rovnému refactoru: **měřit nejdřív**, neznáme bottleneck (a) generace / (b) scene mutace / (c) per-frame render. Plus low-hanging fruit: `faceMaterialFor` aktuálně alokuje `MeshStandardMaterial` per blok (~16000 per 30×30 regen), memoizace by ušetřila GC/allocator pressure i kdyby calls zůstaly.

User „Jasně!" → diagnostika.

**(C) Atlas refactor — data po diagnostice.**

Tři instrumenty v `feat/terrain-perf`:
1. **`faceMaterialFor` memoizace** — `Map<key, Material>` cache. Po 30×30 regen: mat unikátů 4 (`:grass-top` / `:dirt` / `:stone` / `:sand`) místo ~16k alokací.
2. **`console.time`** v `spawnTerrain` — 3 fáze (`generateTerrain` / `spawnTerrain.blocks` / `spawnTerrain.water+ramps`).
3. **Perf HUD** pravý horní roh — FPS / calls / triangles / geometries / cached materials (`renderer.info`, throttle 1×/s).

**Baseline (po material cache, před atlasem) @ 30×30:**

| metrika | hodnota |
|---|---|
| FPS | 15 |
| calls | 25 106 |
| tri/call | ~2 (smoking gun) |
| generateTerrain | 2.5 ms |
| spawnTerrain.blocks | 38.6 ms |
| total regen | ~43 ms |
| `[Violation]` rAF handler | 50-64 ms |

Klíčový nález: `tri/call ≈ 2` = 1 face per draw call. To je `THREE.Mesh` s `material = [m0..m5]` rozsekáním na 6 material groups → **6× draw calls per box**. Material cache šetří GPU state changes mezi calls, ale počet calls neredukuje. Generace = trivial (5 % regen), worker dead. Render loop = jasný bottleneck.

**Atlas refactor:**
- `getSharedAtlasBoxGeom()` — sdílená `BoxGeometry` (1 instance pro všechny terrain TCUBES) s UVs přepsanými na 1/6-tice atlasu (face N → u ∈ [N/6, (N+1)/6]).
- `getTcubesKindMaterial(kind)` — per-kind atlas: 6 facelets slepené horizontálně do CanvasTexture 96×16 px, `NearestFilter` + `SRGBColorSpace`. Negative cache pro non-terrain kindy.
- `createTCubeFor` fast/slow path dispatch: terrain kind (matches `BLOCK_TEXTURES`) → shared geom + atlas material (1 draw call). Jiné → původní material array (slow path zachován pro emoji demo boxy).

**Po atlas refactoru @ 30×30:**

| metrika | před atlasem | po atlasu | změna |
|---|---|---|---|
| FPS | 15 | **92** | **6.1×** |
| calls | 25 106 | ~5 050 | 0.20× |
| tri | 49 432 | 49 480 | shoda |
| geom | ~5 500 | **8** | sdílená box geom |
| spawnTerrain.blocks | 38.6 ms | 6.5 ms | 5.9× rychlejší |
| total regen | 43 ms | 9 ms | 4.8× rychlejší |
| `[Violation]` | 50-64 ms | žádné | — |

10×10 a 20×20 propad calls 4-6×, FPS roste z 32 → 142 @ 20×20. Atlas dodal predikci přesně (6× redukce). Sez. 28 cíl 30×30 playable splněn (92 FPS = 144 Hz monitor cap ≈ 64 %).

**(D) Rampy atlas — odloženo.**

256 rampů @ 30×30 × 4-5 faces ≈ 1 200 calls (z 5 050 = 24 %). Atlas pro rampy by ubral další ~1 000 calls. **Diminishing return:** marginal benefit malý, refactor 4× repeat pattern (TRRAMPS / TTRAMPS / TDRAMP / TTUNELS, každý vlastní BufferGeometry s manuálním UV mapováním per face). User volba **(2) skončit teď, sez. 29 zvlášť**.

### Rozhodnutí

- **Atlas refactor TCUBES** — shared geom + per-kind material. 6× redukce draw calls.
- **`faceMaterialFor` memoizace** — cache zachová napříč refactory.
- **Perf HUD permanent** — `#perfhud` pravý horní roh, throttle 1×/s. Není dev-only, je to projektová observability pro budoucí refactory.
- **Rampy atlas odloženo** do sez. 29 (diminishing returns, ~17 % incremental).
- **Žádný DD.** Perf optimalizace, ne arch rozhodnutí o modelu.

### Kód

`feat/terrain-perf` větev:

- **`src/main.js`:**
  - `faceMaterialFor` (376) — memoizace přes `_faceMaterialCache` Map (~30 řádků refactor).
  - `createTCubeFor` (996) — fast/slow path dispatch.
  - Atlas helpers po `BLOCK_TEXTURES` (1873): `ATLAS_TILE_PX`, `ATLAS_FACE_KEYS`, `getSharedAtlasBoxGeom`, `_tcubesAtlasMatCache`, `getTcubesKindMaterial`, `buildKindAtlasTexture` inline (~75 řádků).
  - `animate` (2298) — `_perfHud` throttled report (FPS + renderer.info → 5 DOM elementů).
- **`index.html`:**
  - `#perfhud` CSS (top right corner, izomorfně s `#hud` left) + 5 řádků DOM (FPS / calls / tri / geom / mat).

Diff stat: `src/main.js` +135/−25, `index.html` +30/−0.

### Kudos / Censure

- **Kudos! (AI → user):** příjemný flow „Jasně!" → diagnose → data → atlas → result, 2× interaction. Plán + Q schválené stručně → ihned implementovat (memory `feedback_plan_then_go`). 
- **Censure! (AI → AI):** `%BEGIN` ukazoval první pull stav (9 commitů), ale propásl, že na druhém stroji možná existují další větve. User musel říct *„na druhém stroji vidím něco jiného"* a já jsem to teprve diagnostikoval. `git fetch --all` ukázal `feat/terrain` + `feat/factory` hned na začátku — měla jsem komentovat *„remote má 2 topic větve mimo main, zkontrolovat"*. Konvenční gitStatus banner v systémovém promptu mě navedl na sez. 19 stav, ale fetch jasně ukázal víc.
- **Censure! (AI → AI):** Q2 v %THINK jsem nepoložila s explicit labels (1/2 ne Q2.A/Q2.B), takže user odpověď „2" byla dvojznačná („pokračovat rampy" vs. „skončit"). Per CLAUDE.md *„Při kladení více otázek najednou je označovat Q1, Q2, Q3, …"*. Příště numbering + sub-labels.

### Příště (sez. 28 → sez. 29)

**Performance follow-up:**

- [ ] **Rampy atlas refactor** (TRRAMPS / TTRAMPS / TDRAMP / TTUNELS) — stejný pattern jako TCUBES, ~17 % incremental redukce calls. 4× per ramp type: per-(type, surface) atlas material + shared geom s UV remap na 1/N-tice (N = 4 nebo 5 faces).
- [ ] **Vizuální parita** TCUBES atlas — pre-flight kontrola před sez. 29 dalšími changes (user nepotvrdil v sez. 28 explicitly).

**Audit cadence (oba prahy stále překročené, sez. 28 nepřinesla audit):**

- [!] **`%AUDIT:CODE`** — 10/8 sezení (přes prah o 2). Doporučení sez. 29 nebo 30 — `feat/terrain-perf` refactor přepsal `createTCubeFor` + přidal atlas pipeline, audit by zachytil případné dluhy.
- [!] **`%AUDIT:DOCS`** — 12/10 sezení (přes prah o 2). Sez. 29 nebo dříve.

**Governance:**

- [ ] **PROMPTS.md `%END` aktualizovat o topic-branch workflow** — stále z TODO sez. 27.

**Multi-instance:** Sez. 28 single-instance (mrkla stroj). `feat/terrain-perf` lokálně, push do origin při merge.

## Sezení 29

### Diskuse

**(A) `%BEGIN` čistý + pre-flight TCUBES atlas vizuální parita.**

`%BEGIN` proběhl bez nálezů — main čistý, žádné nové commity z jiného stroje, server :8000 už běžel (PID 40544, nebylo třeba spouštět). Posl. merge `6b8580c` (sez. 28 `feat/terrain-perf`). Cadence v Příště sez. 28: AUDIT:CODE 10/8 + AUDIT:DOCS 12/10 (přes prah o 2), plus pre-flight TCUBES atlas parita, plus rampy atlas refactor.

Doporučení: nejprve **pre-flight vizuální parita** (1 min user check), pak `%AUDIT:CODE` (čerstvý refactor v hlavě), pak rozhodnout DOCS vs. rampy. User volba A → pre-flight.

**Analýza atlas pipeline odhalila 2 vzorce:**
1. **Tile pattern uvnitř kindu** (pre-existing od sez. 28 `_faceMaterialCache` memoizace) — všechny grass-top napříč terénem vypadají identicky. Trade-off za 6× FPS.
2. **Atlas/slow-path texture-source divergence** (nová regrese atlas refactoru) — `getTcubesKindMaterial` volá `factory()` přímo, slow path přes `_faceMaterialCache` → pro stejný `:named-texture` dvě různé textury s různými random patches. Potenciální seam mezi terrain blok a rampa.

User po otevření browseru a porovnání reportoval *„parita OK"* → nález #2 buď neviditelný nebo subtle, fix odložen do TODO follow-up (F5).

**(B) `%AUDIT:CODE` — 3 paralelní agenti.**

Pattern ze sez. 18 (12 nálezů přes 3 agenty). Tentokrát 3 agenti s rozdělenými fokusy:
- **Agent A** — terminologie + soulad s GLOSSARY/DD (12 nálezů).
- **Agent B** — kvalita kódu, mrtvý kód, code smells, atlas pipeline specifika (14 nálezů).
- **Agent C** — struktura adresářů, dead files, soulad s README/CLAUDE (10 nálezů).

Po dedup duplicit napříč agenty + filtraci false alarms (Agent B #6 castShadow + #14 THREE global) → **14 silných nálezů**.

**KRITICKÉ (4):**
- **F1** GLOSSARY je 6 sezení pozadu (sez. 22 → 28). Hlavička dokumentuje smazané DD-30/DD-31 (FACILITY rodina, RESOURCES/RECIPES, PATH transport, pixel-voxel COMPOSITES, tree_sway). TDRAMP (DD-35) v sekci BLOCKS rodina už byl, ale jinde sekce zmiňují smazané věci.
- **F2** DD-36 chybí jako záznam sez. 28 atlas refactoru (sez. 28 explicitly *Žádný DD = perf, ne arch*, ale architektonický kontrakt TCUBES rendering pipeline se změnil → Conceptual Integrity vyžaduje zápis).
- **F3** `tools/` + `assets/` + `VOXEL_MODEL` třída + `buildVoxelModel` + OBJLoader/MTLLoader = ~110 ř. + 2 adresáře bez konzumenta od DD-32 wipe (sez. 25). GLOSSARY l. 196 už přiznávalo *„dormant, no consumer"*. 3 varianty: smazat / archive / keep s DD.
- **F4** `WORLD` singleton = dead state. `WIND_STRENGTH` ztratil konzumenta (tree_sway smazán DD-32), `TIME_SCALE` ztratil (productionTick smazán DD-32). `window.world` expose zbytečný. DD-29 politika *„atribut jen s živým konzumentem"* opačně volá pro smaz.

**DOPORUČENÉ + KOSMETICKÉ (10):** F5-F14 (viz `docs/TODO.md` audit follow-up sekce).

**(C) User volba C + AskUserQuestion na F3/F4 dispozici.**

User: *„C"* = všechny 4 kritické v jednom rolu. F3 + F4 vyžadují DO/DROP rozhodnutí (smazat vs. archive vs. keep s DD). AskUserQuestion 3 otázky paralelně:
- F3: smazat / archive / keep s DD → **smazat** (KISS, izomorfismus s tree_sway/factory toy mazáním).
- F4: smazat / keep s explicit komentářem / keep WIND smaz TIME_SCALE → **smazat celou WORLD**.
- Topic branch: `feat/audit-29-cleanup` / direct na main → **topic branch** (per sez. 21+27 workflow).

### Rozhodnutí

- **Topic branch `feat/audit-29-cleanup`** z main (per sez. 21 DD-30 workflow + sez. 27 censure).
- **F4 (WORLD smaz)** — `model.js` smazat třídu (−33 ř.) + komentář, `main.js` smazat instanci + `window.world` expose + import. Žádné placeholder pro budoucí konzument — re-add z git historie (DD-29, sez. 20) v případě potřeby.
- **F3 (VOXEL_MODEL smaz)** — `model.js` smazat třídu, `main.js` smazat `buildVoxelModel` + `createCompositeFor` + COMPOSITES dispatch + OBJLoader/MTLLoader importy, smazat `tools/` + `assets/` adresáře (vč. `.vox` šablon). COMPOSITES vrstva v modelu zůstává abstract bez konkrétních potomků (slot pro budoucí ne-BLOCKS 3D entity).
- **F1 (GLOSSARY rewrite)** — kompletní rewrite místo per-section edit (286 ř., 6 sezení pozadu = víc změn než zachovaných pasáží). Nová struktura: Model (kořenové třídy / BLOCKS rodina / ORIENTATION / ostatní CUBES potomky / PATH / nevizuální), Čas + ANIMATE.kindy, Terrain generator (parametry, biome IDs, ramp smoothing), Vizuální zdroje (TCUBES atlas DD-36, `:named-texture`, SPRITES), Y konvence, Pojmy, UI, Milníky.
- **F2 (DD-36 napsán)** — formální záznam atlas refactoru. Reference: DD-32 (kontext), DD-25 (rodina), DD-14 (puvodní dispatch). Sekce *Známá omezení* zaznamenává atlas/slow-path divergence (carry forward do TODO follow-up).
- **README + CLAUDE.md update** — hierarchie modelu bez WORLD a VOXEL_MODEL, milestones DD-33..36, plán otevřené body. CLAUDE.md Key Files bez `tools/` řádku, doplněn `#perf-hud` do HUD popisu + DD-36 a ramp smoothing do main.js/terrain.js popisů.
- **Žádný nový DD nad DD-36.** Audit cleanup je operační, ne arch.

### Kód

Diff stat: 11 files, +166/−743 = **−577 ř.** celkem.

- **`src/model.js`** −67 ř. (VOXEL_MODEL + WORLD).
- **`src/main.js`** −81 ř. (`buildVoxelModel`, `createCompositeFor`, OBJLoader/MTLLoader imports, WORLD instance + window.world, COMPOSITES dispatch v createMeshFor, hover komentář o VOXEL_MODEL).
- **`tools/`** smazán celý (3 mjs, 385 ř.).
- **`assets/`** smazán celý (2 .vox binární).
- **`docs/GLOSSARY.md`** přepis 286 → ~250 ř. (net −131 změněných řádků na 281 řádek diff hlavičky).
- **`docs/DESIGN_DECISIONS.md`** +47 ř. (DD-36).
- **`README.md`** +5/−42 ř. (Status sekce, hierarchie, milníky, plán).
- **`CLAUDE.md`** +5/−6 ř. (Key Files, DD rozsah).

Node syntax check ✓ na všech 3 src souborech. Smoke test (task #4) — user implicitně potvrdil přes `%END` (browser test akceptován bez reportu errors).

### Kudos / Censure

- **Kudos! (user → AI flow):** plán+Q→C+sub-decisions→Go pattern (`feedback_plan_then_go` memory v praxi). Rychlý batch 4 kritických nálezů v jednom rolu, žádné prodlevy v rozhodování.
- **Kudos! (user, pre-flight discipline):** akceptoval pre-flight visual parity check před AUDIT:CODE batchem. Bez něj by atlas/slow-path divergence (F5) potenciálně zaregistrována jako issue až po commitu.
- **Censure! (AI → AI):** Agent A v auditu tvrdil *„TDRAMP úplně chybí v GLOSSARY"*, ale TDRAMP tam byl (řádek 21 BLOCKS rodina sekce s plnou definicí). Nedostatečná kontrola agent reportu před prezentací user — měla jsem v dedup fázi grepnout TDRAMP v GLOSSARY a falsifikovat nález. Pattern: subagent halucinuje, raw report bez verifikace = false negative do user-facing souhrnu.
- **Kudos! (AI, audit pattern):** 3 paralelní agenti s rozdělenými fokusy efektivnější než single-agent dlouhý audit (paralelismus + per-agent kontextová zaměřenost na svou specialitu). 36 raw → 14 silných po dedup je rozumný recall.

### Příště (sez. 29 → sez. 30)

**Cadence reset:**
- **`%AUDIT:CODE`** — 0/8 sezení. Další doporučený sez. 37+.
- **`%AUDIT:DOCS`** — 0/10 sezení (GLOSSARY rewrite + DD-36 = doc audit ekvivalent). Další doporučený sez. 39+.
- **IDEAS/TODO pruning** — 10/12.

**Otevřené (z TODO sez. 29):**

- [!] **PROMPTS.md `%END` aktualizovat o topic-branch workflow** — sub-prah governance, stále z TODO sez. 27. Krátký Edit, kandidát pro sez. 30 první krok.
- [ ] **Rampy atlas refactor** (TRRAMPS / TTRAMPS / TDRAMP / TTUNELS) — sez. 30 follow-up perf, 4× repeat patternu sez. 28, ~17 % incremental redukce calls.
- [ ] **Audit follow-up F5/F6/F9/F10** doporučené + F11/F12/F14 kosmetické.

**Multi-instance:** Sez. 29 single-instance (mrkla stroj). `feat/audit-29-cleanup` mergnut do main na konci %END.

## Sezení 30

### Diskuse

**(A) `%BEGIN` čistý, server už běžel.**

Main `0afb0c8` (sez. 29 merge), žádné commity z jiného stroje. Server :8000 živý (PID 40544 zděděný z předchozího sezení). Příště sez. 29 → 30 mělo 3 položky: `[!]` PROMPTS.md %END topic-branch governance, rampy atlas refactor, audit F5/F6/F9/F10/F11/F12/F14. Doporučeno začít governance fixem (sub-prah, krátký Edit), pak větší úkol.

**(B) PROMPTS.md `%END` (3.) Git — topic-branch workflow update.**

Aktuální text *„Commit na větev `main`"* zastaralý od sez. 21 (DD-30 zavedl `feat/factory`+`feat/terrain`+`feat/terrain-perf`+`feat/audit-29-cleanup` jako default branching pattern). Censure sez. 27 + 28 stejně. Edit doplnil:

- `(3a.) Commit + push` na aktuální větev (default topic od DD-30, `feat/<topic>`).
- `(3b.) Merge rozhodnutí` *(jen pokud jsme na topic branch)* — explicit otázka *„úkol dokončen?"* + příkaz `git checkout main && git merge --no-ff feat/<topic> -m "…" && git push`.
- Zdůvodnění `--no-ff`: drží hranici topic branch viditelnou v `git log` (bez něj fast-forward zploští historii).

`+13/−2` v `docs/PROMPTS.md`. Sub-prah governance ze sez. 27→28→29→30 (4× v Příště, stále pod stale prahem 5 ale blízko).

**(C) Rampy atlas refactor — plán+Q→Go.**

Po PROMPTS.md fix uživatel volil refactor jako další (alternativy: F5/F6 cache izomorfismus / IDEAS pruning / nový směr). Pre-flight grep odhalil současný stav: TRRAMPS/TTRAMPS/TDRAMP mají per-instance `materials[N]` array (5/4/5 přes `faceMaterialFor`) + IIFE-cached geom s per-face `material groups`. TTUNELS = identický pattern, ale **0 instancí ve scéně** (model existuje od sez. 16, terrain ho negeneruje, žádná static scéna). Ramp surface set: 3 fixed (`grass`/`stone`/`sand`) z `RAMP_*_TEXTURES` konstant.

Plán izomorfně s DD-36 TCUBES atlas: per typ TRRAMPS/TTRAMPS/TDRAMP → sdílená atlas geom (UV remap na 1/N-tici U-axis, single material group) + per-(typ, surface) atlas material (canvas N×16 × 16 px, N facelet `:named-texture` factories slepené horizontálně, `NearestFilter` + `SRGBColorSpace`). Detection surface z `instance.KIND` (`"ramp_grass"` → `"grass"`).

3 user Q:
- Q1 Topic branch — **A: feat/ramps-atlas (DD-30)** vs. **B: přímo na main**. User volba **B: přímo na main** (KISS, atomický refactor s jasným `git revert` fallback).
- Q2 TTUNELS — **A: skip** (0 ROI) vs. **B: preemptive refactor**. User volba **A: skip**.
- Q3 Slow path — **A: zachovat izomorfně s DD-36** vs. **B: odstranit** (žádný producer non-fixed surfaces). User volba **B: odstranit**.

Plán Go.

**(D) Implementace — 7 Edit batch.**

1. `TRRAMP_GEOM_CACHE` IIFE transform — `addQuad/addTri` helpers signatura `materialIndex` → `faceIdx`, nová `remapU(uv, faceIdx)` inline (`faceIdx/N + uv[0]/N`), `groups.push`/`for...addGroup` smazáno. Komentář u TRRAMPS docstring „5 faces, každý má vlastní material" → „5 faces se single-material atlas mapováním".
2. `createTRRampFor` přepis — `getRampAtlasMaterial("trramps", surface)` + `new Mesh(geom, mat)`. Žádný `materials[5]` array, žádné `faceMaterialFor` volání.
3. `TTRAMP_GEOM_CACHE` + `createTTRampFor` — totéž (N=4).
4. `TDRAMP_GEOM_CACHE` + `createTDRampFor` — totéž (N=5, sdílí slot mezi 2 quady WALL_FULL E+S a 2 trii WALL_TRI N+W — atlas má 5 unique slotů, 7 geom face).
5. Nová sekce `=== Ramp atlas pipeline (DD-36) ===` za `RAMP_DIAGONAL_TEXTURES`: `RAMP_SURFACE_FROM_KIND` (3 keys), `RAMP_ATLAS_SPECS` per typ (keys + texTable), `_rampsAtlasMatCache` Map, `getRampAtlasMaterial(type, surface)` factory — N facelet canvas slepení izomorfně s `getTcubesKindMaterial`. 9 unique kombinací = 9 atlas materials lazy-cached.

Smoke test: `node --check src/main.js` OK. Module load `terrain.js`: 321 blocks + 5 water + 34 ramps (default seed 42). User browser test: **vše OK, hover OK, ORIENTATION OK, vizuální parita OK**.

**Výsledky 30×30:**

| metrika | sez. 28 baseline | sez. 30 ramp atlas | změna |
|---|---|---|---|
| FPS | 92 | **123** | +34 % |
| draw calls | ~5050 | 4290 | −14 % |
| `mat` HUD | 4 | 0 *(F9 bug)* → 7 *(po fix)* | — |

Odhad TODO (~17 % incremental) → reálných 14 %, drobné rozdíl ale na řádu.

**(E) F9 perf HUD `mat` = součet 3 cache.**

User upozornil v reportu `mat: 0` po refactoru. Pre-existing F9 (sez. 29 audit follow-up) — HUD měřil jen `_faceMaterialCache.size`, ale po atlas TCUBES + rampy se slow path prakticky nikdy nevolá (TTUNELS bez producenta, TCUBES non-terrain emoji demo bez instance). Edit jednořádkový:

```js
_perfHud.el.mat.textContent = _faceMaterialCache.size
                            + _tcubesAtlasMatCache.size
                            + _rampsAtlasMatCache.size;
```

Souvisí přímo s touhle změnou (přidání `_rampsAtlasMatCache`), žádný feature creep. F9 vyřešen ve stejném commitu.

**(F) Commit 1 + push: `2f6ac79`.**

`feat(perf): rampy atlas refactor + F9 perf HUD mat součet + PROMPTS.md %END topic-branch (sez. 30)`. Diff `+143/−67`, 2 files (`docs/PROMPTS.md` + `src/main.js`). Push na main úspěšný.

**(G) Size 100×100 user request.**

*„Zkus umožnit v generátoru SIZE 100×100"*. Pre-edit analýza:
- Strop hardcoded v `index.html` slidery `max="30"` (×2).
- `terrain.js` validation open-ended (jen `sx > 0`).
- Lineární odhad 30×30 → 100×100 (11× cells): FPS 123 → ~11, calls 4290 → ~47k. CPU bound (draw call overhead), GPU si lebedí (tri scale linearly s počtem cells).
- **F12** (sez. 29 kosmetické TODO) by 100×100 zhoršilo bez fixu — shadow frustum hardcoded ±8 → stíny by pokrývaly jen centrální 1/6 mapy. Logicky souvisí, F12 fix preemptive.

Plán: 2 změny HTML (slider max 30 → 100, ×2) + F12 fix reaktivní (`updateShadowFrustum(maxDim)` helper volaný z `spawnTerrain`). Žádné nové Q, user jasně chce 100×100.

**(H) F12 fix — `updateShadowFrustum(maxDim)` helper.**

Definovaný hned za `sun.shadow.normalBias = 0` setup: `half = ceil(maxDim/2) + 4` buffer pro vyšší objekty (balon, sprites), mutuje `sun.shadow.camera.left/right/top/bottom` + `updateProjectionMatrix()`. Volání na konci `spawnTerrain(params)`: `updateShadowFrustum(Math.max(params.size[0], params.size[1]))`. Module-load default ±8 zachován jako fallback před prvním `buildScene` (technicky se nikdy nepoužije — `buildScene` volá `spawnTerrain(TERRAIN_DEFAULTS)`).

Reaktivní místo statické konstanty `SHADOW_FRUSTUM = 30` proto, že malé scény (10×10) by zbytečně rozpouštěly shadow map detail přes velký frustum. Reactive scaling drží `2048² shadow map / (2 × half)` ratio per scéně.

**(I) 100×100 user stress test.**

User v `#terrainctrl` panelu táhl X+Z na 100. Výsledky:

| metrika | hodnota | odhad | poznámka |
|---|---|---|---|
| FPS | **7** | ~11 | horší než lineární, CPU bound dominance |
| calls | **47 642** | ~47k | matchne lineárně |
| tri | **549 698** | — | GPU easily zvládá |
| `geom` | **8** | konstantní | sdílení optimální |
| `mat` | **7** | konstantní | atlas constant per (typ, surface) |

**Atlas pipeline exhaust:** sdílení geom + material funguje optimálně (8 + 7 constant napříč scale), ale **1 `THREE.Mesh` objekt = 1 draw call** v Three.js — atlas merger materials, ne instances. Dalším skokem je **abstrakce instances v 1 draw call** = `InstancedMesh` (per atlas material → 1 batch, ~13 celkem pro TCUBES + rampy) nebo `BatchedMesh` (r167+, Three.js v projektu r160 = bump nutný).

Predikce InstancedMesh 100×100: 47k → ~13 calls → **FPS ~150+** (GPU bound, 549k tri easily).

### Rozhodnutí

**Žádný nový DD.** Ramp atlas pipeline = pokračování DD-36 konceptu na další BLOCKS potomky. F9 + F12 = audit follow-up fix, žádné architektonické změny. Size 100×100 = UX uvolnění, žádný rozhodovací moment.

**InstancedMesh refactor kandidát na DD-37** — sez. 31+, po `%THINK` (pool strategy: per atlas material? per kind? regen cost? hover instanceId pattern?).

### Kód

- **`src/main.js`** — 3× IIFE geom transform (TRRAMP/TTRAMP/TDRAMP), 3× `createXxxRampFor` přepis na atlas-only, nová sekce `=== Ramp atlas pipeline (DD-36) ===` s `getRampAtlasMaterial` generický factory, F9 fix v perf HUD report (3 cache součet), F12 fix `updateShadowFrustum(maxDim)` helper + call ze `spawnTerrain`.
- **`index.html`** — 2× slider `max="30"` → `max="100"` (`tc-size-x`, `tc-size-z`).
- **`docs/PROMPTS.md`** — `%END` (3.) Git topic-branch workflow update.
- **`docs/TODO.md`** — odstraněny hotové [!] PROMPTS.md + Rampy atlas + F9 + F12, přidán [!] InstancedMesh refactor (DD-37 kandidát), audit cadence reset 1/8 + 1/10.
- **`docs/DONE.md`** — nová sekce „Sezení 30" s 4 bloky.
- **`docs/DIARY.md`** — index řádek 2026-05-13 prodloužen o sez. 30.
- **`docs/GLOSSARY.md`** — DD-36 zmínka rozšířena o rampy, slider rozsah `3..100`.
- **`docs/IDEAS.md`** — Performance optimalizace sekce aktualizována o sez. 30 ramp atlas výsledky a 100×100 stress test conclusions.

### Kudos / Censure

**Kudos! (user → AI flow):** Plán+Q→Go pattern dvakrát (rampy atlas s 3 explicit Q, size 100 bez Q — user jasná žádost). Realistický odhad FPS 100×100 (lineární ~11) blízko reálných 7 — drift dán CPU/GPU bound switch, nepředpovězen přesně ale řád seděl.

**Censure! (AI → AI):** `%BEGIN` nepoložil pre-flight ramp atlas analytický check *„kolik unique combos a kolik draw calls per ramp instance?"* — bez té analýzy by Q1/Q2/Q3 byly hypotetické. Naštěstí grep před plánem zachytil sám.

**Censure! (AI → AI, drobné):** Slow path TTUNELS po user volbě „odstranit slow path pro rampy" zůstal (`createTTunnelFor` line ~1680-1690 stále volá `faceMaterialFor`). To je správně (TTUNELS = jiný typ, nedotčen refactorem), ale komentář v sez. 29 audit follow-up F6 už mluvil o sjednocení API (`materialFor(spec)` discriminated union) — TTUNELS slow path je teď osamělý konzument `_faceMaterialCache` pro non-TCUBES. F6 sílí.

### Příště (sez. 30 → sez. 31)

**Cadence:**
- **`%AUDIT:CODE`** — 1/8 sezení. Další doporučený sez. 37+.
- **`%AUDIT:DOCS`** — 1/10 sezení. Další doporučený sez. 39+.
- **IDEAS/TODO pruning** — 11/12 (po sez. 30 přesun hotových do DONE).

**Otevřené (TODO update):**
- [!] **InstancedMesh refactor** — sez. 30 100×100 stress test ukázal, že další perf skok je tady. Plánování přes `%THINK`: pool strategy, hover refactor, regen cost. Predikce 100×100 FPS 7 → 150+. DD-37 kandidát.
- [ ] **Audit follow-up F5/F6/F10** doporučené + F11/F14 kosmetické.

**Multi-instance:** Sez. 30 single-instance (mrkla stroj). `main` přímo bez topic branche (user KISS volba — atomický refactor, snadný revert).

## Sezení 31

### Diskuse

**(A) `%BEGIN` čistý, návrh `%THINK` pro InstancedMesh.**

Main `b986464` (sez. 30 close), clean. Server :8000 živý (PID 40544). Příště sez. 30 → 31 [!] **InstancedMesh refactor** s předpokladem `%THINK` před implementací. User volba **1** = `%THINK`.

**(B) `%THINK` InstancedMesh — 6 bodů + 3 Q.**

Pre-flight grep `createTCubeFor` / `spawnTerrain` / `regenerateScene` / `setHoverHighlight` / `pointermove`. Argumentace:

1. **Architektura** — batch dispatcher mezi `spawnTerrain` a `scene.add`. 1 `InstancedMesh` per `(geom, atlas material)` pár. 13 batchů max (4 TCUBES + 9 ramps = 3 typy × 3 surfaces). Water plane(y) zůstávají single-mesh (low count typically <10).
2. **KISS / DRY / izomorfismus** — DRY atlas mat cache + nová batch cache. Izomorfní contract. First-things-first: hover redesign je **předpoklad**, ne add-on (jinak hover zhasne pro celý terrain). Pořadí: hover model → pool → spawn → regen → tooltip.
3. **Alternativy 4:**
   - **(A) InstancedMesh per (geom, mat).** 13 batchů, stable Three.js r129+ (`setMatrixAt` + `setColorAt`). **Doporučeno.**
   - **(B) BatchedMesh (r167+).** TCUBES + rampy → ~3 calls, ale projekt na r160 (dep bump). Menší community.
   - **(C) Web worker generace.** Sez. 28 baseline `generateTerrain = 2.5 ms @ 30×30` — NENI bottleneck. Skip.
   - **(D) Mesh merge per cell.** Zruší per-instance hover → zruší editor cíl ze sez. 9. Skip.
4. **Rizika:**
   - **Hover regrese** — emissive boost nepřenositelný do batche bez custom shader. Compromise: albedo tint via `instanceColor` (jemnější).
   - **Frustum culling** — 1 bounding box per batch, 10×10 plýtvání, neproblém.
   - **Tooltip lookup** — `hit.object.userData.instance` neexistuje na batchi; refactor přes `instancesByIdx[hit.instanceId]` reverse map.
   - **F10 hover-clone leak** — zmizí (jiný mechanismus).
5. **Procedural** — vyjádřitelné stávajícím modelem bez rozšíření. ✓

3 Q: **Q1 hover light** (A albedo `instanceColor` / B custom shader emissive), **Q2 implementační pořadí** (A 2 sezení / B 1 sezení), **Q3 topic branch** (A `feat/instanced-mesh` / B přímo main). User **A1A A2B A3B** = albedo tint + 1 sezení + main.

**(C) Implementace — 6 Edit batch.**

1. Sekce `=== Terrain InstancedMesh batches ===` za `getRampAtlasMaterial` (~50 ř.) — `_terrainBatches` Map, `HOVER_TINT_COLOR = (1, 1, 0.55)` (initial), `_batchColorWhite`, scratch `_batchMatrix/Pos/Euler/Quat/Scale`, `createTerrainBatch(key, geom, mat, capacity)`, `pushInstanceToBatch(batch, instance, x, y, z, rotY)`.
2. `spawnTerrain` 3-pass přepis: count → allocate batches → fill loop + flush `instanceMatrix/Color.needsUpdate`. Water plane(y) zůstávají single-mesh.
3. `regenerateScene` přepsáno na batch-dispose: vyčisti `meshByInstance` z `instancesByIdx`, `scene.remove(batch) + batch.dispose()`, `_terrainBatches.clear()`, water single-mesh filter, pak `spawnTerrain`.
4. `setHoverHighlight` discriminated union: `if (ref.batch)` → batch case `setColorAt(idx, HOVER_TINT_COLOR) + instanceColor.needsUpdate`; jinak legacy single-mesh emissive boost (lazy clone-on-first-hover).
5. `resolveInstanceFromHit(hit)` helper + pointermove refactor: `hit.object.isInstancedMesh` → `instancesByIdx[hit.instanceId]`; jinak `hit.object.userData.instance`.
6. `showTooltip` skip-check `lastHoveredInstance !== instance` místo `lastHoveredMesh.userData.instance` (bug — InstancedMesh nemá `userData.instance`, per-frame DOM mutate).

Smoke test `node --check` ✓.

**(D) User browser test 100×100 — výsledky.**

| metrika | sez. 30 | sez. 31 | změna |
|---|---|---|---|
| FPS @ 100×100 | 7 | **104** | 14.9× |
| draw calls | 47 642 | **1 016** | 46.9× |
| triangles | 549 698 | 549 698 | shoda |
| `geom` (info.memory) | 8 | 8 | konstantní |
| `mat` | 7 | 13 | +6 ramp atlas materials |

Predikce v `%THINK` 150+ FPS realita 104. Drift dán **GPU bound** (549k tri × shadow pass × DOF post-process), nikoli draw call limitem.

**(E) User feedback hover „není patrný".**

`HOVER_TINT_COLOR = (1.0, 1.0, 0.55)` = bledá žlutá. Multiplikace albedo (values 0..1) ztmavuje, nerozzáří. **Trik:** `Float32Array` v `InstancedBufferAttribute` nemá clamp → values > 1.0 = albedo overbright. Edit `(1.6, 0.8, 0.2)` = R/G boost + B redukce = sytá oranžová. User po refresh ✓.

**(F) User feedback ramp smoothing — Stage 2 TDRAMP → TTRAMP (slepá ulička).**

User: *„Koukám, že vyhlazování terénu by si zasloužilo ještě jeden průchod. Ve scéně je možné přidat velké množství TTRAMPS např. -7,1,9."*

Pre-flight Node verify default seed 42 100×100: cell (-7,9) y_top=−1 sand, ne 1. User clarification: ramp coords (-7, 0, 9) = ramp Y= y_top+1 na cellu (-7,9). Sousedi:
- Direct S (-7,8) y_top=0 sand
- Direct W (-8,9) y_top=0 sand
- Diag SW (-8,8) y_top=1 stone (= +2 nad cell A, ne +1)

Algorithm: `highDirs = [S, W]`, Stage 1 (3-cell convex peak) fail (diag +2 ne +1), Stage 2 L-shape TDRAMP. User hypotéza: vrátit Stage 2 zpět na TTRAMP (sez. 26 raná iterace, *„TTRAMPS 0 new walls vs. TDRAMP 1.0"* per kvantitativní bilance).

Implementace `terrain.js` Edit Stage 2 → TTRAMP corner s direct-soused surface match (b1/b2 fallback, ne diag). Verify 100×100 seed 42: TDRAMP 848 → 783, TTRAMPS 548 → **613** (+65 L-shape cells přepnutých). Cell (-7,9) corner sand orient 0 ✓.

User vizuální verdict: *„Nn, vrať se k předchozí verzi, tohle je špatně"* → revert `terrain.js`. Sez. 26 final volba TDRAMP na L-shape platí dál. **Žádný code change v finální commitu sez. 31** ze strany ramp logiky.

**(G) Sun mesh — bílá koule.**

User: *„V některých Three.js projektech jsem viděl zobrazené světlo/slunce jako bílou (velmi vzdálenou) kouli. Uměl bys také?"*

Standardní pattern: `SphereGeometry + MeshBasicMaterial` (unlit, ignoruje DirectionalLight = „svítí" vlastní jas). Q1=A realistická (radius ~1-2, vzdálenost 50+), Q2=A statická.

Implementace: `sunMesh = THREE.Mesh(SphereGeometry(1.5, 16, 16), MeshBasicMaterial(color: 0xffffff))` na `sun.position × SUN_DISTANCE_SCALE=5` = (-50, 40, 50). První iteration `material.fog` neset → sun by se rozplynul do mlhy (po fog Edit). Po user iteration `radius 3 → 1.5`, `scale 8 → 5`.

**(H) Post-process — atmospheric fog + DOF (BokehPass).**

User: *„Uměl bys také přidat efekt, nevím jak se jmenuje, ale trochu se rozostří pozadí i popředí."*

Diagnose: efekt = **Depth of Field**. Three.js `BokehPass` přes post-processing pipeline (`EffectComposer`). Pre-flight tradeoff: pixel-art voxel scéna × Gaussian blur = riziko rozmazaného vzhledu. Alternativa atmospheric fog (vzdálené blednou, popředí ostré).

Q1=C obojí (fog + DOF), Q2=A center focus.

Implementace:
- Imports `EffectComposer` / `RenderPass` / `BokehPass` / `OutputPass` z `three/addons/postprocessing/`.
- `scene.fog = new THREE.Fog(SKY_COLOR, 30, 120)` lineární mlha matchne pozadí. `sceneFog` reference držena.
- `MeshBasicMaterial(fog: false)` override pro sunMesh (jinak by se rozplynula).
- Composer setup: RenderPass → BokehPass(focus=10 placeholder, aperture=0.0005, maxblur=0.005) → OutputPass.
- Dynamic focus v animate(): `bokehPass.uniforms.focus.value = camera.position.distanceTo(controls.target)`.
- Resize handler `composer.setSize`.
- Render loop `composer.render()` místo `renderer.render(scene, camera)`.

**(I) Settings panel `#settings` — toggle UI.**

User: *„Bude to toggle volba zobrazení. Nový panel SETTINGS."*. Q1=C (DOF + Fog + Sun 3 toggles), Q2=A (vše ON default).

- `window.settings = { setDOF(on), setFog(on), setSun(on) }` API v `main.js`. `bokehPass.enabled` / `scene.fog = sceneFog|null` / `sunMesh.visible`. Restore semantika via `sceneFog` reference (cheap toggle, žádná realokace).
- HTML `#settings` panel **levý dolní roh** (volný kout — 4 rohy: HUD top-left, perfhud top-right, terrainctrl bottom-right, settings bottom-left). Izomorfní CSS: coral akcent header, tyrkysový accent checkbox.
- Wire inline script: 3 checkbox `change` event → `window.settings.setXxx(checked)`.

**(J) Cleanup `lastHoveredMesh` dead code.**

Po refactoru `lastHoveredMesh` se v pointermove psala (`firstHit.object`), ale po `showTooltip` refactoru na `lastHoveredInstance !== instance` se nikde nečetla. Smazán declaration + 2 write sites.

### Rozhodnutí

- **DD-37 InstancedMesh batch pipeline pro terrain** zapsán. Architektonický kontrakt: per (geom, atlas mat) batch, `meshByInstance` discriminated union, hover přes albedo overbright. Pokračování DD-36, eliminuje draw call ceiling.
- **Žádné další DD.** Sun mesh + post-process (fog/DOF) + settings panel = čistě vizuální features, žádný architektonický kontrakt. Stage 2 TDRAMP → TTRAMP revertnut, sez. 26 final volba platí.

### Kód

**Sez. 31 commit** (uncommitted před `%END`):
- `src/main.js`: +~250/−40 (sekce `=== Terrain InstancedMesh batches ===`, `spawnTerrain` 3-pass přepis, `regenerateScene` batch-dispose, `setHoverHighlight` union, `resolveInstanceFromHit`, post-process composer setup, `sunMesh`, `sceneFog`, `window.settings`, dead-code cleanup).
- `index.html`: +~65/−5 (`#settings` CSS + HTML + wire script).
- `docs/*`: ~+220 ř. (DD-37 v DESIGN_DECISIONS, DONE.md sez. 31 sekce, TODO update, DIARY index, GLOSSARY update).

### Kudos / Censure

- **Kudos! (user → AI flow):** plán+Q→Go pattern několikrát v jednom sezení — InstancedMesh `A1A A2B A3B`, sun `A1A A2A`, DOF `A1C A2A`, settings `A1C A2A`. `feedback_plan_then_go` v praxi, žádné prokrastinování. User clean reads krátké Q strukturou (A/B/C) a odpovídal kompaktně.
- **Kudos! (user, vizuální revize Stage 2):** po implementaci + Node verify TTRAMP cell (-7,9) sand orient 0 ✓, user udělal browser test, viděl celkem-scenu změnu, rozhodl *„špatně, revert"*. Místo *„počkám"* (ego zachovat investovaný refactor) realisticky revertl 1 sezení staré rozhodnutí. Zachoval *„sez. 26 final volba platí"* — ne potřeba retroaktivní zápis.
- **Censure! (AI → AI):** `%BEGIN` Stage 2 změnu navrhl + implementoval bez **pre-flight vizuální revize** (per `feedback_subagent_verify` memory — Node counts ≠ render diff). Měl jsem ukázat side-by-side screenshot, nebo aspoň zmínit *„změna ovlivní 65 cells, prosím vizuálně otestuj před commitem"*. User musel diff identifikovat *po* commit-ready stavu = wasted Edit cycle.
- **Censure! (AI → AI):** `%THINK` predikce FPS 100×100 = 150+. Realita 104. Drift dán nepředpovězeným GPU bound switchem (DOF + shadow). Měl jsem zmínit *„predikce platí pro CPU-bound případ; pokud GPU work roste, predikce klesá"*. Bilance řádově OK (10× skok predikt, 15× realita), ale precision lower than sez. 30 (lineární 11 vs. realita 7 byl bližší fit).

### Příště (sez. 31 → sez. 32)

**Cadence:**
- **`%AUDIT:CODE`** — 2/8 sezení od sez. 29. Další doporučený sez. 37+.
- **`%AUDIT:DOCS`** — 2/10 sezení od sez. 29. Další doporučený sez. 39+.
- **IDEAS/TODO pruning** — 12/12 (po sez. 31 přesun hotových do DONE).

**Otevřené (TODO update):**
- [ ] **Audit follow-up F5/F6/F10** doporučené + F11/F14 kosmetické.
- [ ] **BatchedMesh refactor** kandidát (Three.js r167+, 13 → ~3 calls) — diminishing returns, low prio.
- [ ] **DAY/SUN_ANGLE animace sun mesh** — `updateSunMesh()` reactive `sun.position.copy(...).multiplyScalar(SUN_DISTANCE_SCALE)` driven z WORLD attributu (DD-29 politika, gating konzumentem). Plus directional light pohyb (azimuth/elevation per DAY phase).

**Multi-instance:** Sez. 31 single-instance (mrkla stroj). `main` přímo bez topic branche (KISS, atomický refactor s `git revert` fallback).

## Sezení 32

### Diskuse

**(A) `%BEGIN` clean, server :8000 živý (PID 40544), Příště 3 body** — (1) Audit follow-up F5/F6/F10 + F11/F14, (2) BatchedMesh refactor low prio, (3) DAY/SUN_ANGLE animace sun mesh. AI návrh **3** (vizuální payoff + DD-29 izomorfismus), user volba `1 a 2`. AI oponoval **2** (BatchedMesh):
- Diminishing returns po DD-37 — z 1016 → ~3 calls je už dole na ploše křivky; predikce 104 → ~108 FPS, nebo i méně (BatchedMesh setup overhead).
- Three.js r160 → r167+ bump = wide risk surface (ColorManagement default, WebGPU split, post-process API změny); `EffectComposer` + `BokehPass` ze sez. 31 by se mohly chytit.
- Hover/raycast pipeline doladěná v sez. 31 (`meshByInstance` discriminated union + `resolveInstanceFromHit`); BatchedMesh per-instance geom má jiný raycast model.

User `A1 1+3` přijal.

**(B) Bod 1 — Audit follow-up (5 nálezů).** Ověření v kódu nejdřív (grep `_b`, `_dir`, RELIEF clamp, hover-clone path, NAMED_TEXTURE_FACTORIES call-sites). Single Q1 = F6 přístup (doc komentář vs. refactor vs. odložit). User volba **A doc komentář**. Pak strukturovaný 5-task list (TaskCreate), Stage 1 (F11+F14 kosmetické) → Stage 2 (F10+F5 doporučené) → Stage 3 (F6 doc).

- **F11** `main.js` — `_b` Vector3 (řádek 536) grep potvrdil 0 čtecích sitů, pozůstatek `balloon_bob` (smazaný sez. 15 DD-23). Smazán + komentář na :530 zúžen (`_dir` a `_a`); komentář u `_dir` na :854 "(reuse scratch z balloon animátoru)" refrázován na "(scratch vektor, viz `_up`/`_dir`/`_a` výše)".
- **F14** `terrain.js` — `RELIEF_AMPLITUDE = [0, 0, 1, 1, 2, 3, 4, 5, 6, 6, 6]` indexy 9/10 duplikovaly hodnotu 8, paralelně existoval `Math.min(relief, 8)` clamp + warn na :117-122. **Dva mechanismy pro stejnou věc** (SSoT). Volba: smazat indexy 9/10 z pole → pole 0..8 (délka 9), clamp+warn drží jako *záměr* "9/10 zatím nepodporujeme". `RELIEF_FREQUENCY` totéž. Komentář na :70-74 upraven (`0..8` místo `0..10`, "podporované indexy (SSoT)").
- **F10** `main.js` — `setHoverHighlight(_, true)` v single-mesh path klonuje material na `userData.hoverHotMat`; `regenerateScene` ho nedispose při `scene.remove`. Po sez. 31 InstancedMesh refactoru batch case nepoužívá klon (overbright `setColorAt`), takže leak vznikal jen v single-mesh path (primárně water plane, ale teoreticky i slow-path TCUBES / PATH / sun mesh). Helper `disposeHoverClones(root)` extrahován (traverse + `mat.dispose?.() + userData.hoverHotMat = null`), volán ve staleWater cyklu před `scene.remove`. Sdílený `hoverOrigMat` nedispose — patří atlasu / sdílené factory.
- **F5** `main.js` — atlas (`getTcubesKindMaterial`, `getRampAtlasMaterial`) i slow path (`faceMaterialFor`) volaly `NAMED_TEXTURE_FACTORIES[name]()` přímo. Každé volání = nová `THREE.Texture` instance. Pro random-patch textury (`:dirt`, `:stone`, …) to znamenalo 2+ různé visual varianty pro stejný `:name`. Nový helper `getNamedTexture(name)` s `_namedTextureCache` Map memoizací (`null` cached miss zabrání spam warnů); 4 call-sites přepojeny — `faceMaterialFor` (`val.startsWith(":")` větev), `getTcubesKindMaterial` (atlas drawImage), `getRampAtlasMaterial` (atlas drawImage). **PATH zachován** (`_pathTexCache` s `wrapS/wrapT = RepeatWrapping` — jiné texture settings, sjednocení by způsobilo side effect na slow-path map mode).
- **F6** `main.js` — Hub doc komentář u `_faceMaterialCache` o 3 paralelních cache: (1) `_faceMaterialCache` = slow path, klíč = stringified `val`; (2) `_tcubesAtlasMatCache` = TCUBES atlas, klíč = kind; (3) `_rampsAtlasMatCache` = ramp atlas, klíč = `${type}_${surface}`. Důvod proč ne refactor: 3 různé klíče, 3 různé fáze pipeline, sjednocení by stačila discriminated union (~50 ř.) ale risk regrese na hover/raycast cestě sez. 31. Záměrně oddělené. Atlas cache dva mají 1-řádkový odkaz "Druhá/Třetí ze tří paralelních cache — viz komentář u `_faceMaterialCache` výše".

Syntax check ✓ na každém kroku. Test (`%TEST OK` user reload) ✓.

**(C) Bod 3 — DD-38 WORLD re-introduce.** `%THINK` strukturovaný:
- **Kontext z kódu** — WORLD smazána sez. 29 F4 (singleton bez konzumenta po DD-32 wipe `tree_sway`). Sez. 31 sun mesh komentář explicitně předjímal *„Až WORLD dostane TIME/SUN_ANGLE atribut..."*.
- **Architektura** — re-introduce WORLD bez X/Y/Z (DD-01 demonstrace). 2 živí konzumenti: sun pozice + intensity, render loop tick. **DD-38** založen.
- **Math sun position** — plane EAST-UP s 30° náklonem (severní polokoule v létě). Bez sklonu zenith → stíny svislé → nevizuální. Sklon drží stíny i v poledne.
- **Edge cases** — slunce pod zemí (Y<0): intensity lerp na 0 (`max(0, sin)`) = noc tmavá, scéna spoléhá na ambient (0.15) + post-process fog. Frame timing big dt (tab inactive): modulo `% 1` cyklic, stín se teleportuje, akceptovatelné.
- **Procedural check** — vyjádřitelné stávajícím modelem (WORLD jako DO + engine konzument = DD-29 pattern). Emergentní z parametrů (DAY_SPEED 0 = paused, 0.05 = 20 s cyklus).

4 mikro-Q:
- **Q1** atributy WORLD — **A** = `DAY + DAY_SPEED`.
- **Q2** default DAY_SPEED — **A** = 0 (paused).
- **Q3** light intensity lerping (noc tmavá) — **A** = ano.
- **Q4** UI slidery — **A** = ano v `#settings` panelu.

Implementace:
- `model.js` — `WORLD extends OBJECTS` třída +24 ř. (DAY 0.25 default = poledne, DAY_SPEED 0 default). Docstring odkazuje na DD-29 → DD-38 chain. Komentář v constructoru vysvětluje `% 1` neg-safe wrap.
- `main.js` — import `WORLD`, instance `world = new WORLD("world_0001", "Svět", ...)`, `window.world` dev exposure. Konstanty `SUN_BASE_INTENSITY = 0.8`, `SUN_DISTANCE = √(100+64+100) ≈ 16.25` (zachovat magnitudu původní pozice (-10,8,10) kvůli `shadow.camera.near=1, far=50`), `SUN_TILT = π/6` (30° sklon). Sun mesh komentář (sez. 31 *„Statická pozice — DAY/SEASON animace (Příště)..."*) přepsán na current state.
- `updateWorldTime(dt)` — `((world.DAY + dt * world.DAY_SPEED) % 1 + 1) % 1` (JS `%` vrací záporky pro záporná `t`). Skip při speed=0.
- `updateSun()` — derivuje pozici `sun.position.set(cos·dist, sin·dist·cos(tilt), sin·dist·sin(tilt))`, intensity `0.8 * max(0, sin)`, sun mesh `position.copy(sun.position).multiplyScalar(5)`, `visible = _sunUserVisible && y > 0`.
- Render loop — `updateAnimations(now) → updateWorldTime(dt) → updateSun() → updateBubbleTails() → updateKeyboardCamera(dt) → bokehPass.uniforms.focus = ... → composer.render()`.
- Settings API — `setDay(v)` / `setDaySpeed(v)` mutují `world.*`; `setSun(on)` z sez. 31 přepracován na `_sunUserVisible = on` flag (override AND s `sun.position.y > 0` v `updateSun()`).
- `index.html` — `#settings` panel +2 `.slider-row` divovce (DAY 0..0.999 step 0.01, DAY_SPEED 0..0.1 step 0.001) + numerické labely `<span id="set-day-val">0.25</span>`. CSS `.slider-row` izomorfně s `#terrainctrl .tc-row` (label nad inputem, value vpravo, tyrkysový accent). Wire script `input` event live: `parseFloat(e.target.value) → window.settings.setXxx(v) + label.textContent = v.toFixed(2/3)`.
- **DD-38** zapsán do `DESIGN_DECISIONS.md` (+50 ř.): kontext (DD-29 → DD-38 historie), rozhodnutí (6 bodů: třída, bez XYZ, konzumenti, math s tilt, dev exposure, UI), důvod (re-introduce po cleanup = strukturální, DAY první časový atribut mimo TIME singleton, sun direction render-derived = nový kontrakt), známá omezení (sklon fixed bez SEASON atributu, sky/ambient barva konstantní, slider DAY nesync při auto-advance, shadow frustum reactive jen na size), reference (DD-29, DD-01, sez. 29 F4, sez. 31).

### Rozhodnutí

- **F6 doc komentář** vs. refactor — KISS, hub komentář vysvětlí 3 sémantiky + důvod proč ne refactor. Atlas cache dva mají 1-řádkový odkaz na hub.
- **WORLD re-introduce** s 2 konzumenty (sun + render tick). DD-29 politika *"atribut přibude jen s živým konzumentem"* zachována, jen tentokrát celé DO vrátili.
- **Sun math** plane EAST-UP s 30° náklonem (fixed `SUN_TILT = π/6`). Realistická sférická dráha s SEASON = roadmap.
- **Light intensity lerping** ano (`max(0, sin)` = noc bez DirectionalLight, scéna na ambient + fog).
- **Slider sync z value** ne (per-frame DOM write drahé). Známé omezení.
- **DD-38** — re-introduce WORLD, atributy DAY+DAY_SPEED.

### Kód

- `model.js` — `WORLD extends OBJECTS` třída +24 ř.
- `main.js` — import WORLD, instance, `_namedTextureCache + getNamedTexture`, `disposeHoverClones`, F11 cleanup, sun konstanty + komentář update, `updateWorldTime + updateSun`, render loop, settings API rozšířeno, hub komentář F6.
- `terrain.js` — RELIEF SSoT (pole 0..8).
- `index.html` — 2 slidery + CSS `.slider-row` + wire script.
- `docs/DESIGN_DECISIONS.md` — DD-38 (+50 ř.).

### Kudos / Censure

- **Kudos! (user → AI flow):** plán+Q → Go pattern třikrát (bod 1 strukturovaný 5-task list + F6 single Q, bod 3 `%THINK` + 4 Q). Stručné `A1 1+3`, `A1=A`, `Test OK %END` — `feedback_plan_then_go` v praxi.
- **Kudos! (AI držela oponování):** user návrh 1+2 (BatchedMesh) byl odsouhlasen plánem oponovaný se 3 konkrétními argumenty (diminishing returns, version bump risk, hover regrese). User volba A1 = 1+3 = ROI win, sez. 32 by se jinak naplnil refactorem r160→r167+ s marginal payoff. CLAUDE.md 80/20 tonu drženo.
- **Censure! (AI → AI):** Slider DAY nesync při auto-advance je known UX limitation, mohl být zachycen v Q5 před implementací (např. *„Slider DAY se sync z value periodicky vs. statický"*). Nakonec zapsán jako známé omezení v DD-38 — dokumentace OK, ale Q5 by ušetřil "test → discovery". Plán+Q série měla 4 Q, 5. by zachytila.
- **Censure! (AI → AI):** F10 helper `disposeHoverClones` je volaný **jen** ve staleWater cyklu (`regenerateScene` krok 2), ne pro batchy v kroku 1. Důvod: batch case nepoužívá klon (overbright). Ale komentář u F10 by mohl být přesnější — "voláno tam, kde lazy clone path mohla proběhnout = single-mesh entity". Dnes water, zítra možná COMPOSITES — pak by se mělo volat i ze starého non-water cleanup. Vědomí omezení dokumentu rozsahu.

### Příště (sez. 32 → sez. 33)

**Cadence:**
- **`%AUDIT:CODE`** — 3/8 sezení od sez. 29. Další doporučený sez. 37+.
- **`%AUDIT:DOCS`** — 3/10 sezení od sez. 29. Další doporučený sez. 39+.
- **IDEAS/TODO pruning** — **13/12** (po sez. 32 přesun hotových do DONE — **překročen prah**, audit ideas/todo v sez. 33+).

**Otevřené (TODO update):**
- [ ] **BatchedMesh refactor** kandidát (Three.js r167+, 13 → ~3 calls) — diminishing returns, low prio. **Sez. 32 oponován pro tuto iteraci.**
- [ ] **SEASON atribut na WORLD** — driver pro sklon dráhy slunce (sférická dráha místo fixed `SUN_TILT = π/6`). Po DD-38 přirozený další konzument.
- [ ] **Sky/ambient color lerping** — `SKY_COLOR` + `sceneFog.color` reactive na DAY (sunset oranžová, noc tmavě modrá, poledne jasná). Driver = `DAY`. Out of scope sez. 32, kandidát sez. 33+.
- [ ] **Slider DAY sync z value** — drobnost UX, periodic update (throttle 4×/s) z `world.DAY` zpět do DOM. Kosmetické.

**Multi-instance:** Sez. 32 single-instance (mrkla stroj). `main` přímo bez topic branche (KISS, drobné izolované Edity + 1 strukturální DD-38).

## Sezení 33

### Diskuse

**(A) `%BEGIN` clean, IDEAS/TODO pruning prah.** Repo synchronizováno (`110fc8f` sez. 32), server :8000 už běžel (PID 40544). Cadence: AUDIT:CODE 3/8, AUDIT:DOCS 3/10 pod prahem; **IDEAS/TODO pruning 13/12 — překročen prah** (Příště sez. 32 explicitně eskaluje na sez. 33+). User volba: `IDEAS/TODO` jako první bod programu.

**(B) Pruning plán + 4 mikro-Q.** AI strukturoval 3 kategorie změn: (A) TODO add 3 body z Příště sez. 32 nereflektované v TODO (SEASON, Sky lerping, Slider DAY sync), (B) TODO drop 3 self-protiřečící body (WCUBES *„nápad, možná"* → není TODO ale IDEA; INVISIBLE *„možná zbytečné"*; CCUBES typizace *„překryto DD-24"*), (C) IDEAS změny (Voidspan sekce ~30ř obsolete po DD-32 wipe; Performance sekce smíšené DONE/TODO; SEASON dostane → TODO značku v WORLD rozšíření).

4 Q paralelně:
- **Q1 Voidspan**: A smazat / B zkrátit 3 ř. / C nechat. → **B**
- **Q2 Performance**: A smazat hotovou historii / B zhustit changelog / C nechat. → **B**
- **Q3 SEASON**: A přesunout do TODO / B `→ TODO` značka v IDEAS. → **B**
- **Q4 DROP položky**: A do DONE s důvodem / B neevidovat. → **B**

`A1B A2B A3B A4B` všechny minimal-impact varianty.

Implementace 5 Editů:
- **TODO** — drop 3 self-protiřečící z M8+, add nová sekce `## DAY-cycle (WORLD DD-38 follow-up)` s 2 body (Sky lerping + Slider sync; SEASON zůstal v IDEAS per A3B). Audit cadence IDEAS/TODO pruning čítač `13/12 → 0/12 (reset sez. 33)`.
- **IDEAS Voidspan** ~30 ř. → 3 ř. (kotva s odkazem na git history `feat/factory` + DD-30/31).
- **IDEAS Performance** ~25 ř. → ~7 ř. (changelog 1 odstavec: sez. 26 FPS 15 → sez. 28 atlas 92 → sez. 30 rampy atlas 123 → sez. 31 DD-37 InstancedMesh 100×100 z 7→104 + jen otevřené kandidáty BatchedMesh/Mesh merge).
- **IDEAS SEASON** dostal `→ TODO` značku v sekci WORLD rozšíření.

Net: TODO 15 → 14 položek (drop 3, add 2). IDEAS −45 ř. obsolete factory-toy.

**(C) Scene cleanup — orientační pomůcky + ground stíny.**

User: *„Odeber grid a vertex"* — `GridHelper(20×20)` + `AxesHelper(2)` v `buildScene()` smazány (3 řádky inline). User: *„Stíny scény se vytváří na neviditelné rovině Y=0. Odebrat, kolem je prázdnota."* — `ShadowMaterial` ground plane (`PlaneGeometry(20,20)` + `ShadowMaterial opacity 0.35`, Y=−0.501) smazán (14 řádků). Po DD-32 wipe (sez. 25) groundplane ztratil smysl — generated terrain má vlastní voxely, stíny existují per-voxel, ne na neviditelnou plochu.

**(D) `%THINK` — proč v noci stále vidím? Diagnose 3 zdrojů.**

User test DD-38 noc: *„Co svítí v noci za světlo? Proč je stále vidět?"*. Grep ukázal:
1. **`AmbientLight 0xffffff, 0.15`** (`main.js:73`) — fill light konstantní, DD-38 stmívá jen `DirectionalLight`. Odvrácené strany + zatažené plochy stále viditelné.
2. **`scene.background = SKY_COLOR`** — barva pozadí (`0x1a1a2e` tmavě modrofialová) konstantní. Sama neosvětluje, ale viditelné nebe v noci.
3. **`scene.fog = THREE.Fog(SKY_COLOR, 30, 120)`** — fog barva konstantní, vzdálenost splývá.

Diagnose mapuje na TODO bod `## DAY-cycle → Sky/ambient color lerping` z dnešního pruningu — natural next step. Plus AmbientLight intensity lerping (TODO text nezmiňoval explicit, ale logicky součást).

**(E) Sky/ambient color lerping plán + 4 Q.**

Architektura: driver `world.DAY`, 3 konzumenti (`ambientLight.intensity`, `scene.background.color`, `sceneFog.color`). Hookpoint nová funkce `updateAtmosphere()` po `updateSun()` v render loopu. Sdílí daylight = `max(0, sin(2π·DAY))` (DRY s `updateSun()` intensity).

4 Q:
- **Q1 keypointy**: A 2 barvy (KISS) / B 3 barvy se sunset oranžovou. → **A**
- **Q2 AmbientLight rozsah**: A 0.04 noc / B 0.02 / C 0.0. → **A**
- **Q3 současný `SKY_COLOR` jako**: A `SKY_DAY` / B baseline + odchylky. → **A**
- **Q4 driver curve**: A `max(0, sin(2π·DAY))` / B s tilt. → **A**

`A1A A2A A3A A4A` minimální KISS, sunset peak Q1B do TODO follow-up.

Implementace:
- `main.js` const block: `_skyDay = THREE.Color(0x1a1a2e)`, `_skyNight = THREE.Color(0x05080f)`, `AMBIENT_DAY = 0.15`, `AMBIENT_NIGHT = 0.04`. Rename `SKY_COLOR` smazán (placeholder `scene.background = THREE.Color().copy(_skyDay)`).
- `sceneFog = new THREE.Fog(_skyDay.getHex(), 30, 120)` — placeholder, updatuje `updateAtmosphere()`.
- `ambientLight` reference (z inline `scene.add(new THREE.AmbientLight(...))` → proměnná).
- `updateAtmosphere()` po `updateSun()`: `scene.background.lerpColors(_skyNight, _skyDay, daylight)` + `sceneFog.color.copy(scene.background)` + `ambientLight.intensity = AMBIENT_NIGHT + (AMBIENT_DAY - AMBIENT_NIGHT) * daylight`. Žádná alokace per-frame (`lerpColors` zapisuje do `this`).
- Render loop call `updateAtmosphere()` po `updateSun()` (řádek 2785+).
- Komentář v `updateSun()` aktualizován o ambient lerping.

**DD-39** zapsán: třetí konzument DD-38 `world.DAY`, izomorfní s `updateSun()` (stejná daylight curve). Strukturální (nový kontrakt: atmosféra reactive na DAY).

**(F) Tmavší noc + LAMP.**

User test: *„Klidně ještě ztmav noc. Přidej na -3,0 kompozit lampy, ať to vynikne."* Plán:
- Tmavší noc: `AMBIENT_NIGHT 0.04 → 0.015`, `_skyNight 0x05080f → 0x010207`.
- LAMP třída + builder + instance.

LAMP plán:
- `LAMP extends COMPOSITES` v `model.js`, žádné vlastní atributy (zdědí `ORIENTATION` z DD-26).
- `buildLamp(instance)` v `main.js`: sloupek (BoxGeometry 0.15×1.5×0.15 dark iron) + svítící hlava (BoxGeometry 0.4³ emissive 0xffaa00) + `PointLight(0xffaa00, 1.2, 8, castShadow=true)`.
- Dispatch v `createMeshFor` před `SPRITES` větev.
- Instance `lamp_0001` na `(-3, 0, 0)` v `buildScene()`.

Test po `DAY=0.75`: lampa svítí, ale **stín pod lampou**. User: *„Pozor, lampa musí být three.js zdrojem světla tj. vytvářet stíny. ne?"* — diagnostika: `light.castShadow=true` ano, ale **PointLight 360° + hlava lampy uvnitř** = hlava blokuje vlastní světlo (cast shadow uvnitř shadow casteru → tma na všechny strany). První fix: `head.userData.noShadow = true` (opt-out z `createMeshFor` traverze) + `light.shadow.bias = -0.002` (proti acne).

**(G) SpotLight upgrade + voxel-lamp refactor.**

User: *„Super, ale pod lampou je stín. Udělej ji směrovou. Třeba v tomto balíčku [PBR Street Props .glb/.zip/.usdz] je hezká lampa."*

2 fixy:
1. **Směrové světlo** — `PointLight` (360°) → `SpotLight` (kužel dolů). Sloup pak nezpůsobí "stín pod lampou" — světlo míří dolů, sloup je v záporném směru = mimo kužel.
2. **Vizuální refactor** — klasická pouliční lampa: sloup + horizontální paže + visící stínítko + SpotLight v stínítku.

2 Q:
- **Q1 `.glb` import pipeline**: A skip teď, voxelově / B implementovat. → **A**
- **Q2 voxel-lamp design**: A sloup + paže + stínítko (Victorian) / B sloup + svítidlo nahoře (current). → **A**

Implementace refactor `buildLamp`:
- Sloup 0.15×2×0.15 dark iron (`0x1a1a1a`, roughness 0.7).
- Horizontální paže 0.6×0.08×0.08 z vrcholu sloupu (X=+0.3 střed, Y=1.95).
- Stínítko 0.35×0.3×0.35 visí z konce paže (X=0.6, Y=1.7), tmavé venku + emissive `0xffaa00` 0.8.
- `SpotLight(0xffaa00, intensity=5, distance=12, angle=π/5, penumbra=0.4, decay=2)`, pozice (0.6, 1.7, 0).
- `light.target = Object3D` v Group (target pozice (0.6, -3, 0) → vertikální dolů, rotuje s ORIENTATION).
- `shade.userData.noShadow = true` (světlo uvnitř ní → nesmí blokovat).
- Shadow setup: 512×512 cube map, near 0.1, far 14, bias -0.002.

TODO M8+: **`.glb`/glTF asset import pipeline** přidán (otevírá hezčí lampu + Stickman integraci + dekorativní obyvatele). Sez. 29 F3 smazala MagicaVoxel OBJLoader/MTLLoader — pattern obnovit na glTF stack. Vyžaduje nový DD (asset pipeline = strukturální).

**DD-40** zapsán: LAMP třída + SpotLight pattern (první lokální dynamický light, předtím jen DirectionalLight sun + AmbientLight). Pattern *„světelný zdroj uvnitř meshe → mesh `noShadow`"*. `SpotLight.target = Object3D` v group držuje vertikalitu při ORIENTATION rotaci.

**(H) Noc maxed + popisek reliefu → tooltip + lampa pryč.**

User: *„1 Test OK. 2 Klidně ještě ztmav noc. 3 Popisek reliefu přesunout do infotipu slideru reliefu."*

- Noc maxed: `AMBIENT_NIGHT 0.015 → 0.005`, `_skyNight 0x010207 → 0x000000` (úplná čerň).
- Popisek reliefu: `<div class="tc-relief-name" id="tc-relief-name">Rolling</div>` smazán z `index.html`, CSS `.tc-relief-name` smazán, JS `reliefName.textContent = ...` přesunut na `relief.title = ...` (native browser tooltip). Default `title="Rolling"` (matchne `value=3`).

User: *„Test OK. Odeber lampu ze scény, v objektech ji ponechej."* — `lamp_0001` instance + `scene.add(createMeshFor(lamp))` smazán z `buildScene()`. Třída `LAMP`, builder `buildLamp` + dispatch v `createMeshFor` zachovány (připravené pro budoucí spawn).

### Rozhodnutí

- **IDEAS/TODO pruning** — sez. 33 reset 13/12 → 0/12. Voidspan/Performance sekce zhuštěny po DD-32 wipe (factory toy obsolete).
- **DD-39 atmospheric lerping** — `updateAtmosphere()` třetí konzument `world.DAY`. 2 keypointy (DAY/NIGHT) lineární lerp, sunset peak roadmap.
- **DD-40 LAMP třída + SpotLight pattern** — pouliční lampa Victorian-style, SpotLight kuželové místo PointLight, `noShadow` na meshi okolo světla.
- **Scene cleanup** — GridHelper + AxesHelper + ShadowMaterial ground plane smazány (obsolete po DD-32 wipe).
- **`.glb` import pipeline** — TODO M8+ kandidát pro budoucí DD (otevírá Stickman + dekorace + PBR Street Props).
- **Lampa odebrána ze scény** — třída + builder zachován, instance ne (sez. 33 byla test).

### Kód

**Sez. 33 uncommitted před `%END`:**
- `src/main.js`: ~+90/−50 (atmospheric lerping konstanty + `updateAtmosphere()` + LAMP import + `buildLamp` ~70 ř. + dispatch v `createMeshFor` + render loop call; cleanup GridHelper/AxesHelper/groundPlane/lamp instance).
- `src/model.js`: +11 ř. (`LAMP extends COMPOSITES`).
- `index.html`: −10 ř. (CSS `.tc-relief-name` smaz + HTML `<div>` smaz + JS reliefName getter smaz + `title="Rolling"` add + `relief.title = ...` v input listener).
- `docs/*`: ~+150 ř. (TODO update G0-G3 + DAY-cycle + audit cadence; DIARY index + sez. 33; DESIGN_DECISIONS DD-39 + DD-40; GLOSSARY LAMP + atmosphere; DONE sez. 33; IDEAS Voidspan zhuštěné + Performance zhuštěné + SEASON značka).

### Kudos / Censure

- **Kudos! (user → AI flow):** plán+Q→Go pattern celé sezení 5×: pruning (`A1B A2B A3B A4B`), Sky lerping (`A1A A2A A3A A4A`), LAMP design (Q1A Q2A SpotLight upgrade `A1A A2A`). User udržoval stručný feedback (`Test OK`, `A1A A2A A3A A4A`, `go ahead`, `Klidně ještě ztmav noc`) — `feedback_plan_then_go` memory v praxi.
- **Kudos! (user → AI):** dobrá detekce *„pod lampou je stín, udělej ji směrovou"* — AI neaplikoval domain knowledge o PointLight 360° vs. realistická pouliční lampa (kužel dolů), user dotáhl.
- **Censure! (AI → AI):** první lampa s `PointLight` neměla pre-flight zhodnocení vlivu sloupku na světlo. Pattern *„světelný zdroj uvnitř meshe → mesh `noShadow`"* je generalizovatelný, měl být v plánu (vyšel až po test feedback).
- **Censure! (AI → AI):** `AMBIENT_NIGHT` iterace 0.04 → 0.015 → 0.005, user dvakrát žádal *„ještě tmav"*. Tweak parameter ne intuitivní bez vizuálního referenčního baselineu — AI mohla nabídnout 0.005 hned v původním plánu (s warning *„noc bez lampy bude téměř černá"*).
- **Censure! (AI → AI):** `.glb` import pipeline by mohla být zachycena jako kandidát DD už při sez. 14 Stickman pivotu (TBD pipeline TBD). Sez. 33 user inspirace PBR pack vyšla z jeho strany, ne z AI proaktivního *„integrace .glb modelu otevřela by hezčí dekorace"*.

### Příště (sez. 33 → sez. 34)

**Cadence:**
- **`%AUDIT:CODE`** — 4/8 sezení od sez. 29. Další doporučený sez. 37+.
- **`%AUDIT:DOCS`** — 4/10 sezení od sez. 29. Další doporučený sez. 39+.
- **IDEAS/TODO pruning** — 0/12 (reset sez. 33).

**Roadmap kapitoly Terrain generator (sez. 34+):**
- [ ] **G0 Totální předělávka zobrazení bloků a ramp** — od textur k lowpoly barevným objektům. Důvod: atlas pipeline má vizuální dluh (tile pattern uvnitř kindu sez. 29 F5 částečný fix). Lowpoly + flat shading = 1 material per kind, simplifikuje pipeline + uvolnil draw call rozpočet. DD-scale.
- [ ] **G1 Volba max Y** — UI slider pro RELIEF_AMPLITUDE clamp, závislý na `MIN(sizeX, sizeZ)` (na 10×10 mapě alpine 6 voxelů = neproporční).
- [ ] **G2 Severní šířka / podnební pásmo** — `world.LATITUDE` nebo `world.CLIMATE` atribut (gated po DD-29 politice). Konzument: sun tilt + sezónní paleta + surface mix.
- [ ] **G3 SURFACES driver-derived per G2** — biome map z `generateTerrain` reactive na klimat (sand u rovníku, grass mírné pásmo, snow polar).

**Otevřené sub-prah:**
- [ ] **Sunset/sunrise barevný peak** (DD-39 rozšíření) — `_skyDusk` oranžová s peakem kolem `daylight ≈ 0`, piecewise lerp.
- [ ] **Slider DAY sync z value** (DD-38 dluh) — throttle 4×/s update z `world.DAY` zpět do DOM `#set-day` input.
- [ ] **`.glb`/glTF asset import pipeline** (M8+) — nový DD, otevírá Stickman + dekorativní obyvatele + PBR Street Props.

**Multi-instance:** Sez. 33 single-instance (mrkla stroj). `main` přímo bez topic branche (drobné izolované Edity + 2 strukturální DD).

## Sezení 34

### Diskuse

**(A) `%BEGIN` clean.** Repo synchronizováno (`5ef6ca6` sez. 33), server :8000 už běžel (PID 40544). Cadence: AUDIT:CODE 4/8, AUDIT:DOCS 4/10, IDEAS/TODO pruning 0/12 — všechny sub-prah. Žádné Stale Příště. User volba: G0 Lowpoly z roadmap kapitoly Terrain generator jako primární cíl (DD-scale, řeší atlas dluh, připravuje G3).

**(B) `%THINK` G0 — 3 alternativy, A vítěz.**

Cílový stav: per-face barvy přímo v geomu místo CanvasTexture atlasu, simplifikace pipeline. Stávající architektura (relevantní vrstvy): `BLOCK_TEXTURES`, `getSharedAtlasBoxGeom`, `getTcubesKindMaterial` (TCUBES atlas DD-36), `RAMP_*_TEXTURES`+`RAMP_ATLAS_SPECS`+`getRampAtlasMaterial` (ramp atlas DD-36), `_terrainBatches` (DD-37 InstancedMesh batche). DD-37 batche musí zůstat platné (jinak 100×100 zpátky na 7 FPS).

3 alternativy zváženy:
- **(A) Vertex colors per face + 1 global material** — sdílený `MeshStandardMaterial({ vertexColors: true, flatShading: true })`, per-kind `BoxGeometry.setAttribute('color', ...)` 24 verts × 3. DD-37 InstancedMesh batche unchanged (per-kind geom drží barvy, 1 mat shared). ✓ **doporučeno**
- **(B) Per-kind solid material (no top/side variation)** — 4 materials, 1 shared geom. **−** ztratí grass-top vs. grass-side (vrch zelený, sides hnědé). ✗
- **(C) Atlas s 1×1 solid color textures (minimum effort)** — replace pixel-art textury solid color CanvasTexture (16×16 jednobarevný). **−** neeliminuje texture sampling overhead + 13 materiálů pořád. ✗

**(C) Inspirace `jasonkneen/tiny-world-builder` (user GitHub link mid-diskuse).** User: *„Neber projekt doslova, ale je to parádní inspirace."* AI fetch README + AGENTS.md přes WebFetch. Klíčové takeaways:
- **MeshLambertMaterial** (ne Standard) — Three.js r128 pin, diffuse-only bez PBR specular = lepší fit pro neotexturované voxely.
- **ExtrudeGeometry** pro nepravidelné tvary (rampy potenciální zjednodušení, mimo G0 scope).
- **Shared material registry `M.*`** — izomorfní s Q1A 1 global mat + DD-25 hover clone pattern.
- **`dropAnims` queue** — staggered ease-out spawn (nice-to-have TODO).
- **Tilt-shift post-process** (z screenshotů) — gradient blur podle screen Y, miniaturní svět feel (TODO).
- **Adjacency-aware `setCell`** — paths/fences/shorelines re-render sousedů (TODO future PATH/FENCE).

Revize plánu: **Q1 update — MeshLambertMaterial místo Standard**. Q2-Q4 zachovány. Plus **4 follow-up TODO** zachycující inspirace.

User volba `A1A' A2A A3A A4A`: Lambert + flatShading=true + fáze G0a→G0b→cleanup + smazat BLOCK_TEXTURES úplně.

**(D) G0a TCUBES implementace.** Topic branch `feat/lowpoly-blocks` (per DD-30). Update TODO.md (G0 → G0a/G0b/cleanup checkpoint plán + 4 follow-up). Implementace v `src/main.js`:
- `BLOCK_COLORS` mapa: `grass {TOP: 0x6aaa3a, BOTTOM: 0x6b4423, SIDE: 0x6b4423}`, `dirt` homogenní hnědý, `stone {TOP: 0x7a7a7a, BOTTOM/SIDE: 0x6a6a6a}`, `sand {TOP: 0xd9c178, BOTTOM/SIDE: 0xc4a878}`.
- `_lowpolyMat = new THREE.MeshLambertMaterial({ vertexColors: true, flatShading: true })`.
- `_lowpolyBoxGeomCache: Map<kind, BoxGeometry>` + `getTcubesKindGeom(kind)` factory. BoxGeometry default face order +X/−X/+Y/−Y/+Z/−Z, 24 verts (4 per face), Float32Array(24*3) color attribute. Per face: 4 verts × stejná barva (solid face). sRGB→linear convert přes `Color.convertSRGBToLinear()` per face.
- `spawnTerrain` Pass 1 whitelist `BLOCK_TEXTURES[kind]` → `BLOCK_COLORS[kind]`. Pass 2 TCUBES dispatch: `geom = getTcubesKindGeom(surface)`, `mat = _lowpolyMat`.

User test: TCUBES lowpoly look ✓, grass top zelený + sides hnědé matchne, ostatní kindy homogenní solid. DD-39 atmospheric lerp pořád funguje (lighting interakce přes Lambert = standardní). Hover overbright (DD-37 instanceColor multiplikace × vertex colors) = oranžový tint stále funguje.

**(E) G0b rampy.** RAMP_FACE_VERT_COUNTS per typ:
- `trramps: [4, 4, 4, 3, 3]` (SLOPE quad + BOTTOM quad + BACK quad + LEFT tri + RIGHT tri = 18 verts)
- `ttramps: [3, 3, 3, 3]` (4 trojúhelníky = 12 verts)
- `tdramp:  [3, 3, 4, 8, 6]` (SLOPE tri + TOP tri + BOTTOM quad + WALL_FULL 2 quads = 8 + WALL_TRI 2 tris = 6, celkem 24 verts ve 5 color groups; 7 fyzických faces).

RAMP_FACE_PALETTE_KEYS per typ — mapuje faceIdx na BLOCK_COLORS klíč:
- `trramps: ["TOP", "BOTTOM", "SIDE", "SIDE", "SIDE"]` (SLOPE = kind TOP barva)
- `ttramps: ["TOP", "BOTTOM", "SIDE", "SIDE"]`
- `tdramp:  ["TOP", "TOP", "BOTTOM", "SIDE", "SIDE"]` (SLOPE+TOP sdílí lomený povrch)

`_lowpolyRampGeomCache: Map<"trramps:grass", BufferGeometry>` (max 9 entries) + `getRampGeom(type, surface)` factory: clone atlas IIFE raw geom + `deleteAttribute("uv")` + inject color attribute z palety. `spawnTerrain` Pass 2 ramp dispatch sloučen do 1 větve: `if (type === "trramps" || "ttramps" || "tdramp") { geom = getRampGeom(type, surface); mat = _lowpolyMat; }`.

User test 100×100: FPS **101** (parita sez. 31 baseline 104, statisticky stejné), 15 geomů (4 TCUBES + 9 ramp + water + sun + sky group), 13 batchů zachovaných. Rampy lowpoly look ✓.

**(F) User nález — šedý seam na 30×30 při slabém světle.** *„Šev mezi -13 a -14, zelená rozdělená jemnou šedou/černou linií. Při nižší intenzitě světla švů přibude."*

AI diagnose: `flatShading: true` nutí shader spočítat per-face normálu z `dFdx/dFdy` derivatives v fragment shaderu (derivace `gl_Position`). U **InstancedMesh** každá instance běží v separátním shader passu s vlastní `instanceMatrix` — na hraně mezi 2 sousedními instancemi může derivative-based normála mírně drifovat (numerická precision). Dva sousední pixely mají subtle odlišnou intensity Lambertu → tenká `1 px` linka. Při slabém světle (DD-39 day→night) klesá `DirectionalLight.intensity` + ambient, **relativní kontrast** mezi normálním face a artifact pixelem narůstá (Lambert `intensity * max(0, dot(N, L))`): malá intensity + malá odchylka v N = proporcionálně větší rozdíl.

Klíčové insight: **BoxGeometry už defaultně má per-face normály v `geometry.attributes.normal`** (vertices nesdílené přes faces). `flatShading: true` je redundantní — geom samotná dělá flat look. Drop `flatShading: true` → shader vezme normály z attribute přímo, žádné derivative computation, žádný cross-instance precision drift.

Stejně pro ramp BufferGeometries — atlas IIFE buildery ručně setují `normal` per face (`normals.push(...n, ...n, ...n)`), lowpoly clone má per-face normály taky.

User: *„Ano, fix flatShading a refresh."* 1 řádek edit (`flatShading: true` → drop). Refresh: seam zmizel ✓.

**(G) Cleanup atlas garbage.** Smazáno z `src/main.js`:
- `BLOCK_TEXTURES` mapa
- `ATLAS_TILE_PX`, `ATLAS_FACE_KEYS`
- `_sharedAtlasBoxGeom` + `getSharedAtlasBoxGeom()`
- `_tcubesAtlasMatCache` + `getTcubesKindMaterial()`
- `RAMP_EDGE_TEXTURES`, `RAMP_CORNER_TEXTURES`, `RAMP_DIAGONAL_TEXTURES`
- `RAMP_SURFACE_FROM_KIND`
- `RAMP_ATLAS_SPECS`
- `_rampsAtlasMatCache` + `getRampAtlasMaterial()`
- `createTRRampFor`, `createTTRampFor`, `createTDRampFor` slow-path funkce
- `createMeshFor` dispatch case pro `TRRAMPS`/`TTRAMPS`/`TDRAMP` (single-ramp spawn = dead pattern)
- `createTCubeFor` atlas fast-path **i** slow-path `material[6]` array — nahrazeno lowpoly variantou: pokud kind v `BLOCK_COLORS` → lowpoly geom + `_lowpolyMat`, jinak šachovnice fallback (DD-07 unknown kind, materiál sdílen přes `faceMaterialFor(null)`)
- Perf HUD `mat` čítač: drop atlas cache součty (`_tcubesAtlasMatCache.size + _rampsAtlasMatCache.size`), zůstane jen `_faceMaterialCache.size` (slow path). Komentář aktualizován.

Smazáno z `src/model.js` (DD-41 commit u tříd):
- `TCUBES` constructor: drop `textures` arg + 6 `TEXTURE_*` fields
- `TRRAMPS` constructor: drop `textures` arg + 5 `TEXTURE_*` fields (zachován `orientation`)
- `TTRAMPS` constructor: drop `textures` arg + 4 `TEXTURE_*` fields
- `TDRAMP` constructor: drop `textures` arg + 5 `TEXTURE_*` fields
- TTUNELS si zachovává `TEXTURE_*` (TTUNELS instance dnes nikdo nespawne, mimo G0 scope — TODO follow-up: migrate nebo smazat dead třídu).

Volání update v `createBlock`/`createRampEdge`/`createRampCorner`/`createRampDiagonal`: drop `tex` argument předávaný do constructorů.

JSDoc komentáře v `model.js` updated: face geometry zachována (svah, podstava, etc.), drop per-face `TEXTURE_*` atribut prefix; nové wording *"Barvy faces řídí `BLOCK_COLORS[surface]` + `RAMP_FACE_PALETTE_KEYS` (DD-41)"*.

Atlas IIFE `TRRAMP/TTRAMP/TDRAMP_GEOM_CACHE` zachovány jako raw geom source pro `getRampGeom` clone (drop UV + add color). IIFE pořád builduje `uv` attribute + `remapU` UV transformace = zbytečný compute (~50 ř.), sub-prah cleanup: strip UV at source + rename na `_RAMP_RAW_GEOM_*`. Odloženo.

**(H) Palette tweak.** User: *„Ještě změň barvu písku (víc žlutá) a skály (světlejší šedá)."*
- `stone {TOP: 0x7a7a7a, BOTTOM/SIDE: 0x6a6a6a}` → `{TOP: 0x9a9a9a, BOTTOM/SIDE: 0x8a8a8a}` (světlejší šedá)
- `sand  {TOP: 0xd9c178, BOTTOM/SIDE: 0xc4a878}` → `{TOP: 0xe8d97a, BOTTOM/SIDE: 0xd9c66a}` (víc žlutá)

### Rozhodnutí

- **DD-41 — Lowpoly vertex-color pipeline (supersede DD-36 atlas).** Per-face barvy v `BLOCK_COLORS` mapě + vertex-color geometrii, 1 sdílený `MeshLambertMaterial` napříč 13 terrain batchi. Eliminuje atlas tile pattern dluh, připravuje G3 climate-driven barvy. DD-37 InstancedMesh batche stable.
- **`flatShading: false` (důležitý fix).** BoxGeometry + ramp BufferGeometries už mají per-face normály v `attributes.normal`, derivative-based flat shading na materiálu vytváří cross-instance precision artifact u InstancedMesh.
- **4 TODO follow-up z `tiny-world-builder` inspirace:** drop-in animace tiles při regenerateScene, tilt-shift post-process, ExtrudeGeometry pro rampy (lazy refactor), adjacency-aware re-render pro PATH/FENCE.
- **Cleanup TTUNELS odloženo** (mimo G0 scope, sub-prah TODO).
- **Atlas IIFE raw geom retained** — sub-prah cleanup: UV strip + rename.

### Kód

**Sez. 34 commits (na topic branch `feat/lowpoly-blocks`):**
- `src/main.js`: net ~−120 ř. (G0a/G0b pipeline ~+150 ř., atlas builders/tabulky ~−250 ř., cleanup `createTCubeFor`/`createMeshFor` dispatch ~−50 ř.).
- `src/model.js`: ~−25 ř. (drop `textures` args + `TEXTURE_*` fields z 4 BLOCKS tříd, JSDoc update).
- `docs/TODO.md`: G0 plán + 4 follow-up TODO.
- `docs/DESIGN_DECISIONS.md`: DD-41 zápis.
- `docs/DIARY.md` + tento `docs/diary/2026-05-13.md` Sez. 34 sekce.

### Kudos / Censure

- **Kudos! (user → AI flow):** plán+Q→Go pattern (`A1A' A2A A3A A4A`) — `feedback_plan_then_go` memory v praxi. Inspirace `tiny-world-builder` GitHub link **přesně v pravý moment** (po %THINK před implementací) — rozšířila plán o Lambert + 4 TODO follow-up bez breaking decision flow.
- **Kudos! (user):** rychlá diagnostika seam jako lighting-related (*„při nižší intenzitě světla švů přibude"*) zúžila search space na shader/normal artifact, ne biome boundary nebo geometry overlap.
- **Censure! (AI → AI):** první návrh `flatShading: true` bez InstancedMesh-cross-instance pre-flight úvahy. Cross-instance derivative artifact je known Three.js limitation (issue tracker, Three.js forum), AI měla vědět nebo aspoň zvážit — per-vertex normály jsou už default v `BoxGeometry`, `flatShading: true` byla redundantní volba. User musel diagnostikovat sám.
- **Censure! (AI → AI):** atlas IIFE `_RAMP_RAW_GEOM_*` rename + UV strip at source odloženo na sub-prah cleanup — IIFE pořád builduje UV + `remapU` (~50 ř. zbytečný compute). DD-41 to explicitně zmiňuje jako known limitation, ale lepší by bylo provést cleanup hned (KISS dluh).
- **Censure! (AI → AI):** `createTCubeFor` slow-path mazání — single-spawn TCUBES (non-terrain) je dnes mrtvý pattern, ale nahradil jsem ho šachovnice fallback místo úplného drop dispatch case. Defensive ne KISS. Pokud někdo budoucně spawne TCUBES mimo BLOCK_COLORS, dostane DD-07 placeholder. OK kompromis.

### Příště (sez. 34 → sez. 35)

**Cadence:**
- **`%AUDIT:CODE`** — 5/8 sezení od sez. 29. Další doporučený sez. 37+.
- **`%AUDIT:DOCS`** — 5/10 sezení od sez. 29. Další doporučený sez. 39+.
- **IDEAS/TODO pruning** — 1/12 (sez. 33 reset + sez. 34 přidalo 4 follow-up + closed G0).

**Roadmap kapitoly Terrain generator (sez. 35+):**
- [ ] **G1 Volba max Y** — UI slider `RELIEF_AMPLITUDE` clamp závislý na `MIN(sizeX, sizeZ)` (na 10×10 mapě alpine 6 voxelů neproporčně, na 100×100 OK). Quick win, jednoduchá DOM změna v `#terrainctrl`. Doporučeno na main bez topic branche.
- [ ] **G2 Severní šířka / podnební pásmo** — `world.LATITUDE` nebo `world.CLIMATE` atribut (gated po DD-29 politice — DD-scale). Konzument: sun tilt (přidá k DD-38 fixed `SUN_TILT = π/6`), sezónní paleta, surface mix. Vlastní topic branch.
- [ ] **G3 SURFACES driver-derived per G2** — biome map z `generateTerrain` reactive na klimat (sand u rovníku, grass mírné pásmo, snow polar). Refactor `surfaces` z UI prop na driver-derived per `world.CLIMATE`. Vlastní topic branch po G2.

**Sub-prah (G0 follow-up):**
- [ ] **Atlas IIFE raw geom strip UV at source + rename** na `_RAMP_RAW_GEOM_*` (~50 ř. cleanup, lazy).
- [ ] **TTUNELS migrate na lowpoly** nebo drop dead třída (sub-prah, mimo G0 scope).
- [ ] **Drop-in animace tiles při `regenerateScene`** (z tiny-world-builder `dropAnims`).
- [ ] **Tilt-shift post-process** (gradient blur podle screen Y, DD-kandidát).
- [ ] **ExtrudeGeometry pro rampy** (lazy refactor, diminishing returns po G0).

**Multi-instance:** Sez. 34 single-instance (mrkla stroj). Topic branch `feat/lowpoly-blocks` — Q při %END: dokončený? ANO → merge `--no-ff` do `main`.

## Sezení 35

### Diskuse

**(A) `%BEGIN` clean.** Repo synchronizováno (`ab3be1e` sez. 34 merge), server :8000 už běžel. Cadence: AUDIT:CODE 5/8, AUDIT:DOCS 5/10, IDEAS/TODO pruning 1/12 — všechny sub-prah. Žádné Stale Příště. User volba: **G1 Volba max Y** (quick win) z Roadmap kapitoly Terrain generator jako primární cíl.

**(B) G1 plán + Go.** Pattern plán → Go ([[feedback_plan_then_go]]) — KISS implementace: clamp `relief` slideru `max` dynamicky dle `MIN(sx, sz)` (dynamický DOM, ne defensive clamp v generátoru). 3 soubory, žádný DD.

**(C) G1 implementace.**
- `src/terrain.js` (+18 ř.) — export `maxReliefForSize(sx, sz)`. Vzorec: `maxAmp = floor(MIN(sx,sz) / 10)`, reverse lookup do `RELIEF_AMPLITUDE` → poslední `reliefIdx`. Index 8 (cap amp 6) povýší na 10 (zachová 11-jmenné spektrum názvů na velkých mapách). Mapping: 3→1 (Level), 10→3 (Rolling), 30→5 (Uneven), 50→7 (Craggy), 60+→10 (Alpine).
- `src/main.js` (+2 ř.) — import + `window.maxReliefForSize` expose pro inline UI controller.
- `index.html` (+15 ř.) — `updateReliefSliderMax()` v terrainctrl IIFE, wire na `sizeX/sizeZ.input` event (live při dragu), init call. Programmatic `relief.value` setting `change` event neemituje → snížení relief během size dragu nespustí regen, až user dotáhne size, `readParams()` přečte konzistentní stav.

**TEST 1 OK.**

**(D) User otázka „co je G2"** → vysvětlení konceptu G2 + 3 sub-Q.

**(E) User A1 = matice 4×3 biomů.** Rozšíření 1 atributu (původní `LATITUDE`) na **2 atributy** (`LATITUDE` × `HUMIDITY`). Skutečná geografie Země (Tropický deštný prales / Savana / Sahara / Vlhké subtropy / Mediteránní / Subtropická step / Listnatý les / Step Prérie / Chladná poušť Gobi / Tundra / Ledová poušť — 11 ze 12 buněk; polar/wet vzácný). **A2 DAY=0.5=poledne fix** — DD-38 mapping `0.25=poledne` matematicky úsporné, prakticky matoucí pro user-facing slider. **A3 UI navrhni.**

**(F) AskUserQuestion: UI varianta + DAY fix.** UI 3 možnosti (2 slidery diskrétní / 2 selecty / 4×3 matice radio buttons). User volba **C** (slidery, izomorfně s `relief` sliderem). DAY fix: ANO pre-G2 (sun-related koherence před G2 LATITUDE → sun tilt). Topic branch `feat/world-climate` checkout.

**(G) Fáze 1 — DAY=0.5 = poledne fix.**
- `src/main.js`: `updateSun()` math `sin(α)/cos(α)` → `sin(α)/(-cos(α))` parametrizace (X horizont = sin, Y výška = -cos · cos(tilt)). `updateAtmosphere()` daylight `sin` → `-cos`. Verifikace: 0.0=půlnoc (intensity=0), 0.25=východ (Y=0, X=+SUN_DISTANCE), 0.5=poledne (zenith), 0.75=západ.
- `src/model.js`: `this.DAY = 0.25` → `this.DAY = 0.5`. JSDoc nový mapping (0=půlnoc, 0.25=východ, 0.5=poledne, 0.75=západ).
- `index.html`: `#set-day value="0.25"` → `value="0.5"`, readout `0.25` → `0.50`.

**(H) Fáze 2 — G2 LATITUDE/HUMIDITY.**
- `src/model.js`: WORLD `this.LATITUDE = "temperate"` (4 enum: tropical/subtropical/temperate/polar), `this.HUMIDITY = "mid"` (3 enum: wet/mid/dry). JSDoc rozšířen.
- `src/main.js`: `SUN_TILT = π/6` → `SUN_TILT_BY_LATITUDE` lookup map (tropical=0 / subtropical=π/12 / temperate=π/6 / polar=π/3). `updateSun()` čte `SUN_TILT_BY_LATITUDE[world.LATITUDE]` (fallback temperate). `window.settings.setLatitude/setHumidity` (validace klíče). Import `BIOME_NAMES` + `window.BIOME_NAMES` expose.
- `src/terrain.js`: export `BIOME_NAMES` 4×3 (`tropical: { wet: "Tropický deštný prales", mid: "Savana", dry: "Horká poušť" }`, ...). Polar/wet = `"—"` placeholder.
- `index.html`: Climate sekce v `#terrainctrl` (2 slidery `tc-latitude` 0..3 + `tc-humidity` 0..2 + biome readout `tc-biome-val`). CSS `.climate-row` wider val column (88px pro slovní enum), `.tc-biome` tyrkysový akcent (read-only derivovaný atribut). Wire-up v IIFE: `LATITUDE_KEYS/NAMES` + `HUMIDITY_KEYS/NAMES` + `updateBiomeReadout()` + `input` listenery (mutují WORLD, žádný `change` regen — sun tilt se aktualizuje per-frame).

**TEST 1 OK (DAY), 2 OK (Climate sun tilt + biome readout).**

**(I) TEST 3 — bug report.** User: některé size×relief×surface kombinace neregenerují (dump: `surfaces 0.11+0.08+0.25+0.57 = 1.01`). Diagnóza: **pre-existing bug** v `normalizeSurfaces` (sez. 26) — `toFixed(2)` rounding drift po sekvenci multi-slider pohybů (worst case ±0.02 sum drift, generátor tolerance ±0.001 → throw → tichá failure). Fix: **divide-by-sum normalize v `readParams()`** (4 řádky). KISS, single-point fix v UI (jediný caller generátoru).

**TEST OK po fix (mlčky předpokládáno z user `%END` triggeru).**

### Rozhodnutí

- **DD-42 — G2 WORLD Climate atributy (LATITUDE × HUMIDITY) + sun tilt driver.** 2 enum atributy na WORLD (matice 4×3 = 12 biomů), `SUN_TILT_BY_LATITUDE` lookup nahrazuje DD-38 fixed `SUN_TILT = π/6`, `BIOME_NAMES` tabulka pro UI readout, UI Climate sekce v `#terrainctrl` (2 slidery + biome readout). Aktivuje DD-29 odložený `CLIMATE` slot (s 2 živými konzumenty: sun tilt + biome readout).
- **DD-43 — DAY mapping standardizace 0.5 = poledne (supersede DD-38 sun position math).** `updateSun()` `sin/cos` parametrizace → `sin/(-cos)`, `updateAtmosphere()` daylight křivka stejný posun, default `world.DAY = 0.5`, HTML default sync. Mění user-facing semantiku DAY (DD-38 explicitně dokumentoval `0.25=poledne`).
- **G1 (sez. 35) — quick win, žádný DD.** UI clamp `relief.max` dle `MIN(sx, sz)` přes export `maxReliefForSize` z `terrain.js`.
- **Surface drift fix — bug fix, ne DD.** Divide-by-sum normalize v `readParams()` eliminuje pre-existing rounding drift (sez. 26).

### Kód

**Sez. 35 commits (na topic branch `feat/world-climate`):**
- `src/terrain.js`: +33/0 ř. (G1 `maxReliefForSize` export, G2 `BIOME_NAMES` export 4×3).
- `src/main.js`: +55/−23 ř. (G1 import+expose, G2 `SUN_TILT_BY_LATITUDE` lookup nahrazuje fixed const, `updateSun` math posun + tilt lookup, `updateAtmosphere` math posun, `window.settings.setLatitude/setHumidity`, `BIOME_NAMES` import+expose, koment fix DD-43 mapping).
- `src/model.js`: +22/−1 ř. (WORLD JSDoc rozšíření, `this.DAY = 0.5`, `this.LATITUDE = "temperate"`, `this.HUMIDITY = "mid"`).
- `index.html`: +104/0 ř. (G1 `updateReliefSliderMax` + wire, G2 CSS `.climate-row`/`.tc-biome` + Climate sekce DOM + wire-up v IIFE + biome readout, surface fix `readParams` divide-by-sum, set-day default 0.5).
- `docs/DESIGN_DECISIONS.md`: ~+95 ř. (DD-42 + DD-43).
- `docs/DIARY.md` + tento `docs/diary/2026-05-13.md` Sez. 35 sekce.
- `docs/TODO.md`, `docs/DONE.md`, `docs/GLOSSARY.md` — update.

### Kudos / Censure

- **Kudos! (user A1 návrh):** rozšíření 1 atributu LATITUDE na 2D matici biomů (Pásmo × Vlhkost) postavené na skutečné geografii Země (Tropický deštný prales / Savana / Sahara / ... / Tundra / Ledová poušť). Lépe než 1D LATITUDE — emergentní biome map z 2 ortogonálních os, připravuje G3 surface mix s reálnou variabilitou (12 typů místo 4).
- **Kudos! (user A2):** pojmenování DAY mapping bugu jako otázka *„DAY=0.5 by mělo být poledne, ne?"* — UX vs. code-economy trade-off ([[feedback_critic_ratio]] v praxi: user oponoval AI implementaci z předchozího sezení). DD-38 mapping `0.25=poledne` byl matematicky úsporný (`sin(2π·DAY)` přímo), ale prakticky matoucí. AI by si toho sama nevšimla.
- **Kudos! (AI → user flow):** pattern plán + Q → Go ([[feedback_plan_then_go]]) bezchybně — `A1A' A2A A3A`, AskUserQuestion preview rendering pomohl zvolit C variant slideru rychle.
- **Censure! (AI → AI):** **Pre-existing surface drift bug v `normalizeSurfaces` (sez. 26)** odhalený až user testem sez. 35. AI psala generátor tolerance `Math.abs(sum-1.0) <= 0.001` v sez. 24 (DD-32) **bez analýzy reálného UI rounding range** (4 slidery × ±0.005 toFixed precision = ±0.02 worst case = 20× nad tolerancí). Měla buď tolerance vyšší (defensive), nebo `readParams()` divide-by-sum normalize od začátku. 11 sezení dluh, tichá failure (žádný error v console = obtížné diagnostikovat).
- **Censure! (AI → AI):** `SUN_TILT_BY_LATITUDE.tropical = 0` (přímý zenith) bez varování o trade-off — vizuálně objekty ploché v poledne (degenerate shadow plane). Fyzikálně OK pro rovník, vizuálně suboptimal. AI měla buď navrhnout `π/24` (~7.5°) jako kompromis, nebo aspoň upozornit user. Sub-prah TODO pro tweak pokud user řekne.

### Příště (sez. 35 → sez. 36)

**Cadence:**
- **`%AUDIT:CODE`** — 6/8 sezení od sez. 29. Další doporučený sez. 37+.
- **`%AUDIT:DOCS`** — 6/10 sezení od sez. 29. Další doporučený sez. 39+.
- **IDEAS/TODO pruning** — 2/12 (sez. 33 reset + sez. 34 přidalo 4 follow-up + sez. 35 closed G1+G2 + 2 nových sub-prah).

**Roadmap kapitoly Terrain generator (sez. 36+):**
- [ ] **G3 SURFACES driver-derived per biome** (DD-scale, vlastní topic branch `feat/biome-surfaces`). Refactor `surfaces` z UI prop na driver-derived per `BIOME_SURFACES[LATITUDE][HUMIDITY]` lookup. Polar/wet fallback rozhodnutí (Tundra default vs. validation error). UI: skrýt 4 surface slidery v Climate-driven módu, přidat toggle "Manual surfaces"?

**Sub-prah G2 follow-up (sez. 35):**
- [ ] **Polar/wet biome fallback** — dnes `BIOME_NAMES.polar.wet = "—"`, UI zobrazí prázdné. Rozhodnout v G3: Tundra fallback nebo validation error.
- [ ] **`SUN_TILT_BY_LATITUDE.tropical` tweak** — 0° = žádné stíny v poledne, objekty vypadají ploše. Kandidát `π/24` (~7.5°) zachová stínovou viditelnost. Wait pro user feedback.

**Sub-prah G0 follow-up (zachované z sez. 34):**
- [ ] **Atlas IIFE raw geom strip UV at source + rename** na `_RAMP_RAW_GEOM_*` (~50 ř. cleanup).
- [ ] **TTUNELS migrate na lowpoly** nebo drop dead třída.
- [ ] **Drop-in animace tiles při `regenerateScene`** (z tiny-world-builder `dropAnims`).
- [ ] **Tilt-shift post-process** (DD-kandidát).
- [ ] **ExtrudeGeometry pro rampy** (lazy refactor).

**DAY-cycle (DD-38/DD-43 follow-up):**
- [ ] **Slider DAY sync z value** — periodic update z `world.DAY` zpět do DOM `#set-day` při auto-advance (`DAY_SPEED > 0`).
- [ ] **Sunset/sunrise barevný peak** (DD-39 ext.) — 3 sky keypoint (oranžová peak při daylight ≈ 0).

**Multi-instance:** Sez. 35 single-instance (mrkla stroj). Topic branch `feat/world-climate` — Q při %END: dokončený? ANO → merge `--no-ff` do `main`.

---

## Sezení 36

### Diskuse

**(A) `%BEGIN` clean.** Repo synchronizováno na `a303d69` (sez. 35 merge feat/world-climate), server :8000 už běžel (PID 13928). Cadence: AUDIT:CODE 6/8, AUDIT:DOCS 6/10, IDEAS/TODO pruning 2/12 — všechny sub-prah. Žádné Stale Příště. User volba: **G3 SURFACES driver-derived per biome** (logické pokračování G2, hlavní roadmap kapitola).

**(B) `%THINK` G3 — 3 rozhodnutí.** AI před návrhem otevřela `terrain.js` + Climate sekci `index.html` (žádná spekulace o aktuálním stavu) a strukturovala 3 micro-Q:

1. **Q1 UI behavior** — Hard override (Climate řídí, surface slidery skryté) vs. Soft hint (Climate plní + user dotahá) vs. Toggle Manual. AI doporučila A: izomorfismus s G2 sun tilt (climate driver = vždy aktivní), KISS, debug surface mix přes konzoli (`window.regenerateScene({surfaces:...})`).
2. **Q2 polar/wet fallback** — Tundra alias vs. validation error vs. snow stub. AI doporučila A: BIOME_SURFACES.polar.wet = alias polar.mid + rename `BIOME_NAMES.polar.wet` na "Polární tundra". Arktida geo nejbližší tundře.
3. **Q3 mix tabulka 4×3** — schválit per-cell hardcoded 12 buněk × 4 koef. (HUMIDITY → water+grass, LATITUDE → stone vs. sand, Sahara extrém 0.90 sand). Parametric formula by ztratila výrazovou volnost (Tropický prales ≠ Vlhké subtropy množstvím vody).
4. **Q4 (proaktivně přidaný) sub-prah snow surface** — G4 kandidát (`sand` v `polar.*` je proxy).

**Rizika** uvedená: `regenerateScene` vždy 1 mapa = 1 biome (multi-biome map gradient = out-of-scope, G4+); ztracený UI surface state po skrytí; polar+wet UI redirect zmate user, dokud nepřibude snow. Sandbox model check: žádné nové entity, jen data tabulky.

**User volba `A1A A2A A3A A4A`** — všechny doporučené (`feedback_plan_then_go` v praxi: po Q+plán Go → AI nepřesypává diskusi). Topic branch `feat/biome-surfaces` checkout.

**(C) Implementace G3.1 — `terrain.js`.** Přidán export `BIOME_SURFACES` (12 buněk hardcoded) + `surfacesForBiome(lat, hum)` helper s fallback temperate.mid. `BIOME_NAMES.polar.wet` rename `"—"` → `"Polární tundra"`. `polar.wet` v BIOME_SURFACES = identický mid (alias). +60/-1 ř.

**(D) Implementace G3.2 — `main.js`.** Import `BIOME_SURFACES`/`surfacesForBiome`. Expose `window.BIOME_SURFACES` + `window.surfacesForBiome` paralelně s `window.BIOME_NAMES` (G2 pattern). `TERRAIN_DEFAULTS` zbavený hardcoded `surfaces: {...}` (komentář vysvětluje proč). `buildScene()` derivuje surface mix přes `surfacesForBiome(world.LATITUDE, world.HUMIDITY)`. ~+15/-5 ř.

**(E) Implementace G3.3 — `index.html` UI cleanup.** DOM Surfaces sekce kompletně smazána (4 slidery + sekce label). IIFE refactor: `SURFACE_KINDS`/`surfInputs`/`surfVals`/`normalizeSurfaces` pryč, surface listenery pryč. `LATITUDE_KEYS`/`LATITUDE_NAMES`/`HUMIDITY_KEYS`/`HUMIDITY_NAMES` + `lat`/`hum`/`latVal`/`humVal`/`biomeVal` DOM refs **přesunuto nahoru** (sloučená deklarace), protože `readParams()` níž je potřebuje pro `surfacesForBiome` lookup. `readParams()` přepsáno: `surfaces = window.surfacesForBiome?.(latKey, humKey) ?? temperate.mid fallback`. Climate slidery dostaly `change` event handler → `onChange = window.regenerateScene(readParams())` (kromě dnešního `input` handler pro per-frame sun tilt). ~+35/-95 ř.

**(F) Smoke test (ESM Node).** Test pokrýval 4 invarianty: (1) sum=1.0 ±0.001 ve všech 12 buňkách BIOME_SURFACES → OK; (2) `polar.wet === polar.mid` JSON match → OK; (3) `BIOME_NAMES.polar.wet === "Polární tundra"` → OK; (4) `surfacesForBiome('tropical','dry')` = `{grass:0, stone:0.1, sand:0.9, water:0}` (Sahara extrém) → OK; fallback `(garbage, garbage)` = temperate.mid → OK; (5) `generateTerrain({size, relief, surfaces: surfacesForBiome(lat, hum), seed})` projde všemi 12 combiny bez throw → 12/12 OK. Plus `node --check` na oba `src/` soubory + HTTP 200 na `/`, `/src/main.js`, `/src/terrain.js`, `/index.html`.

**(G) Browser test (user).** Předán user pro vizuální verifikaci 6 testovacích case: Tropický+Sucho = Sahara look (sand 0.90), Tropický+Vlhko = prales s jezery, Mírný+Středně = baseline Step/Prérie, Polár+Vlhko = "Polární tundra" readout (ne "—") + stone-heavy terén, Polár+Středně = stejný terén (alias check), `#terrainctrl` panel bez Surfaces sekce + Climate change triggeruje regen.

### Rozhodnutí

- **DD-44 G3 SURFACES driver-derived per biome.** `BIOME_SURFACES` 4×3 lookup (12 buněk × 4 koef. `grass`/`stone`/`sand`/`water`), `surfacesForBiome(lat, hum)` helper. Hard override v UI (surface slidery smazány). `polar.wet` = alias `polar.mid` + rename `BIOME_NAMES.polar.wet` na "Polární tundra". Aktivace DD-29 odložený `CLIMATE` slot full (2 živí konzumenti: sun tilt G2 DD-42 + surface mix G3 DD-44).
- **Žádný další DD** — sub-prah snow surface G4 = TODO kandidát, ne DD-scale (čeká na vlastní implementaci).

### Kód

**Sez. 36 implementace (topic branch `feat/biome-surfaces`):**
- `src/terrain.js` +60/−1 ř. (`BIOME_SURFACES` export 12 buněk + `surfacesForBiome` helper + `BIOME_NAMES.polar.wet` rename).
- `src/main.js` ~+15/−5 ř. (import + window expose + `TERRAIN_DEFAULTS` bez surfaces + `buildScene` driver wire).
- `index.html` ~+35/−95 ř. (Surface DOM sekce delete, IIFE refactor: SURFACE_KINDS/surfInputs/normalizeSurfaces pryč, Climate KEYS/NAMES/DOM refs nahoru, `readParams()` přes `surfacesForBiome`, climate `change` → `onChange` regen).
- `docs/DESIGN_DECISIONS.md` ~+50 ř. (DD-44 sekce).
- `docs/TODO.md`, `docs/DONE.md`, `docs/GLOSSARY.md`, `docs/DIARY.md` + tento záznam.

### Kudos / Censure

- **Kudos! (user → AI `%THINK` flow):** AI před návrhem doslova otevřela `terrain.js` + Climate sekci `index.html` (paralelní Read), zjistila konkrétní fakta o stávajícím stavu (`generateTerrain` API surfaces parametr, `BIOME_NAMES` jen display, climate slidery žádný change event), a teprve pak strukturovala 3 Q. Žádná spekulace o stavu. Mitiguje [[feedback_subagent_verify]] (verifikuj před prezentací) preventivně.
- **Kudos! (user volba A1A A2A A3A A4A):** Konzistentní `feedback_plan_then_go` pattern — užitečná Q-batch, pak rychlý souhlas, pak Go. Žádné prodlužování diskuse o doporučeném plánu.
- **Kudos! (AI design):** UI hard override > soft hint je netriviální KISS volba (na první pohled by "ponechej slidery pro experimentátory" vypadalo bezpečně). Argument izomorfismem s G2 sun tilt (= climate driver vždy aktivní pro climate-derived atributy, žádné výjimky) udržuje konceptuální integritu nad pohodlím.
- **Censure! (AI → AI):** Při G3.3 IIFE refactoru AI musela přesouvat `LATITUDE_KEYS`/`HUMIDITY_KEYS` + DOM refs nahoru (původně byly **uvnitř** Climate sekce na řádku 454+, ale `readParams()` výš je teď potřebuje). To bylo viditelné už při `%THINK` (`readParams()` při Q1 hard override musí znát aktuální lat/hum), ale AI se to dořešilo až v implementaci. Drobnost, nicméně dvě iterace Edit místo jedné koncepční vize.
- **Censure! (AI → AI, polar fallback transparency):** Rename `BIOME_NAMES.polar.wet` na "Polární tundra" je transparent fallback — ale `BIOME_SURFACES.polar.wet === BIOME_SURFACES.polar.mid` (identický objekt v JS smyslu) je **mlčky alias**. Kdyby budoucí code někdy mutoval `BIOME_SURFACES.polar.mid`, mutace by se silently propagovala do `polar.wet` (object identity, ne deep copy). Pro G3 to je akceptovatelné (tabulka je read-only), ale pattern by se mohl ukázat jako pasti v dynamic config refactoru. Sub-prah pozorování.

### Příště (sez. 36 → sez. 37)

**Cadence stav (po sez. 36):**
- **`%AUDIT:CODE`** — 7/8 sezení od sez. 29. **Doporučený sez. 37** (prah dosažen). Velký nový kód v rampe smoothing + InstancedMesh + WORLD + Climate driver — připraven materiál.
- **`%AUDIT:DOCS`** — 7/10 sezení od sez. 29. Další doporučený sez. 39+.
- **IDEAS/TODO pruning** — 3/12 (sez. 36: close G3, close polar/wet sub-prah, add snow surface G4 sub-prah).

**Sub-prah G3 follow-up (sez. 36):**
- [ ] **Snow surface (G4 kandidát)** — `polar.*` v `BIOME_SURFACES` aktuálně používá `sand` jako sníh proxy. Vizuálně to vypadá jako poušť, ne sníh. G4: přidat `snow` surface kind do `SURFACE_Y_OFFSET` (terrain.js) + atlas paleta (klon `grass-top` s bílou paletou — BLOCK_COLORS sekce v main.js) + migrate `polar.*` z `sand` na `snow`. Drobný DD scope.

**Sub-prah G2 follow-up (zachované):**
- [ ] **`SUN_TILT_BY_LATITUDE.tropical` tweak** `0 → π/24` (~7.5°) — vrátí stínovou viditelnost v tropech (dnes objekty ploché v poledne). Wait pro user feedback.

**Sub-prah G0 follow-up (zachované):**
- [ ] **Atlas IIFE raw geom strip UV at source + rename** na `_RAMP_RAW_GEOM_*` (~50 ř. cleanup).
- [ ] **TTUNELS migrate na lowpoly** nebo drop dead třída.
- [ ] **Drop-in animace tiles při `regenerateScene`** (z tiny-world-builder `dropAnims`).
- [ ] **Tilt-shift post-process** (DD-kandidát).
- [ ] **ExtrudeGeometry pro rampy** (lazy refactor).

**DAY-cycle (DD-38/DD-43 follow-up):**
- [ ] **Slider DAY sync z value** při auto-advance.
- [ ] **Sunset/sunrise barevný peak** (DD-39 ext.).

**Multi-instance:** Sez. 36 single-instance (mrkla stroj). Topic branch `feat/biome-surfaces` — Q při %END: dokončený? ANO → merge `--no-ff` do `main`.
