# 2026-05-13

## Sezení 27

### Diskuse

**(A) `%BEGIN` post-mortem — 7 nepushnutých commitů `feat/terrain` v `main`.**

`%BEGIN` proběhl normálně (git pull no-op, server :8000 spuštěn). Cadence prahy: `%AUDIT:CODE` 8/8 ✓, `%AUDIT:DOCS` 10/10 ✓ — oba dosažené, doporučené pro sez. 27. Performance při 30×30 priorita z Příště sez. 26.

Při shrnutí stavu user upozornil: *„Aha, asi neproběhl merge z feat/terrain."* Diagnostika `git log main..feat/terrain --oneline` potvrdila **7 commitů** od kotvy DD-32 (`d027001`) přes Fáze 1+2+3 (wipe `010d7ac` → MVP `845c0d2` → ramp smoothing `68a1fda`) + 3 docs commity sez. 24/25/26.

Není to nedopatření z `%END` sez. 26 — je to topic-branch workflow stejně jako `feat/factory` (sez. 21–23, mergnuta `e0b5fa3` po dokončení fáze A+B+steady-state). Sez. 26 push šel správně do `origin/feat/terrain`. Co chybělo: explicitní rozhodnutí o **kdy mergnout** topic branch zpět do main.

**(B) Merge varianta — A vs. B vs. C.**

- **(A)** Merge teď. Terrain MVP end-to-end funkční (Fáze 0+1+2+3 hotové, ramp smoothing 92 % pokrytí, UI panel). Performance refactor pak na vlastní `feat/terrain-perf` — clean scope.
- **(B)** Pokračovat na `feat/terrain` přes sez. 27 performance fix, merge až poté („není production-ready bez performance při 30×30").
- **(C)** Merge teď + TODO „aktualizovat PROMPTS.md `%END` o topic-branch workflow."

User vybral **(A)**. Provedeno: `git checkout main && git merge --no-ff feat/terrain` — merge commit `b1295ea`, **12 souborů, +1557/−1709**, nový `src/terrain.js` 411 ř. `git push origin main` ✓.

### Rozhodnutí

- **Merge `feat/terrain` → `main` (--no-ff, b1295ea)** po dokončení Fáze 0+1+2+3 DD-32. Topic branch zůstává (nesmazaná) — sez. 28 použije `feat/terrain-perf` pro performance refactor.
- **Žádný DD.** Operační rozhodnutí, ne arch.

### Kód

Žádný kód v sez. 27. Pouze merge + dokumentace.

### Kudos / Censure

- **Censure! (AI → AI):** `%END` sez. 26 měl explicitně řešit merge strategii topic branche, ne jen pushnout do `origin/feat/terrain` a tiše skončit. User musel sám zachytit že merge chybí — `%BEGIN` sez. 27 neukázal stav `main..feat/terrain` jako standard kontrolu (ukazoval jen současné `git status` + `git log` z výchozího banneru).
- **Censure! (PROMPTS.md):** `%END` text doslova říká *„Commit na větev `main`"* — zastaralé od sez. 21, kdy DD-30 zavedl topic branches (`feat/factory`, později `feat/terrain`). PROMPTS.md `%END` (3.) potřebuje přepis o topic-branch workflow + merge rozhodnutí. **TODO** zapsáno.
- **Kudos! (user):** rychlá detekce stavu po `%BEGIN` shrnutí — předtím než sez. 27 začalo skutečnou práci. Multi-instance discipline (přechod na druhý stroj) by jinak byla riziková (mrkla stroj by pullnul `main` bez terrain MVP).

### Příště (sez. 27 → sez. 28, na stroji mrkla)

**Performance — priorita (přesun ze sez. 26→27):**

- [!] **30×30 vykreslování pomalé** — `%THINK` výběr cesty:
  - `InstancedMesh` per (kind, surface).
  - `BatchedMesh` (Three.js r172+).
  - Mesh merge per cell.
  - Web worker pro `generateTerrain`.
- Větev: **`feat/terrain-perf`** (založit na `main` po pull).

**Audit cadence (přesun ze sez. 26→27, oba stále dosažené):**

- [!] **`%AUDIT:CODE`** — 8/8 sezení od sez. 18. Doporučení: provést **až po** performance refactoru (refactor pravděpodobně přepíše `spawnTerrain`/`createMeshFor`).
- [!] **`%AUDIT:DOCS`** — 10/10 sezení od sez. 16. Může proběhnout kdykoli (nezávislé).

**Otevřené body:**

- [ ] **PROMPTS.md `%END` aktualizovat** o topic-branch workflow (založit větev / merge rozhodnutí na konci milestone). Sub-prah governance.
- [ ] TDRAMP compatibility check, 2-voxel stepy, 4-cell jámy, LIQUID třída, klastrování water cells (z Příště sez. 26).

**Multi-instance discipline:** Sez. 27 single-instance. Přechod stroj hejna → mrkla je sekvenční (po push), ne souběh — žádný worktree potřeba.

## Sezení 28

### Diskuse

**(A) `%BEGIN` zajímavost — pull dvakrát.**

`%BEGIN` `git pull --ff-only` proběhl normálně (9 commitů z hejna). User upozornil, že na druhém stroji vidí jiné UI (`#terrainctrl`) — pre-pull stav byl 9 commitů pozadu (factory toy), post-pull terrain MVP. Po `git fetch` se objevily 2 nové remote větve: `origin/feat/factory` (už mergovaná), `origin/feat/terrain` (NEmergnutá v main na čas %BEGIN). User vysvětlení: hejno mezitím sez. 27 mergnulo `feat/terrain` → main + push, takže druhý `git pull` dotáhl ještě 2 commity (sez. 27 merge + diary). **Lesson:** `gitStatus` banner v systémovém promptu Claude Code se NEaktualizuje za session; vždy `git status` / `git log` přes Bash.

**(B) `%THINK` performance — diagnose first.**

User feedback ze sez. 26: 30×30 vykreslování pomalé (FPS ~15). TODO sez. 27 navrhuje 4 cesty (InstancedMesh / BatchedMesh / mesh merge / web worker). `%THINK` default oponoval rovnému refactoru: **měřit nejdřív**, neznáme bottleneck (a) generace / (b) scene mutace / (c) per-frame render. Plus low-hanging fruit: `faceMaterialFor` aktuálně alokuje `MeshStandardMaterial` per blok (~16000 per 30×30 regen), memoizace by ušetřila GC/allocator pressure i kdyby calls zůstaly.

User „Jasně!" → diagnostika.

**(C) Atlas refactor — data po diagnostice.**

Tři instrumenty v `feat/terrain-perf`:
1. **`faceMaterialFor` memoizace** — `Map<key, Material>` cache. Po 30×30 regen: mat unikátů 4 (`:grass-top` / `:dirt` / `:stone` / `:sand`) místo ~16k alokací.
2. **`console.time`** v `spawnTerrain` — 3 fáze (`generateTerrain` / `spawnTerrain.blocks` / `spawnTerrain.water+ramps`).
3. **Perf HUD** pravý horní roh — FPS / calls / triangles / geometries / cached materials (`renderer.info`, throttle 1×/s).

**Baseline (po material cache, před atlasem) @ 30×30:**

| metrika | hodnota |
|---|---|
| FPS | 15 |
| calls | 25 106 |
| tri/call | ~2 (smoking gun) |
| generateTerrain | 2.5 ms |
| spawnTerrain.blocks | 38.6 ms |
| total regen | ~43 ms |
| `[Violation]` rAF handler | 50-64 ms |

Klíčový nález: `tri/call ≈ 2` = 1 face per draw call. To je `THREE.Mesh` s `material = [m0..m5]` rozsekáním na 6 material groups → **6× draw calls per box**. Material cache šetří GPU state changes mezi calls, ale počet calls neredukuje. Generace = trivial (5 % regen), worker dead. Render loop = jasný bottleneck.

**Atlas refactor:**
- `getSharedAtlasBoxGeom()` — sdílená `BoxGeometry` (1 instance pro všechny terrain TCUBES) s UVs přepsanými na 1/6-tice atlasu (face N → u ∈ [N/6, (N+1)/6]).
- `getTcubesKindMaterial(kind)` — per-kind atlas: 6 facelets slepené horizontálně do CanvasTexture 96×16 px, `NearestFilter` + `SRGBColorSpace`. Negative cache pro non-terrain kindy.
- `createTCubeFor` fast/slow path dispatch: terrain kind (matches `BLOCK_TEXTURES`) → shared geom + atlas material (1 draw call). Jiné → původní material array (slow path zachován pro emoji demo boxy).

**Po atlas refactoru @ 30×30:**

| metrika | před atlasem | po atlasu | změna |
|---|---|---|---|
| FPS | 15 | **92** | **6.1×** |
| calls | 25 106 | ~5 050 | 0.20× |
| tri | 49 432 | 49 480 | shoda |
| geom | ~5 500 | **8** | sdílená box geom |
| spawnTerrain.blocks | 38.6 ms | 6.5 ms | 5.9× rychlejší |
| total regen | 43 ms | 9 ms | 4.8× rychlejší |
| `[Violation]` | 50-64 ms | žádné | — |

10×10 a 20×20 propad calls 4-6×, FPS roste z 32 → 142 @ 20×20. Atlas dodal predikci přesně (6× redukce). Sez. 28 cíl 30×30 playable splněn (92 FPS = 144 Hz monitor cap ≈ 64 %).

**(D) Rampy atlas — odloženo.**

256 rampů @ 30×30 × 4-5 faces ≈ 1 200 calls (z 5 050 = 24 %). Atlas pro rampy by ubral další ~1 000 calls. **Diminishing return:** marginal benefit malý, refactor 4× repeat pattern (TRRAMPS / TTRAMPS / TDRAMP / TTUNELS, každý vlastní BufferGeometry s manuálním UV mapováním per face). User volba **(2) skončit teď, sez. 29 zvlášť**.

### Rozhodnutí

- **Atlas refactor TCUBES** — shared geom + per-kind material. 6× redukce draw calls.
- **`faceMaterialFor` memoizace** — cache zachová napříč refactory.
- **Perf HUD permanent** — `#perfhud` pravý horní roh, throttle 1×/s. Není dev-only, je to projektová observability pro budoucí refactory.
- **Rampy atlas odloženo** do sez. 29 (diminishing returns, ~17 % incremental).
- **Žádný DD.** Perf optimalizace, ne arch rozhodnutí o modelu.

### Kód

`feat/terrain-perf` větev:

- **`src/main.js`:**
  - `faceMaterialFor` (376) — memoizace přes `_faceMaterialCache` Map (~30 řádků refactor).
  - `createTCubeFor` (996) — fast/slow path dispatch.
  - Atlas helpers po `BLOCK_TEXTURES` (1873): `ATLAS_TILE_PX`, `ATLAS_FACE_KEYS`, `getSharedAtlasBoxGeom`, `_tcubesAtlasMatCache`, `getTcubesKindMaterial`, `buildKindAtlasTexture` inline (~75 řádků).
  - `animate` (2298) — `_perfHud` throttled report (FPS + renderer.info → 5 DOM elementů).
- **`index.html`:**
  - `#perfhud` CSS (top right corner, izomorfně s `#hud` left) + 5 řádků DOM (FPS / calls / tri / geom / mat).

Diff stat: `src/main.js` +135/−25, `index.html` +30/−0.

### Kudos / Censure

- **Kudos! (AI → user):** příjemný flow „Jasně!" → diagnose → data → atlas → result, 2× interaction. Plán + Q schválené stručně → ihned implementovat (memory `feedback_plan_then_go`). 
- **Censure! (AI → AI):** `%BEGIN` ukazoval první pull stav (9 commitů), ale propásl, že na druhém stroji možná existují další větve. User musel říct *„na druhém stroji vidím něco jiného"* a já jsem to teprve diagnostikoval. `git fetch --all` ukázal `feat/terrain` + `feat/factory` hned na začátku — měla jsem komentovat *„remote má 2 topic větve mimo main, zkontrolovat"*. Konvenční gitStatus banner v systémovém promptu mě navedl na sez. 19 stav, ale fetch jasně ukázal víc.
- **Censure! (AI → AI):** Q2 v %THINK jsem nepoložila s explicit labels (1/2 ne Q2.A/Q2.B), takže user odpověď „2" byla dvojznačná („pokračovat rampy" vs. „skončit"). Per CLAUDE.md *„Při kladení více otázek najednou je označovat Q1, Q2, Q3, …"*. Příště numbering + sub-labels.

### Příště (sez. 28 → sez. 29)

**Performance follow-up:**

- [ ] **Rampy atlas refactor** (TRRAMPS / TTRAMPS / TDRAMP / TTUNELS) — stejný pattern jako TCUBES, ~17 % incremental redukce calls. 4× per ramp type: per-(type, surface) atlas material + shared geom s UV remap na 1/N-tice (N = 4 nebo 5 faces).
- [ ] **Vizuální parita** TCUBES atlas — pre-flight kontrola před sez. 29 dalšími changes (user nepotvrdil v sez. 28 explicitly).

**Audit cadence (oba prahy stále překročené, sez. 28 nepřinesla audit):**

- [!] **`%AUDIT:CODE`** — 10/8 sezení (přes prah o 2). Doporučení sez. 29 nebo 30 — `feat/terrain-perf` refactor přepsal `createTCubeFor` + přidal atlas pipeline, audit by zachytil případné dluhy.
- [!] **`%AUDIT:DOCS`** — 12/10 sezení (přes prah o 2). Sez. 29 nebo dříve.

**Governance:**

- [ ] **PROMPTS.md `%END` aktualizovat o topic-branch workflow** — stále z TODO sez. 27.

**Multi-instance:** Sez. 28 single-instance (mrkla stroj). `feat/terrain-perf` lokálně, push do origin při merge.

