# 2026-05-12

## Sezení 23

### Diskuse

**(A) Steady-state polish — bod #3 z Příště sez. 22.**

Sezení 22 odhalilo PAUS/RSUM oscilaci na `sawmill`: forest produkuje 0.5 logs/s, recipe spotřebuje 1 log/cyklus × 1.0/s = 1.0 logs/s. Source-limited (0.5 vs. 1.0) → sawmill trvale starves, event log 30 Hz spam.

Tři možnosti rozdiskutovány v Příště:
1. **Druhý forest** (data, fyzikálně narativní — „víc lesů víc dřeva").
2. **Ladění rates** (data, nejmenší tření — 1 změna).
3. **`setPaused` debounce 200 ms** (engine, čistší ale skrývá real podmínku).

User vybral **#2** (`lad rates na 1:1`). Provedeno: `forest.outputs.logs = 0.5 → 1.0`. Conveyor `THROUGHPUT=2` ks/s zůstává nad rate, není bottleneck.

**(B) CLAUDE.md slim — meta cleanup.**

User: *„v lokální CLAUDE.md vidím navíc, co bych spíš čekal v README.md"*. Diagnostika redundancí proti globálnímu `~/.claude/CLAUDE.md` a `README.md`:

| Sekce v lokálním `CLAUDE.md` | Patří | Důvod |
|---|---|---|
| Project / Status / scéna / 4-vrstvá taxonomie / hierarchie modelu / Aktuální obsah scény | `README.md` | Project info pro čtenáře, ne AI overlay. README už to má. |
| Sezení changelog (sez. 14–22, lineárně roste 1 odst./sezení) | `docs/DIARY.md` | Duplicitní s DIARY, regrese roste se sezeními. |
| Dokumenty seznam, Plánované třídy | `README.md` / `docs/TODO.md` | Duplicitní. |
| Design Principles (zkrácená) / Workflow / Kudos!Censure (1 ř.) | — *(smazat)* | Globální má bohatší verze. |

**Provedeno:** lokální `CLAUDE.md` zredukován **150 → 32 řádků** (78 % redukce). Zůstává jen skutečný AI overlay: Code Style nuance (user-learner), `%THINK` bod 5 (sandbox/factory mechanika), Makra odkaz na `PROMPTS.md`, Key Files mapa, kde najít co.

**(C) Globální pravidlo — kam příště podobnou kontrolu zařadit.**

User: chce kontroly redundance / správné rozdělení informací mezi soubory dělat pravidelně. Otázka: `%AUDIT:DOCS` nebo `%CALIBRATE`?

**Verdict: `%CALIBRATE` sekce A (Context hygiene).** Důvod: `%AUDIT:DOCS` cílí na content kvalitu (překlepy, broken links, chronologie) v project docs. Dnešní problém byl **governance** — role-confusion mezi řídícím dokumentem (CLAUDE.md) a content dokumenty (README/DIARY/DD). To je doména CALIBRATE A („CLAUDE.md / PROMPTS.md aktuální?").

Plus: dnešní regrese ukázala, že **CLAUDE.md roste lineárně** se sezeními, takže prah ≥ 15 sezení pro CALIBRATE je pomalý. Přidán **sub-prah nezávislý na sezení-count:** *„nárůst projektového CLAUDE.md o ≥ 50 % od posledního CALIBRATE"* jako mimořádný trigger.

Plus: doplněna explicit **bariéra v `%DOCS`** proti role-confusion — 4 pravidla, kam se *nikdy* nepíše (changelog → DIARY, status → README, DD → DESIGN_DECISIONS, CLAUDE.md jen pro AI pravidla). To je preventivní bariéra; bez ní bude CALIBRATE jen periodicky čistit obnovující se nepořádek.

### Rozhodnutí

- **Steady-state forest rate** 0.5 → 1.0 logs/s. 1:1 matchne sawmill, žádná oscilace.
- **Lokální `CLAUDE.md` slim** 150 → 32 ř. — jen skutečný AI overlay nad globálním.
- **`%CALIBRATE` sekce A rozšířena** o „Role discipline řídících dokumentů" + sub-prah 50 % nárůstu CLAUDE.md (globální `~/.claude/CLAUDE.md`).
- **`%DOCS` dostal bariéru** proti role-confusion — 4 explicitní pravidla, kam se nepíše (globální).
- **Žádný nový DD.** Steady-state je data tweak. CLAUDE.md slim + globální pravidla jsou governance, ne arch rozhodnutí o modelu projektu.

### Kód

- `src/model.js`: `FACILITY_DEF.forest.outputs.logs` 0.5 → 1.0 + aktualizovaný komentář (1 řádek dat, 3 řádky docstring).
- `CLAUDE.md` (projektový): kompletně přepsán, 150 → 32 řádků.
- `~/.claude/CLAUDE.md` (globální): `%CALIBRATE` A + `%DOCS` bariéra (mimo repo TheCubes, necommituje se).

### Kudos / Censure

- **Kudos! (user → AI):** AI diagnostikovala redundance s konkrétní tabulkou (řádek / patří kam / důvod) místo abstraktního „je to dlouhé". Plus explicit rozdíl mezi „role-confusion" (governance, CALIBRATE) a „content redundance" (AUDIT:DOCS) — usnadnilo verdikt o správném makru.
- **Kudos! (AI → user):** *„kam příště podobnou kontrolu zařadit"* — proaktivní governance otázka po jednorázové intervenci. Ne fix-and-forget, ale systematizace.
- **Censure! (AI → AI):** nárůst CLAUDE.md o 4 odstavce za sez. 20+21+22 měla AI registrovat sama při každém `%DOCS`. Pravidlo „status do CLAUDE.md, changelog do CLAUDE.md" nebylo explicitně zakázané, ale princip „role discipline" měla AI držet bez explicit pravidla. Bariéra v `%DOCS` je sice systémový fix, ale ideálně by tu být nemusela — měla by stačit selfdiscipline.

### Příště (sez. 23 → sez. 24)

**Fáze B dokončení (`feat/factory` větev):**

- [ ] **Material gate vizualizace** — emissive boost (červená/amber) na PAUSED fasilitách, lazy material clone à la hover highlight. *(Sez. 22 a 23 odložené.)*
- [ ] **Druhý zdrojový řetězec** — quarry → conveyor → crusher → conveyor → storage. Plus well → pipeline → storage, coal_mine → conveyor → storage (bez transformeru, jen těží a teče). Verifikuje izomorfismus dvou řetězců paralelně. Pozn.: quarry/well/coal_mine mají defaultní rates 0.4/0.6/0.3 — crusher recipe je 1 stone × 0.8/s = 0.8/s spotřeba ⇒ source-limited stejně jako sawmill byl. Buď bumpnout quarry na 0.8, nebo nechat (event log spam je info, ne bug — observer toy).
- [ ] **Merge fáze B → main** — po stabilizaci výše.
- [ ] **Fáze C plánování** — editor MVP (click na voxel → KIND paleta, R-klik delete, PATH draw mode). `%THINK` pro modal/dropdown vs. embedded toolbar.

**Audit cadence stav (po sez. 23):**
- `%AUDIT:CODE` — 5/8 sezení od sez. 18 (OK).
- `%AUDIT:DOCS` — 7/10 sezení od sez. 16 (OK, ale blíží se).
- IDEAS/TODO pruning — 4/12 (OK).
- `%CALIBRATE` — 23-X (last nedohledáno; nový sub-prah „CLAUDE.md +50 %" by se po dnešním slimu měl resetovat).

---

## Sezení 24

### Diskuse

**(A) %BEGIN diagnostika.** Větev `feat/terrain` (kotva DD-32 sez. 23, commit `d027001`) bez upstreamu — `git fetch origin` doběhl, `main` synchronní. **Uncommitted strom při startu:** `M src/main.js M src/model.js` (-568 / +9 řádků = první fáze wipe per DD-32 — práce z předchozí instance / přerušeného sezení, ne tato instance). Server na :8000 už poslouchá (PID 28496). Příště ze sez. 23 (Material gate, druhý zdrojový řetězec, fáze C) vědomě překonáno DD-32 — ne stale, pivot.

**(B) Návrh programu sez. 24.** Tato instance navrhla rozdělit DD-32 implementační plán: sez. 24 = wipe-commit + `src/terrain.js` MVP s hardcoded parametry, sez. 25 = `buildScene` integrace + HUD panel `#terrainctrl`. Souhlas s rozdělením otevřen otázkou.

**(C) Pivot — user otázka:** *„Běží více instancí Claude nad tímto projektem. Jak se provádí, aby se předešlo nežádoucím konfliktům?"*

5 reálných rizik (od největšího):
1. **File-level race** v pracovním stromu — dvě instance editují stejný soubor, druhý zápis přepíše první (žádný OS lock, Edit tool chytí jen předchozí Read téže instance).
2. **`%BEGIN` stash race** — dvě paralelní `git add . && git stash` posloupnosti se rozpadnou (první stashne, druhá stashne nic, popování pak vede ke konfliktu nebo ztrátě).
3. **Docs race** — DIARY.md / TODO.md / MEMORY.md (index) — Write přepíše bez merge.
4. **Session numbering collision** — obě instance napíšou `## Sezení 24` v diáři.
5. **Port 8000** — bezpečné (jeden listener, ostatní vidí „už poslouchá").

Doporučení (od silného ke kompromisu):
- **`git worktree add ../TheCubes-<topic> feat/<topic>`** per instance — každá instance vlastní adresář + větev, konflikty se řeší až mergem. Nejčistší.
- **„Dirigent" pattern** — jedna instance smí docs + `%BEGIN`/`%END` + commits, ostatní jen kód v izolovaném souboru (např. `src/terrain.js` ve sdíleném worktree, kde nikdo jiný `src/` needitujе).
- **Minimum** — single instance pro `%BEGIN`/`%END` a Edit operace, paralelní jen `%THINK`/research.

**(D) Live demonstrace konfliktu.** Mezi `%BEGIN` a `%END` této instance paralelní Claude commitla na `feat/terrain` **dva commity**:
- `010d7ac refactor(wipe): odstranit factory toy + severskou diorámu (DD-32 Fáze 1)`
- `845c0d2 feat(terrain): generateTerrain MVP — value-noise heightmap + biome map (DD-32 Fáze 2)`

A vytvořila `src/terrain.js` (210 ř.) z untracked přes commit. Mezi prvním a druhým `git status` v této instanci se strom přepnul z `M main.js + ?? terrain.js` → `clean`. **Přesně ten case, který jsme diskutovali, naživo.** Tahle instance dělala jen %BEGIN diagnostiku a meta-diskusi; veškerý kód sez. 24 patří paralelní instanci.

### Rozhodnutí

- **Adoptovat `git worktree` pattern** pro multi-instance setup. Konkrétní příkaz: `git worktree add ../TheCubes-<topic> feat/<topic>`. Spouštěč Claude (uživatel) ve worktree adresáři.
- **Žádný DD** (workflow doporučení, ne arch rozhodnutí). Pokud se ujme, kandidát na zápis do globálního `~/.claude/CLAUDE.md` v sekci „Multi-instance discipline" při příštím `%CALIBRATE`.

### Kód

Tato instance: **žádný kód**.

Paralelní instance (sez. 24 práce — řazena pod sez. 24 v diáři proto, že je to stejná wallclock perioda):
- `010d7ac` — wipe per DD-32 Fáze 1: smazány factory toy třídy (FACILITY/GENERATOR/TRANSFORMER/STORAGE), registry (RESOURCES_DEF/RECIPES_DEF/FACILITY_DEF), engine (productionTick/pathTick/transferOnPath/aggregateResources/logEvent/renderEventLog), HUD (`#resources`/`#eventlog`/`#simctrl`), severská dioráma (SCENE_LAYOUT/populateNorthernScene/topVoxelMap/mulberry32), tunely+rampy+dekorativní cesta, COMPOSITES TREE/GRASS_TUFT/ROCK_PIXEL/LOG, `tree_sway` + `WIND_STRENGTH`. PATH atributy SOURCE/SINK/RESOURCE/THROUGHPUT odstraněny, PATH třída sama zůstala. Delta: model.js -258 ř., main.js -301 ř.
- `845c0d2` — `src/terrain.js` (210 ř.) MVP: value-noise heightmap + biome map (`generateTerrain({ size, relief, surfaces, seed })`). Sez. 25 dokončí integraci do `buildScene` + HUD panel.

### Kudos / Censure

- **Censure! (AI → AI):** `%BEGIN` této instance měl jako *první* krok zkontrolovat, zda na repu nemůže být jiná aktivní instance (signál: uncommitted `-568` ř. wipe odpovídající DD-32 Fáze 1, kterou tahle instance nezačala). Místo toho navrhla program sez. 24, který paralelně realizovala druhá instance. Bariéra: před návrhem programu se ptát „existuje jiná instance, která tohle už dělá?".
- **Kudos! (user → AI workflow):** User identifikoval governance problém **dřív**, než došlo k tvrdému konfliktu (file collision, lost stash, double-edit). Včasná diagnostika ušetřila pozdější merge cleanup.
- **Censure! (AI → AI, dříve dnes):** předchozí instance commitla wipe + terrain MVP bez varování v diáři ani upstream pushe (větev `feat/terrain` zůstává bez `origin`), takže koordinace s ostatními instancemi je čistě přes lokální `.git` stav — nový spuštěč Claude na jiném stroji o této práci neví. **Fix:** `git push -u origin feat/terrain` jako součást tohoto `%END`.

### Příště (sez. 24 → sez. 25)

*(Paralelní instance zapsala Příště; reálný stav po sez. 25 níž ukazuje, že 3 ze 4 bodů jsou hotové.)*

- [x] **`buildScene` integrace** — *Dořešeno v sez. 25 commitu `845c0d2` (součást generátor MVP, ne separátní krok).*
- [ ] **HUD panel `#terrainctrl`** v `index.html` — relief slider 0..10, size sliders [sx, sz], surface mix sliders (grass/stone/sand/water), seed input, Generate button. *(Zůstává pro sez. 26.)*
- [x] **Vodní plane(y)** — *Hotovo v sez. 25 (helper `createWaterPlane` v `main.js`, MVP 1×1 plane per water cell).*
- [ ] **Multi-instance workflow** — pokud user dál provozuje paralelní instance, převést na `git worktree` (`git worktree add ../TheCubes-<topic> feat/<topic>`). *(Workflow doporučení, ne kódovou TODO.)*

---

## Sezení 25

*Pořadí dne: sez. 23 (factory polish) → sez. 24 (paralelní meta + Live demo o multi-instance discipline) → sez. 25 (toto — DD-32 implementace end-to-end). Souběžně byly aktivní dva chaty; tato instance držela „dirigent" role pro kód+commits+`%END`.*

### Diskuse

**(A) `%BEGIN` proběhl normálně.** `git pull` čistý, `main` `Already up to date` (origin/feat/factory zatím nepushnuto, ale repo v synchronu). Příště ze sez. 23 (Material gate, druhý zdrojový řetězec, fáze C editor MVP) bylo nabídnuto. Cadence prahy OK.

**(B) Pivot otázkou „Jak je kódován terén?"** User si přečetl `SCENE_LAYOUT` strukturu (sez. 14 export z builderu) — ploché pole tuplů `[kind, x, y, z]` + per-instance konstruktory pro rampy/tunely/cestu. Tato debata dala kontext pro další otázku.

**(C) `%THINK` o `generateTerrain` API.** User: *„Něco na způsob `GenerujScenu(5x5, 1 [<-relief: from flat to alpine], [80% grass, 10% stone, 5% water, 5% sand])`."* — 4 mikro-rozhodnutí:

1. **Reliéf**: spojitý 0..1 vs. discrete preset vs. dva skaláry (amp + roughness). → **Spojitý 0..1** (později user upgradoval na **0..10 integer** s vlastní 11-stupňovou škálou Flat / Level / Gently undulating / Rolling / Hilly / Uneven / Rugged / Craggy / Mountainous / Heavily dissected / Alpine).
2. **Surfaces**: random per-voxel vs. biome klastry vs. voronoi. → **Biome klastry** (secondary low-freq noise, sort + exact-match thresholds).
3. **Cíl**: náhrada `SCENE_LAYOUT` vs. paralelní arena vs. obojí. → **Náhrada** (drop-in).
4. **Voda**: TCUBES water blok vs. start bez vody vs. **plane nad depression** → **plane** (efektnější, méně KISS, ale 1×1 mesh per cell zvládáme).

Vše zapsáno do `docs/TODO.md` sekce „Terrain generator" + plán fází 0–3 (kotvení / wipe / generator / UI panel).

**(D) Pivot k full DD-32 implementaci.** User: *„Odstraň vše z naší scény! Vytvoř generátor. V panelu si nastavím parametry terénu."* — Před kódováním 4 AskUserQuestion (Censure! prevence proti silent pivotu):

1. **Factory toy a HUD** → smazat všechno (DD-30/31 immutable historie zůstává).
2. **Větev** → merge `feat/factory` → `main`, nová `feat/terrain` (čisté).
3. **Boot scene** → prázdná, dokud user nezmáčkne Generovat (žádný surprise terén).
4. **Panel controls** → relief + size sx/sz + 4 surfaces + seed + Regenerate.

User vybral všechny doporučené volby → **plný DD-32 pivot s čistou kotvou**.

### Rozhodnutí

- **DD-32** zapsán do `docs/DESIGN_DECISIONS.md` jako kotva před wipe + implementací. Reference na DD-30/31 jako historicky platné, ale revoked z aktivního vývoje.
- **Větev workflow**: merge `feat/factory` → `main` (`--no-ff`, sez. 21–23 historie zachována) → push → nová `feat/terrain` z main → práce. Push `feat/terrain` až v `%END`.
- **Boot default**: `generateTerrain({ size: [10, 10], relief: 3 /* Rolling hills */, surfaces: { grass: 0.80, stone: 0.10, sand: 0.05, water: 0.05 }, seed: 42 })`. Live regenerate přijde s panelem (sez. 26).
- **Realief škála 0..10 integer** s 11 pojmenovanými stupni (od „Flat / Dead flat" po „Alpine / Jagged high-relief"). MVP plně podporuje 0–5; 6–8 přes value-noise jednodušším mappingem; 9–10 graceful degradation (clamp na 8 + warn) — valley carving = roadmap.
- **Žádný další DD** (DD-32 už zafixován).

### Kód

**Fáze 0 — kotvení** (commit `d027001` „docs(DD-32): terrain generator pivot — kotva před wipe a generátorem"):
- `docs/DESIGN_DECISIONS.md`: +113 ř. (DD-32 sekce).
- `README.md`: -52 / +50 (identitní update — sandbox terrain místo factory toy).
- `CLAUDE.md`: -3 / +4 (Key Files mapa: terrain.js přibude, factory engine pryč; %THINK bod 5 reformulován).
- `docs/TODO.md`: +57 ř. (sekce „Terrain generator" s API, stupnicí, úkoly, otevřenými body).

**Fáze 1 — wipe** (commit `010d7ac` „refactor(wipe): odstranit factory toy + severskou diorámu (DD-32 Fáze 1)"):
- `src/model.js`: **705 → 450 ř. (−255 ř., −36 %)**. Smazáno: TREE/GRASS_TUFT/ROCK_PIXEL/LOG (COMPOSITES potomci), FACILITY/GENERATOR/TRANSFORMER/STORAGE třídy, registry RESOURCES_DEF/RECIPES_DEF/FACILITY_DEF, PATH atributy SOURCE/SINK/RESOURCE/THROUGHPUT, world.RESOURCES agregát.
- `src/main.js`: **3252 → 1981 ř. (−1271 ř., −39 %)**. Smazáno: FACTORY TOY ENGINE (productionTick/pathTick/transferOnPath/generatorTick/transformerTick/setPaused/logEvent/renderEventLog/aggregateResources/maybeLogProduction/maybeLogConsumption + facilities/paths/events ringbuffer), createFacilityFor + facilityMat, TREE_BUILDERS (10 builderů: spruce/oak/birch/palm/bush/cypress/willow/bonsai/dead/maple) + treeMat/treeVoxel/treeBlock/treeDiamond + TREE_C, DECO_C + GRASS_TUFT_BUILDERS + ROCK_PIXEL_BUILDERS + LOG_BUILDERS + dispatchery, SCENE_LAYOUT (~145 voxelů), makeStoneBlock/makeDirtBlock/makeGrassBlock factories, buildScene tělo (tunely + rampy + path_0 + 3 fasility + 2 conveyor PATH), populateNorthernScene + topVoxelMap + mulberry32 + pathOccupiedCells + spawnTree/Grass/Rock/Log, animateTreeSway + ANIMATORS.tree_sway entry, TIME_SCALE slider listener, render loop volání productionTick/pathTick/aggregateResources.
- `index.html`: **161 → 85 ř. (−76 ř., −47 %)**. Smazáno: #resources panel, #simctrl (TIME_SCALE slider), #eventlog (severity-colored ticker). Zachováno: #hud (TIME label), #tooltip, #scene canvas.
- **Celkem −1602 řádků (−39 %)** z 4118 na 2516.

**Fáze 2 — generátor MVP** (commit `845c0d2` „feat(terrain): generateTerrain MVP — value-noise heightmap + biome map (DD-32 Fáze 2)"):
- **`src/terrain.js` (nový soubor, 173 řádků)** — `mulberry32` (DRY z populateNorthernScene), `makeValueNoise(sizeX, sizeZ, freq, seed)` (grid sampling + bilineární smoothstep + wrap-around modulo), `generateTerrain({ size, relief, surfaces, seed })` → `{ blocks, water }`. Algoritmus: heightmap (relief-driven amp×freq lookup) → biome map (low-freq noise, sort + exact-match thresholds) → Y modifier per biome (sand −1, water −2, grass/stone netknuté) → sloupcové vyplnění (top voxel dle biome; water → dirt top; dirt middle; stone yBottom) → water cells → 1×1 plane(y) na `y_top + 0.55` (anti z-fight). Validace fail-fast: size kladné, relief 0..10, surfaces součet ≈ 1.0, neznámé surface throw. Graceful degradation relief 9..10 → clamp 8 + console.warn (valley carving roadmap).
- **`src/main.js` integrace**: import `generateTerrain`, **`:sand` textura** (paletový patch SAND_BASE+SAND_ACCENTS), `BLOCK_TEXTURES` lookup per kind (grass/dirt/stone/sand — všechny 6 stran + grass má `:grass-top` na TOP, sides `:dirt` per BLOCKS rodina pravidlo sez. 17), `createBlock(kind, x, y, z)` helper → `TCUBES`, `createWaterPlane(w)` helper → `THREE.Mesh` (transparent 0.7 opacity, MeshStandardMaterial, DoubleSide, PlaneGeometry 1×1, rotace −π/2 X), `TERRAIN_DEFAULTS` konstanta, `buildScene` volá `generateTerrain(TERRAIN_DEFAULTS)` + spawne bloky + water planes + zachová GridHelper/AxesHelper.

**Smoke test (Node ESM)**: 100 cells → **321 bloků** (80 grass + 110 stone + 126 dirt + 5 sand) + **5 water planes** (5 % depression cells klastrované biome noisem). Server `localhost:8000` vrací 200 pro `/`, `/src/main.js`, `/src/terrain.js`, `/src/model.js`. `node --check` OK pro oba JS soubory.

**Browser test** (user report): scéna se vykresluje, *„x rovinatou krajinu, s jedním kaňonem s vodou na dně"* = relief 3 amp=1 + 5 water cells klastrované biome noisem → hluboký 2-voxelový kaňon. Funkce, ne bug.

### Kudos / Censure

- **Kudos! (user → AI):** Před wipe AskUserQuestion s 4 otázkami → vyloučená silent pivot a vyjasněné scope. Identitní pivot (DD-30/31 → DD-32) zaznamenán řádně do `DESIGN_DECISIONS.md`, ne tichá změna. Plus user explicit upgrade `relief: 0..1` na **`relief: 0..10` integer** s pojmenovanými stupni — sémanticky bohatší než původní AI návrh, hodnotnější API.
- **Kudos! (AI → AI workflow):** Sezení 25 zvládlo plný DD-32 lifecycle (rozhodnutí → kotva → wipe → implementace → smoke test → user browser test) na jedno posezení. Generátor stojí napoprvé.
- **Censure! (AI → AI, vůči sez. 24 paralelní instance):** Paralelní instance ve svém diary záznamu (`d2f7c1e`) **přivlastnila si sez. 24** pro meta-diskusi a moji práci popsala jako sub-event „paralelní instance commitla…". Per CLAUDE.md *„Sezení = jeden chat od `%BEGIN` po `%END`"* — paralelní byla samostatné sezení, ale **přivlastnila si číslo bez koordinace**. Pragmaticky to znamenalo, že index DIARY.md a její diary entry vypadají jako oficiální záznam mé práce (s nepřesnostmi: terrain.js 210 ř. namísto 173, main.js wipe −301 ř. namísto −1271). Sez. 25 (tato) zápis přidává přesnosti + vlastní narrative.
- **Censure! (AI → AI):** Při velkém wipe (Fáze 1) jsem 4×ignorovala `<system-reminder>` o TaskCreate. Reminder byl správně ignorován (sekvenční práce, ne paralelní úkoly), ale řádné by bylo to **jednou explicitně zmínit** v zprávě uživateli proč („ignoruju, protože toto je lineární sekvence, ne paralelní agenda"), místo opakovaného „Reminder ignored" bez vysvětlení.

### Příště (sez. 25 → sez. 26)

**Fáze 3 — UI panel parametrů (`#terrainctrl`):**

- [ ] **HTML/CSS panel** v `index.html` — vlastní sekce `#terrainctrl` v pravém dolním rohu (pravý protějšek `#hud` v levém horním), `pointer-events: auto`. Slidery: relief 0..10 step 1 + popisek stupně („Rolling"), size sx/sz 3..30 step 1 (omezení MVP — bez instancing 30×30=900 cells × ~5 voxelů × 6 textur = ~27 000 textures, mez prohlížeče), 4 surface slidery (auto-normalize na součet 1.0). Seed input (number, default 42). „Generovat" button.
- [ ] **`regenerateScene()` funkce** v `main.js` — vymazat všechny terrain bloky a water planes ze scény (přes `scene.children` filter dle userData flag `terrain: true`), volat `generateTerrain` s novými parametry, spawnit. Bez F5 reload.
- [ ] **Live vs. on-click regenerate**: `%THINK` v sez. 26. Live na slider input je hezké, ale 100+ mesh create per drag step je drahé. Doporučení: on-click button + slider mění jen state, nebo debounce 300 ms.

**Otevřená rozhodnutí (kandidáti DD-33+):**

- [ ] `LIQUID` třída (DD-25 vrstva 4) pro vodní plane(y) — momentálně mimo OOP model.
- [ ] Autogenerated TRRAMPS na heightmap step grass(y) → grass(y+1).
- [ ] Klastrování spojitých water cells do bounding boxů (jeden plane na celé jezero) místo 1×1 per cell.

**Multi-instance discipline:** Pokud user dál provozuje paralelní instance, doporučuju adoptovat `git worktree add ../TheCubes-<topic> feat/<topic>` per instance (viz sez. 24 doporučení paralelní instance). Sez. 25 si vystačilo s „dirigent" pattern (tato = kód+commits, paralelní = jen meta+diary entry), ale tlak roste s další featurou.

**Audit cadence stav (po sez. 25):**
- `%AUDIT:CODE` — 7/8 sezení (blíží se prah, terrain MVP je velký nový kód + masivní wipe).
- `%AUDIT:DOCS` — 9/10 sezení (blíží se prah).
- IDEAS/TODO pruning — 6/12 (OK).
- `%CALIBRATE` — žádný práh, ale kandidát na zápis „Multi-instance discipline" do globálního CLAUDE.md při příštím spuštění.
