# 2026-05-14

## Sezení 41

### Diskuse

**(A) `%BEGIN`.** Repo synchronizováno na `04cf578` (sez. 40 DD-49+DD-50+kalibrace), server :8000 spuštěn nový (předchozí PIDy z včerejška uzavřeny). Cadence všechny sub-prah (`AUDIT:CODE` 2/8, `AUDIT:DOCS` 1/10, IDEAS/TODO pruning 9/12 — blízko prahu). Žádné Stale Příště. AI nabídla 4 směry z Příště sez. 40 → 41: (1) DD-50 follow-up LEAF_AUTUMN, (2) Fáze 6 DECOR (palm/cactus/stump), (3) Water bbox clustering, (4) HSL hue shift. User volba: **(1) LEAF_AUTUMN** — uzavřít sezonní visual loop, který DD-50 otevřel.

**(B) LEAF_AUTUMN paleta implementace (DD-50 follow-up).** Per-instance season propagace přes `DECOR.SEASON` atribut (drží model+engine separation, ne globální `world.SEASON` read v builderu). Změny ve 4 souborech: builders.js (`LEAF_AUTUMN` import + oak/bush priorita `snowed > autumn > default`), terrain.js (`decorSpecForClimate(lat, hum, season)` + `decorate(..., season, seed)` + `decoration.season` push), model.js (DECOR.SEASON konstruktor param), main.js (spawn loop + createDecor + buildScene). Smoke test: autumn se nezobrazil.

**(C) Bug regen path — Censure! (AI → AI).** User: „Listnaté stromy celoročně zelené, oranžovou na podzim nevidím." Diagnóza: `readParams()` v `index.html` volá `decorSpecForClimate(latKey, humKey)` **bez `seasonKey`** → `decorSpec.season` padá zpět na `"summer"` při každém regen z UI sliderů. Hlavní impl path (main.js `buildScene`) byl OK, ale to je jen boot — regen běží přes `readParams()`. Při edit API signature jsem updatoval jen 1/2 callers. Pattern „update signature = grep všech callers" zafailoval, memory `feedback_subagent_verify` v praxi (= ověřuj claimy gripem před hlášením done). Fix: 1 řádek v index.html.

**(D) Winter defoliation — user pointa.** User: „A co dělají listnaté stromy v zimě?" Reálný listnáč v zimě = holé větve, žádné listí. Sez. 40 user feedback („stromy udělej celé bílé až na kmen") byl Spruce-driven (smrk drží jehličí + sníh), AI to v sez. 41 slepě extrapolovala na oak/bush → SNOW_WHITE vata místo opadaných listů. Druhá logická chyba v jednom sezení. Doporučená oprava: koherentní seasonal cycle pro listnaté:
- spring/summer → LEAF_GREEN
- autumn → LEAF_AUTUMN
- winter (snowed) → holé větve (oak = jen kmen; bush = skip entity)
- spruce season-invariant zelený, snowed → SNOW_WHITE patra
User Q1 volba **A** (skip bush v decorate) — KISS, žádný empty mesh ve scéně.

**(E) Decor scale 0.333× + tree density × 2.** User: „Velikost dekorací kromě trávy zmenšit o 1/3. Stromy nyní hustší aby prales vypadal hustěji. Teď, když rozměry stromů poloviční, zdvojnásob počty stromů ve výpočtech." Změny:
- `DECOR_BASE_SCALE.spruce/oak/bush/rock`: `0.5` → `1/3` (= 0.333, o 1/3 menší).
- `DECOR_DENSITY`: všechny oak + spruce weights × 2 napříč 12 biomy.
AI flag tradeoff: `decorate()` má max 1 decor per cell, váhy > 1.0 znamenají jen 100 % fill rate, ne víc stromů per cell. Tropical.wet sum 0.80 → 1.15 — strop dosažen.

**(F) Ramp Y slope-aware (DD-52).** User: „Stromy na rampách (oak) visí ve vzduchu, přepočítej výšku paty stromu na svahu." Sez. 40 měl konstantní `y_top + 1.0` (= střed ramp diagonal); jitter pozice mimo střed = floating nebo zapuštěná. Implementace: `ramps[].slopeDir = { dx, dz }` (unit vector low→high) přiřazen v candidate creation (edge: `pick.dir.dx/dz`, corner: `c.ddx/ddz`, diagonal: `tdrampCorner.ddx/ddz`). `decorate()` rozšířen: `slopeT = (jitterX·dx + jitterZ·dz) / (|dx|+|dz|)`, `decY = y_top + 1.0 + slopeT`. Normalizace unifikuje edge (norm=1) + corner/diagonal (norm=2). Tradeoff: TDRAMP je diskontinuální (step shape, ne smooth slope) — bilinear formula je mild aproximace, sub-prah pro exact step model. User test: „Opraveno OK."

**(G) Multi-decor per cell (rolled back).** User: „Byl by problém vygenerovat na jednom tile více stromů? Prales je řídký." AI plán 2 attempts × všechny cells (Q1 A + Q2 A). User: „A2+A3" interpretováno jako schválení A+A. Implementace: `MAX_ATTEMPTS_PER_CELL = 2` smyčka v `decorate()`, per-attempt jitter + pick + seed. Tropical.wet 100×100 = ~20k decor entities.

**(H) Perf regrese.** User: „F12 rAF handler took 123-131ms" (= ~8 FPS, regrese ze sez. 31/40 FPS 104). Příčina: 20k DECOR Object3D × ~5 child meshes per Group = ~100k scenetree + shadow casting × 2 pass. 12× cost. **Censure! (AI → AI):** flagoval jsem perf risk v Q1, ale doporučil A přesto. Měl jsem nejdřív zkusit shadow opt-out (cheap) nebo přímo navrhnout InstancedMesh refactor (proper). User volba: **A** = rollback MAX_ATTEMPTS=1, FPS zpět na sez. 40 úroveň. Hustota wet biomu zachována přes density × 2 (sum > 1.0 = 100 % fill jediným decor per cell).

**(I) `%END` — generátor scény považuje user za dokončený.** Sezení uzavřeno. Následovat bude úklid (sub-prahy + InstancedMesh DECOR refactor = DD-kandidát triggered).

### Rozhodnutí

- **DD-51** — Seasonal foliage cycle (LEAF_AUTUMN paleta + winter defoliation oak/bush).
- **DD-52** — Slope-aware DECOR Y na rampách (`ramps[].slopeDir` + lineární Y interp v `decorate()`).
- **Decor base scale 0.5 → 1/3** (spruce/oak/bush/rock, grass_tuft invariant 1.0×). Kalibrace, ne DD.
- **DECOR_DENSITY oak/spruce × 2** napříč 12 biomy (rebalance pro hustší prales). Kalibrace, ne DD.
- **Multi-decor rolled back** — 2 attempts implementováno + reverted po perf regresi (8 FPS @ 100×100). Single attempt v kódu, sub-prah InstancedMesh DECOR refactor pro skutečně hustý prales.

### Kód

- `src/composites/builders.js` +~25 / −~10 ř. (LEAF_AUTUMN import, oak/bush seasonal cycle s priority snowed > autumn > default, oak.snowed → kmen-only, bush.snowed → early return prázdná Group).
- `src/composites/toolkit.js` 0 ř. (LEAF_AUTUMN už v paletě sez. 40 jako DD-50 kotva, jen aktivně používaná).
- `src/terrain.js` +~50 / −~15 ř. (`decorSpecForClimate(lat, hum, season)` 3-arg signature, `decorate(..., season, seed)` 6-arg signature, ramp candidates dostali `slopeDir`, ramps.push propaguje `slopeDir`, decorate Y interp z `rampDirMap`, DECOR_BASE_SCALE 0.5 → 1/3, DECOR_DENSITY oak/spruce × 2, bush snowed skip v `allowedEntries.filter`, season propagace do `decorations[]`).
- `src/model.js` +~10 / −~1 ř. (`DECOR.SEASON` 9. konstruktor param, default `"summer"`, docstring).
- `src/main.js` +~5 / −~3 ř. (`createDecor` passne `season: instance.SEASON`, spawn loop `new DECOR(..., d.season ?? "summer")`, `buildScene` passne `world.SEASON` do `decorSpecForClimate`).
- `index.html` +~5 / −~1 ř. (`readParams()` decorSpec přidá `seasonKey` 3. arg — fix regen path).

**Žádný src/ debug** — diagnóza autumn-nevidět + perf rAF přes console F12 user-side, žádný `console.log` v src/.

### Kudos / Censure

- **Kudos! (user → AI plan→Go pattern):** memory [[feedback_plan_then_go]] opět v praxi. „Go!", „A", „A2+A3" — 3 krátké iterativní cykly LEAF_AUTUMN → winter defoliation → multi-decor (rollback). Bez prodlužování diskuse.
- **Kudos! (user → AI design questions):** user pointy „A co listnaté v zimě?" + „prales je řídký" odhalily AI logické chyby (slepá extrapolace snowed = white pro listnaté) i scope gaps (multi-decor missing). 80/20 kritika v praxi z user strany.
- **Kudos! (AI → AI ramp Y slope-aware):** DD-52 implementace přes `slopeDir` na ramp entry je clean (= per-cell metadata, ne hardcoded direction lookup). User test „Opraveno OK".
- **Censure! (AI → AI, decorSpecForClimate caller sweep):** Při edit API signature `decorSpecForClimate(lat, hum)` → `(lat, hum, season)` jsem updatoval jen main.js `buildScene` (boot path), zapomněl `readParams()` v index.html (regen path). Pattern „update signature = grep všech callers" zafailoval. Memory [[feedback_subagent_verify]] v praxi — claim „all callers updated" měl ověřit gripem PŘED hlášením done.
- **Censure! (AI → AI, snowed listnaté = white vata):** Sez. 40 user feedback („stromy celé bílé") jsem aplikoval bez segregace na typ stromu — spruce (jehličnan, drží jehličí) OK, ale oak/bush (listnáče, opadávají) NE. User musel pointovat „A co dělají listnaté v zimě?". Per CLAUDE.md „ověřuj... reality check" — pro biome/seasonal logiku měla validovat botanickou correctness.
- **Censure! (AI → AI, multi-decor perf):** Q1 jsem flagoval perf risk ALE doporučil variantu A přesto. User shrunk na 20k decor = 8 FPS rollback. Měl jsem buď doporučit shadow opt-out (cheap perf win, hustota OK) nebo rovnou InstancedMesh refactor (proper). 1 sezení rollback overhead.

### Příště (sez. 41 → sez. 42)

**Volitelné směry (otevřeno):**

- [!] **InstancedMesh refactor pro DECOR** *(DD-kandidát, trigger byl perf regrese sez. 41)*. Per (KIND × varianta) `InstancedMesh` batch, ~20k Object3D → ~10-15 draw calls. Komplikace: per-instance random scale + rotation v `instanceMatrix`, flatShading + InstancedMesh seam (memory [[feedback_flat_shading_instanced]] — sez. 34 nález; switch na vertex-color pipeline pro DECOR). Otevírá cestu k MAX_ATTEMPTS=2-3 (hustý prales) bez perf hit.
- [ ] **Fáze 6 DECOR KIND extension** — `palm` (tropical.wet), `cactus` (subtropical/temperate.dry), `stump`/`log` (woodland clean-up), `flower` (temperate.wet louka), `_dead` postfix.
- [ ] **Water bbox clustering** — flood-fill connected components, 1 plane per jezero místo 1×1 per cell.
- [ ] **HSL hue shift sky/sun** — DD-48 follow-up, fyzikálně realističtější Rayleigh při dusk.
- [ ] **`%AUDIT:CODE`** — 3/8 (sub-prah, ale 2 sezení v řadě jen impl bez auditu, blíží se k 8).

**Cadence (po sez. 41):**
- `%AUDIT:CODE` — 3/8 (OK, ale roste).
- `%AUDIT:DOCS` — 2/10 (OK).
- IDEAS/TODO pruning — 10/12 (sub-prah, ale **velmi blízko prahu** — další sezení vyhodnotit jako pruning trigger).

**Multi-instance:** Sez. 41 single-instance. Multi-feature (DD-51 + DD-52 + 2 kalibrace), bez topic branche — single commit na main.

## Sezení 42

### Diskuse

**(A) `%BEGIN`.** Repo na `06852e2` (sez. 41 EOF), server :8000 stále běžel. Cadence AUDIT:CODE 3/8, AUDIT:DOCS 2/10, IDEAS/TODO pruning 10/12 (velmi blízko prahu). První bod programu (z Příště sez. 41): **[!] InstancedMesh refactor pro DECOR** = perf trigger po multi-decor rollbacku. User volba **Go!** na `%THINK`.

**(B) `%THINK` InstancedMesh refactor.** Analýza 3 přístupů (full / hybridní / diagnóza shadow path). Klíčové překážky: per-decor variable child count (spruce 3-5 tier, oak 2-4 cluster, …), flatShading + InstancedMesh seam ([[feedback_flat_shading_instanced]]), snowed/autumn batch routing. User Q1=A **diagnóza shadow path** (= test, je shadow viník), Q2=test only, Q3=fixed-count parts pro hypotetický budoucí refactor.

**(C) Diagnostic harness.** `terrain.js`: re-intro `MAX_ATTEMPTS=2` smyčka v `decorate()` (= stress recreate sez. 41 rollback). `main.js`: `window.settings.decorShadows = true` toggle + `wantsShadow` v `createMeshFor` traverse (DECOR-only opt-out). `index.html`: `window.regen()` shortcut pro F12. User test 100×100 tropical+wet: **(B) shadows ON = 5 FPS, (C) shadows OFF = 9 FPS**. Shadows ~44% cost, ale absolutní 9 FPS = unplayable. Diagnóza: **mixed case** — shadow significant, ne sole viník.

**(D) Perf HUD legacy field discovery.** User v reportu měření zaregistroval `calls: 1, tri: 1, mat: 0`. AI vysvětlení: `composer.render()` přepíše `renderer.info.render` za poslední pass (= OutputPass quad) = bezvýznamné. `_faceMaterialCache` po DD-41 prázdná = `mat: 0` permanent. Sub-prah **Krok 3 perf HUD fix** narozen jako vedlejší úkol.

**(E) Cleanup diagnostic + retest baseline.** Q1=A (odložit InstancedMesh refactor — sez. 41 user verdikt "generátor dokončený"), Q2=A (cleanup hned). Diagnostic harness revertován. User: "Hlavně bych nevykresloval celou scénu, ale jen zobrazenou a zaostřenou část… Původní hodnoty FPS před dekorací byla 150 nyní 9."

**(F) Censure! (AI → AI) — 80/20 oponuji k user návrhu.** Three.js frustum culling už default ON pro main pass. DOF/Fog post-process **nezahodí mesh z render path** (jen vizuální blur/tint). Klíčový skutečný viník = **shadow pass** (separate camera, nerespektuje main camera frustum). 9 FPS bylo multi-attempt stress, ne current single-attempt baseline. Retest @ 100×100 tropical+wet single-attempt = 33 FPS (mid-range playable, ne crisis).

**(G) Q1=A sekvenční perf opts plán (Krok 1+2+3, měřit po každém), Q2=D configurable shadowRange, Q3=A akceptovat popping.**

**(H) Krok 1 — shadow camera tighten kolem `controls.target`.** ~5 změn v `main.js`: explicit `sun.target = new Object3D() + scene.add()`, per-frame `sun.target.position.copy(controls.target)` + sun.position relative, `window.settings.shadowRange = 24` + `setShadowRange()`, `updateShadowFrustum()` no-arg, `spawnTerrain` drop velikost-derived call. User test: default ±24 = **15 FPS (regression!)**, ±16 = 21 FPS. Counterintuitive paradox: tighter frustum + same mapSize = denser texels per ground unit = **HIGHER per-mesh shadow cost** (pixels per mesh roste rychleji než kolik mesh vypadne z frustum). Krok 1 alone = quality upgrade, ne perf opt. **Revert.**

**(I) Krok 2 — shadow mapSize 2048 → 1024.** User test: 36 FPS, "Pixely stínů mírně za hranou, ale akceptovatelné." +3 FPS skromné, ale win. Pokračovat ke Kroku 3.

**(J) Krok 3 — perf HUD fix.** `renderer.info.autoReset = false` v setup, manuální `info.reset()` per frame na začátku `animate()`, drop `mat:` field z UI (`index.html` + `_perfHud.el`). User test: **FPS 11 (regrese vs. Krok 2 36)**, calls 58k, tri 3.4M. Suspected per-frame reset() overhead nebo F12 open. Fix: přesun reset z per-frame do HUD report block (1×/s) + avg per-frame stats. User korekce: "Možná jsem neměl u té 36 vlhkost na max. → mnohem méně stromů. Měření 36 bych vyřadil." **Krok 2 měření invalid (jiná density), vyřazeno.**

**(K) Reality check.** Retest 100×100 tropical+wet @ wet max: **FPS 9, calls 62k, tri 3.5M**. Reálný baseline @ wet max = **9 FPS, ne 33** (= sez. 41 user "60-100 FPS" byl @ temperate.mid nebo lower density). Krok 2 mapSize reduce = **no measurable improvement** = shadow fragment NENÍ dominant cost. **CPU draw call submission (62k per frame × ~16 μs = ~1 sek CPU per frame) je viník.** Per-mesh overhead × ~10k decor scenetree.

**(L) User volba directional po reality check.** Q2=A (stress test only, typical use case je menší / nižší density), Q1=A (accept tropical.wet 100×100 max stress jako edge case). Sez. 42 jde **content směr**. Q3=A revert Krok 2 (mapSize zpět 2048), Krok 3 zachovat (validní stats). Q4=B **water bbox clustering** jako content direction.

**(M) `%THINK` water bbox clustering (DD-53 plán).** Connected components BFS přes water cells, per component: bbox + mean Y + majority frozen. A1=A naïve bbox (KISS), A2=A majority frozen (loss per-cell ice/water noise), A3=A mean Y (slight drift). Sub-prahy: non-convex artifact, per-bbox ice texture noise.

**(N) DD-53 implementace.** ~80 ř. v `terrain.js` Krok 6.5 (BFS visited Set, queue, bbox + level + frozen agregace). `createWaterPlane` v main.js mělo skrytý bug: PlaneGeometry(1,1) vertices na XY plane (Z=0), `mesh.scale` aplikuje PŘED `mesh.rotation`, takže scale.y kontroluje POST-rotation Z dim (depth), scale.z je ignorován. Pre-DD-53 (w=d=1) bug invisible, post-DD-53 s bbox w/d > 1 visible (jezera jen 1 j hluboká). Fix: `scale.set(w.w, w.d, 1)` místo `(w.w, 1, w.d)`. Three.js gotcha.

**(O) DD-53 test + visual bug.** User test polar+mid 100×100: FPS 8, water.length=92 (5× reduction z ~500, ale ne 5-20 expected), decorations.length=7236 (4.5× nad polar.mid sum 0.16 expected — pravděpodobně climate slider na wet, ne mid). User: "Vodní hladina dost problikává a není správně vykreslená např. tady -21,1,-3." Visual artifact = bbox over varied water_y → mean Y mezi → boundary cells z-fight.

**(P) Pivot — kritický insight.** User: **"100x100 je spíš stresstest. Hra bude probíhat na drobných cca 20x20 HEZKY VYKRESLENÝCH dioramatech, ať zobrazení zbytečně neořežeme, kvůli nepoužívaným nastavením!"** **Censure! (AI → AI):** AI po celý perf diagnostic dive pracovala na 100×100 max stress jako target. Nezeptala se na real target use case. User explicitně musel intervene.

**(Q) 20×20 real-use test = 60 FPS + visual OK.** Real workflow je playable out of the box bez perf opts. Všechny optimizations pro 100×100 stress jsou irrelevant (a v případě Kroku 1+DD-53 kontraproduktivní).

**(R) DD-53 revert.** Q2=A. Pro 20×20 trivial cost (5-20 water cells × per-cell plane), bbox cluster nepřináší benefit + introducuje visual artifact. Per-cell render always-correct. DD-25 vrstva 4 LIQUID class je proper natural place pro per-lake entity (= sub-prah, ne sez. 42). Revert terrain.js + createWaterPlane scale + drop `window.lastTerrain` diagnostic.

**(S) `%END`.** Q3=A. Sez. 42 byl perf diagnostic dive + pivot k target use case understanding. **Krok 3 perf HUD fix je jediný persistent kódový change.** DD-53 attempt + revert je learning record, ne shipped feature.

### Rozhodnutí

- **Krok 3 perf HUD fix** — `renderer.info.autoReset = false` + per-second manual reset + avg per-frame `calls`/`tri` stats + drop legacy `mat:` field (post-DD-41 `_faceMaterialCache` prázdná, `composer.render()` přepisování `renderer.info` debunked). **Není DD** (= sub-clean, ne immutable koncept).
- **DD-53 attempt + revert** — water bbox clustering BFS implementován, post-pivot revertován per user instruction "ať zobrazení neořežeme". DD-25 vrstva 4 LIQUID class je proper place pro per-lake entity v budoucnu.
- **Target use case = 20×20 dioráma** (= reálný workflow). 100×100 = academic stress, **NE priority pro perf opts**. Per user: "ať zobrazení zbytečně neořežeme, kvůli nepoužívaným nastavením".
- **Krok 1 shadow tighten paradox dokumentován** — tighter frustum + same mapSize = denser texels per ground unit = HIGHER per-mesh shadow rasterization cost. Budoucí shadow tighten MUSÍ být kombinován s mapSize reduction, jinak counterintuitive regression. Memory: [[feedback_shadow_tighten_paradox]].
- **InstancedMesh refactor DECOR** — odložen (A1=A). Per 20×20 target není potřeba. TODO downgrade `[!]` → `[ ]`.
- **Diagnostic harness pattern validován** — A2=test only (implementuj, měř, revertuj) byl správně použit. Krok 1 regression detected and reverted clean.
- **Cadence increment**: AUDIT:CODE 4/8, AUDIT:DOCS 3/10, IDEAS/TODO pruning **11/12** (= last call, sez. 43 MUSÍ pruning).

### Kód

- `src/main.js` +12 / −9 ř. (Krok 3 perf HUD fix only): `renderer.info.autoReset = false` v setup (po `setPixelRatio`), drop `mat: document.getElementById("ph-mat")` z `_perfHud.el`, drop `_faceMaterialCache.size` setter, přidat `callsAvg`/`triAvg = total / frameCount` avg per-frame stats, přesun `renderer.info.reset()` do HUD report block (1×/s).
- `index.html` −1 ř. (Krok 3): drop `<div><span class="label">mat:</span><span class="val" id="ph-mat">—</span></div>` element.

**DD-53 path (implementováno + reverted, net 0):**
- `src/terrain.js` Krok 6.5 BFS bbox cluster ~80 ř. (added + reverted).
- `src/main.js` `createWaterPlane` scale fix (Three.js gotcha) ~4 ř. (added + reverted).
- `src/main.js` `window.lastTerrain` diagnostic exposure ~3 ř. (added + reverted).

**Diagnostic harness path (added + reverted, net 0):**
- `src/terrain.js` `MAX_ATTEMPTS=2` smyčka in `decorate()` ~20 ř.
- `src/main.js` `window.settings.decorShadows` + `wantsShadow` v `createMeshFor` ~10 ř.
- `index.html` `window.regen` shortcut 1 ř.

**Šlépěje (testing prototypes, all reverted):**
- `src/main.js` Krok 1 (sun.target tracking + dynamic shadow frustum + shadowRange settings) ~30 ř. — revertováno.
- `src/main.js` Krok 2 (mapSize 1024) 1 ř. — revertováno.

**Žádný `src/` debug** — `console.warn` v `createDecor` (pre-existing, sez. 40) zachován; žádné nové debug výpisy.

### Kudos / Censure

- **Kudos! (user → AI quick pivot recognition):** User explicit instructed "20x20 je real, neořezávat, ať zobrazení zbytečně neořežeme" — saved AI z over-engineering perf opts které kompromitují visual pro stress case. Critical pivot, saved sezení z dalšího hodinového rabbit hole.
- **Kudos! (user → AI measurement rigor):** User flagged "Krok 2 36 FPS = lower humidity = vyřaď" — caught invalid baseline comparison. Saved bogus interpretation že mapSize reduce = win.
- **Kudos! (AI → AI shadow tighten paradox detection):** Counterintuitive nález (tighter frustum + 2048 mapSize = více cost per mesh kvůli denser texels) je non-obvious. Explained v real time před commitem. Memory písemka pro budoucí shadow opts.
- **Kudos! (AI → AI Three.js gotcha catch):** `createWaterPlane` scale.set order bug (PlaneGeometry XY plane + mesh rotation) byl invisibly hidden pre-DD-53. Catch on first user "visual broken" report, fix v 1 řádku.
- **Censure! (AI → AI target use case assumption):** AI po celý perf diagnostic dive pracovala na 100×100 max stress jako implicit target. **Nezeptala se** na realný workflow scale. User musel explicitně intervene "20x20 dioráma je real" po několika měřeních. Default pro budoucí: **PŘED jakýmkoli perf benchmark zeptat se "co user opravdu chce optimalizovat / hrát?"**. PROMPTS.md projektový `%BEGIN` rozšířit?
- **Censure! (AI → AI Krok 2 win interpretation):** "Krok 2 = +3 FPS" prezentováno jako win bez kontrolního baseline (same density measurement). User musel pointovat density rozdíl. Default: u perf comparison kontrolovat **all** variables, ne jen tu, kterou jsi měnil. Sez. 41 [[feedback_subagent_verify]] aplikováno na vlastní measurement, ne podagent.
- **Censure! (AI → AI Krok 1 shadow tighten plán):** Plán "shadow tighten = perf win" byl naivní. Texel density / mapSize interaction nebyla zvážena PŘED implementací. User reportoval regression, AI vysvětlila paradox post-hoc. Default: **pro perf opts dělat napřed math estimate** — pokud O(N) terms se mění nečekaným způsobem, nezačínat coding.

### Příště (sez. 42 → sez. 43)

**Cadence (po sez. 42):**
- `%AUDIT:CODE` — **4/8** (sub-prah, ale 3 sezení po sobě jen impl/diagnose bez auditu).
- `%AUDIT:DOCS` — **3/10** (OK).
- **IDEAS/TODO pruning — 11/12** (= **MUSÍ pruning v sez. 43**, threshold trigger).

**Volitelné směry (otevřeno, žádný explicit user pick):**

- [ ] **IDEAS/TODO pruning** *(cadence trigger, mandatory v sez. 43)* — projet IDEAS.md + TODO.md, close stale entries (DD-49+DD-50+DD-51+DD-52 follow-up sub-prahy dozraté?), reorganize sections, případně přesun zralých na TODO ze IDEAS.
- [ ] **Fáze 6 DECOR KIND extension** — palm (tropical.wet), cactus (subtropical/temperate.dry), stump/log, flower (temperate.wet louka), `_dead` postfix.
- [ ] **HSL hue shift sky/sun** — DD-48 follow-up, fyzikálně realističtější Rayleigh při dusk.
- [ ] **`%AUDIT:CODE`** — 4/8, blízí se k 8.
- [ ] **`%BEGIN` projekt-specific addendum** — zeptat se na **target use case scale** (velikost grid, climate priority) PŘED tím, než %BEGIN nabídne perf směr. Per sez. 42 Censure.

**Multi-instance:** Sez. 42 single-instance. Perf diagnostic dive + pivot meta-insight. Krok 3 perf HUD fix only persistent. DD-53 implementováno + reverted (learning record, ne shipped). Bez topic branche — single commit na main.

## Sezení 43

### Diskuse

**(A) `%BEGIN`.** Repo na `bc6517f` (sez. 42 EOF), server :8000 stále běžel (PID 33584). Cadence AUDIT:CODE 4/8, AUDIT:DOCS 3/10, **IDEAS/TODO pruning 11/12 = threshold trigger, MUSÍ pruning toto sezení**. Žádné stale Příště. User volba: **pruning** (mandatory cadence), potom volný směr.

**(B) IDEAS/TODO pruning — 4 passes.** AI analýza TODO.md (128 ř, 14 sub-sekcí, 49 open + 12 `[x]` ledger) + IDEAS.md (3 stale `→ TODO` markery). Plán Q1=A full 4 passes / Q2=A kompaktní hlavička / Q3=A Speculative sekce (konzervativní, žádný hard delete). User „A1A A2A A3A" → exekuce.

Pass 1 IDEAS markery refresh: `SEASON → DONE (DD-50)`, `CLIMATE → DONE (DD-42)`, "Voda" driver komentář update DD-47 → DD-50 SEASON driver, WCUBES/INVISIBLE/CCUBES typizace sloučeno pod "parkováno".

Pass 2 ledger drop z TODO do hlavičky: 12 `[x]` řádků odstraněny, nahrazeno kompaktním "Recent DONE (sez. 34-43)" hlavičkou.

Pass 3 Speculative konsolidace: 14 položek z 6 starých "Sub-prah (DD-XX follow-up)" sekcí sloučeno do jedné `## Speculative / wait-for-signal` sekce.

Pass 4 section restructure 14 → 6: Otevřené M8+ / Terrain aktivní / Krajinné DECOR (Fáze 6 + LIQUID) / WORLD-atmosféra / Sez. 42 sub-prahy / Speculative + Audit cadence.

Side effect 3 merge duplicit: `LIQUID třída` + `DD-25 LIQUID class` sloučeno (oba na stejný koncept), `Klastrování spojitých water cells` + `Water cluster connected components` sloučeno, `DD-53 follow-up sub-prah` folded do `Klastrování`.

**Net:** TODO.md 128 → 84 ř (−34 %, 49 → 46 open items), IDEAS.md 125 → 123 ř (surgical).

**(C) Volný směr po pruningu.** User „Fáze 6 DECOR KIND extension (palm/cactus/flower/stump/log/_dead)" — wide content fokus, 6 položek.

**(D) `%THINK` Fáze 6.** AI analýza: 6 položek → 3 skupiny (A=palm+cactus hot biomy, B=flower+stump+log temperate detail, C=`_dead` postfix + Density UI meta). User Q1=A skupina A only, Q2=A palm mix s oak v tropical, Q3=A+B kombo cactus jen subtropical.dry.

**(E) Skupina A implementace.** 3 soubory:
- `toolkit.js`: `CACTUS_GREEN = 0x4a7a4a` sage-green saguaro paleta entry + dev exposure.
- `builders.js`: `Y_AXIS` const (sdílený unit-vector pro `setFromUnitVectors`) + `buildPalm` (~55 ř, tenký vysoký kmen + 5-7 radiálních listů s `quaternion.setFromUnitVectors`) + `buildCactus` (~55 ř, sloupec + 0-2 boční paže pod úhlem, `dead` flag → BARK_BROWN trunk-only) + DECOR_BUILDERS registry 5 → 7.
- `terrain.js`: `DECOR_DENSITY` tropical.wet (palm 0.40 primary, oak 0.30 fallback) + tropical.mid (palm 0.05 savana) + subtropical.dry (cactus 0.04 primary) + DECOR_BASE_SCALE palm/cactus 1/3.

User test: „Vypadá to dobře, jen některé palmy na rampách visí ve vzduchu. Ukotvit!"

**(F) TTRAMPS skip fix.** AI math analýza ramp surface coverage:
- TRRAMPS (edge wedge): SLOPE quad covers full cell top, bilinear formula EXACT (žádný floating).
- TDRAMP (diagonal 1C minus tetrahedron): slope+top, formula SUNKEN 0.2-0.5 j (palm trunk procházela slope, méně viditelné).
- TTRAMPS (trojboký jehlan = corner ramp): slope JEN v projected NW triangle ~1/4 cellu, zbytek empty above bottom face. Formula by dala FLOATING 0.2-0.5 j nad povrchem.

KISS fix v `decorate()`: `rampDirMap` → `rampInfoMap` (uloží celý ramp record vč. `kind`), skip cell pokud `rampInfo.kind === "corner"`. TTRAMPS jsou minorita ramp typů, loss density malá. Side effect: fix platí pro VŠECHEN decor (oak/spruce/bush/rock/grass_tuft/cactus/palm), ne jen palmu. TDRAMP sunken zůstává sub-prah „TDRAMP exact step Y" v TODO Speculative.

User test: „Test OK".

**(G) Skupina B+C batch.** User „B+C potom %END". Velký batch ~280 ř změn napříč 6 souborů:
- `toolkit.js`: `FLOWER_PETAL_RED/YELLOW/WHITE` paleta entries.
- `builders.js`: `dead` flag branch v `buildSpruce` (trunk-only skip tiers) / `buildOak` (skip clusters, kombinovaný s snowed) / `buildBush` (skip entire) + `buildFlower` (~50 ř, stem + per-instance hue picked bloom + slight tilt) + `buildStump` (~30 ř, krátký Cylinder + snow disc) + `buildLog` (~25 ř, horizontal Cylinder 90° kolem X) + DECOR_BUILDERS registry 7 → 10.
- `terrain.js`: DECOR_DENSITY add flower/stump/log do temperate.*/subtropical.wet + DECOR_BASE_SCALE 7 → 10 + `DEAD_PROB_BY_HUM = { wet: 0, mid: 0.05, dry: 0.30 }` + `decorate()` densityMult param + dead RNG roll + `decoration.dead` push + `decorSpecForClimate` 4-arg signature + generateTerrain decorSpec.densityMult propagation.
- `model.js`: `DECOR.DEAD` boolean attribut + docstring + `WORLD.DECOR_DENSITY_MULT = 1.0` atribut + docstring.
- `main.js`: `createDecor` passes `dead`, spawn loop creates DECOR(.., dead), `buildScene` passes `world.DECOR_DENSITY_MULT` via decorSpec.
- `index.html`: tc-decor-density slider v Climate sekce + DOM ref + readParams `densityMult` read + handler (input readout + WORLD mutate, change regen).

**(H) User test po B+C.** „Test OK, stromy nelétají, květiny zmenšit na polovinu." Flower kalibrace: `DECOR_BASE_SCALE.flower` 1.0 → 0.5 (= ~0.13-0.19 j final, sedmikrásky nad voxely místo tulipánů).

**(I) `%END`.** User „Go!" Plán: docs (TODO close + DONE.md sez. 43 + DIARY index + diary/2026-05-14.md Sezení 43 + IDEAS markery) + cleanup (žádné debug logy introduced) + git commit + push + memory (TTRAMPS half-coverage rule).

### Rozhodnutí

- **Žádný nový DD.** Fáze 6 = implementace pre-approved TODO. Builder patterns (`opts.dead` izomorfní s `opts.snowed`, density slider izomorfní s Climate slidery) jdou ve stávajících designových liniích DD-49/DD-50.
- **`DEAD_PROB_BY_HUM`** driver: wet 0% / mid 5% / dry 30%. Drought stress visualizace.
- **`_dead` builder priorita** `dead > snowed > autumn > default`. spruce/oak/palm = trunk-only, bush/flower = skip, cactus = trunk-only BARK_BROWN, stump/log/rock/grass_tuft = no-op.
- **TTRAMPS skip pro decor** — corner ramp half-coverage = decor floating, KISS resolution skipnout cell. TDRAMP sunken zůstává sub-prah (sunken < floating viditelnost).
- **Flower scale kalibrace** 1.0 → 0.5 (user wish, „kvítky" ne „tulipány").
- **Skupiny B+C v jednom batch** — user explicitně „B+C potom %END", AI execute bez intermediate stops. Validated workflow.

### Kód

- `src/composites/toolkit.js` +~9 / −~1 ř.
- `src/composites/builders.js` +~200 / −~15 ř.
- `src/terrain.js` +~40 / −~10 ř.
- `src/model.js` +~10 / −~2 ř.
- `src/main.js` +~3 / −~3 ř.
- `index.html` +~15 / −~2 ř.
- `docs/IDEAS.md` −2 ř. (Pass 1 markery refresh).
- `docs/TODO.md` ~−45 ř. (Pass 2+3+4 restructure 14 → 6 sekcí, 49 → 46 open items, Fáze 6 close 6/9).
- `docs/DIARY.md` +1 ř. index.
- `docs/diary/2026-05-14.md` +~120 ř.
- `docs/DONE.md` +~140 ř.

**Žádný `src/` debug** — pre-existing `console.warn` v `createDecor` / `terrain.js` (sez. 40+38) zachovány, žádné nové debug výpisy.

### Kudos / Censure

- **Kudos! (user → AI design questions):** „Ukotvit!" minimální feedback po Skupina A. AI shrunk na math analýzu TTRAMPS jehlanu (projected NW triangle ~1/4 cellu) z first principles, identifikoval root cause před implementací fixu. „Ukotvit" je 1 slovo, AI nepotřebovala víc.
- **Kudos! (user → AI scope discipline):** User „B+C potom %END" — explicitní scope statement = AI execute batch bez intermediate „mohu pokračovat?" check. Plán-then-Go pattern v praxi (memory [[feedback_plan_then_go]]).
- **Kudos! (user → AI flower kalibrace):** „květiny zmenšit na polovinu" — koncízní user feedback, AI hned tweak 1 hex (DECOR_BASE_SCALE.flower 1.0 → 0.5), bez extra otázek.
- **Kudos! (AI → AI TTRAMPS math identifikace):** Před implementací fixu AI prošla matematicky všechny 3 ramp typy (TRRAMPS exact, TDRAMP sunken −0.2 až −0.5 j, TTRAMPS floating +0.2 až +0.5 j). KISS resolution skip TTRAMPS (1 řádek) místo komplikovaného per-type formula. Side effect: fix benefituje všechen decor.
- **Kudos! (AI → AI Skupina A izomorfní design):** `palm`/`cactus` buildery reusovali `Y_AXIS` const + `quaternion.setFromUnitVectors` pattern napříč 2 buildery (palm fronds, cactus arms). DRY, Three.js idiom místo Object3D wrapper hierarchy.
- **Censure! (AI → AI Skupina A snowed defensive):** Palm/cactus mají snowed branch (`SNOW_WHITE` recolor, `recolorTopChild`), ale snowSpec pro tropical/subtropical biomy je `mode: "none"` — snowed nikdy nedosáhne true v aktuálních biome density tabulkách. Defenzivní kód, ale prakticky dead code path. OK pro konzistenci s ostatními buildery, ale stojí to ~5 ř každý. KISS judgment call: nechat.
- **Censure! (AI → AI dead state visual differentiation):** `oak` `dead` flag = trunk-only stejně jako `snowed` (= holý kmen). Visually identicky, nutí user rozlišit přes context (zima vs. sucho). Sub-prah `BARK_DEAD` darker palette v TODO Speculative — měl bych spíš implementovat hned v batch (= 5 ř toolkit.js + 2 ř per builder), místo sub-prah. KISS judgement v batch, ale „when context distinguishes, no need to differentiate visually" je weakish.

### Příště (sez. 43 → sez. 44)

**Cadence (po sez. 43):**
- `%AUDIT:CODE` — **5/8** (sub-prah, ale 4 sezení po sobě jen impl/diagnose, blíží se).
- `%AUDIT:DOCS` — **4/10** (OK).
- IDEAS/TODO pruning — **0/12** (full pruning DONE). Next: ~sez. 55.

**Volitelné směry (otevřeno):**

- [ ] **Zbývající Fáze 6** — LEAF_AUTUMN HSL variace per instance, DECOR_DENSITY sezonní modifier (autumn -20 %, winter -50 %), receive shadow opt-out flag. 3 z 9 Fáze 6 položek zbývajících.
- [ ] **HSL hue shift sky/sun** (DD-48 follow-up) — fyzikálně realističtější Rayleigh při dusk.
- [ ] **WORLD/atmosféra zbytek** — sky/sun season tint, snow accumulation animace, polar season variace, snow patches calibration, ice canvas texture.
- [ ] **`%AUDIT:CODE`** — 5/8, blízí se k 8 (next: ~sez. 46).
- [ ] **`%BEGIN` projektový addendum target use case check** (sez. 42 Censure carryover).

**Multi-instance:** Sez. 43 single-instance. Pruning + Fáze 6 5 KINDs + 1 meta + 1 sub-fix. Bez topic branche — single commit na main (consistent feature batch).

---

## Sezení 44

### Diskuse

**(A) `%BEGIN`.** Git sync clean, main up-to-date. Server již běžel (PID 33584 z předchozího sezení = 8000 LISTENING bez spuštění). Načteno: TODO, DIARY tail, PROMPTS.md. Audit cadence: AUDIT:CODE 5/8 (sub-prah blíží se), AUDIT:DOCS 4/10, pruning 0/12 po sez. 43 full pass. Příště ze sez. 43: (1) zbývající Fáze 6 3/9 položek, (2) HSL hue shift sky/sun, (3) WORLD/atmosféra zbytek, (4) `%AUDIT:CODE`, (5) `%BEGIN` target use case addendum.

AI návrh: pokračovat Fází 6 close (3 položky = ~50 ř, jeden commit), alternativy HSL sky/sun nebo meta `%BEGIN` addendum. User: „Fáze 6 batch, všechny tři" (= plán-then-Go pattern, žádné intermediate Q).

**(B) Pořadí + design pre-checks.**
- **A. Receive shadow opt-out.** Existující pattern `userData.noShadow` v `main.js` traverze (ř. 1218-1228, skip oba castShadow/receiveShadow). Pro `noReceiveShadow` paralelní opt-out chceme jen `receiveShadow = false` (cast zachovat — strom má vrhat stín). Aplikační místo: `createDecor()` traverze post-builder. KISS = jeden flag na všech mesh children, ne per-builder rozhodování (builder shadow concept neznámí, decor = celé „nehrá receivu").
- **B. LEAF_AUTUMN paleta.** Současně single hex `0xc8722a` v `buildOak`/`buildBush` autumn větvi. Decision per strom vs. per cluster: realistický les má **per druh** (oak vs. lípa vs. javor) — 1 strom monochrom, sousedi různí. **Per strom** vyhrál (DRY: 1 RNG roll před cluster loop, idiom paralelní s `FLOWER_COLORS` pick). Hex hodnoty: ORANGE 0xc8722a (zachováno, base), YELLOW 0xd8a830 (hue ~45°), RED 0xb84020 (~10°), BROWN 0x8a5028 (desaturated ~30°).
- **C. Sezonní density modifier.** Spec z TODO: autumn −20 % leaves, winter −50 % leaves + 20 % rock visibility. „Leaves" = oak/bush/flower (listnaté + květinové, sezonní); spruce jehličnan invariant; palm/cactus/grass_tuft/stump/log season-invariant. Implementace: `seasonalDensityMult(kind, season)` helper + clone `baseWeights → seasonalWeights` v `decorate()` před filter.

**(C) Implementace.** 4 soubory:
- `src/main.js` — traverze ř. 1224 přidán `child.receiveShadow = !child.userData.noReceiveShadow` (cast unchanged). `createDecor` po builder dispatch: `group.traverse((child) => { if (child.isMesh) child.userData.noReceiveShadow = true; })`. Flag se aplikuje BEFORE globální traverze v `createMeshFor` (volaného z `spawn`).
- `src/composites/toolkit.js` — 3 nové paleta entries (`LEAF_AUTUMN_YELLOW/RED/BROWN`) + `AUTUMN_PALETTE` array (orig ORANGE jako index 0, modální barva). Dev exposure rozšířen.
- `src/composites/builders.js` — `buildOak`/`buildBush` autumn větev: `AUTUMN_PALETTE[randInt(rng, 0, AUTUMN_PALETTE.length - 1)]` místo `LEAF_AUTUMN`. RNG roll PŘED cluster loop = jedna barva celé koruny. Palm zachoval single hex (botanicky reálná palma listy neopadává, sub-prah „tropical season cycle invariant" v komentáři).
- `src/terrain.js` — `seasonalDensityMult(kind, season)` helper. `decorate()` clone `baseWeights → seasonalWeights = Object.fromEntries(Object.entries(baseWeights).map(([k, w]) => [k, w × mult]))`. `allowedEntries` filter na `seasonalWeights` (rename, ne na `baseWeights`).

**(D) User test po batch.** „Krása! Kudos! %END." Visual verify OK out of the box bez kalibrace.

**(E) `%END`.** Docs update: TODO close Fáze 6 → 9/9 (3 položky drop, summary rename Sez. 43+44 DONE, cadence ticky AUDIT:CODE 5→6 / AUDIT:DOCS 4→5 / pruning 0→1), DIARY index append k sez. 41-43 řádku (multi-session den), diary/2026-05-14.md Sezení 44 sekce, DONE.md entry sez. 44, memory project_model_hierarchy.md description sync.

### Rozhodnutí

- **Žádný nový DD.** Fáze 6 close = implementace pre-approved TODO. AUTUMN_PALETTE rozšíření DD-51 (LEAF_AUTUMN single hex → 4-color array, izomorfně s FLOWER_PETAL_RED/YELLOW/WHITE z sez. 43). `seasonalDensityMult` = lookup-table modifier v rámci DD-49 `DECOR_DENSITY` scope. `noReceiveShadow` opt-out paralelní `noShadow` (sez. 33 LAMP pattern). Všechny tři položky v existujících DD linkách.
- **Per-strom autumn hue (ne per cluster).** Realistický „1 strom = 1 druh listí". Per cluster by dal pestrý vánoční stromek (nereálné). DRY idiom s `FLOWER_COLORS` v `buildFlower`.
- **Palm season-invariant.** Botanicky reálná palma listy neopadává; LEAF_AUTUMN v palm = dummy konzistence s globálním season UI. AUTUMN_PALETTE pick by zavedl ne-tropickou variabilitu do tropického biome — odmítnuto.
- **`createDecor` traverze pro `noReceiveShadow`.** Single source of truth (jeden flag v Object3D userData, builder shadow concept nezná). KISS, alternativa per-builder by zaváděla 10× duplicitní kód.
- **„Leaves" = oak/bush/flower v `seasonalDensityMult`.** Spruce vyloučen (jehličnan), tropical (palm) + dry (cactus/stump/log) + grass_tuft season-invariant. „Rock visibility" v winter = ×1.2 multiplier (= weight inkrement, ne absolute weight).

### Kód

- `src/main.js` +~13 / −~3 ř. (`noReceiveShadow` traverse respect + `createDecor` post-build traverze).
- `src/composites/toolkit.js` +~16 / −~2 ř. (AUTUMN_PALETTE 4 hex + array + komentáře + dev exposure).
- `src/composites/builders.js` +~12 / −~4 ř. (AUTUMN_PALETTE import + 2× clusterColor `randInt` pick).
- `src/terrain.js` +~28 / −~1 ř. (`seasonalDensityMult` helper + `seasonalWeights` clone + rename allowedEntries source).
- `docs/TODO.md` ~−5 ř. (close 3 položky + summary update + cadence ticky).
- `docs/DIARY.md` +1 řádek append (sez. 44 do sez. 41-43 multi-line).
- `docs/diary/2026-05-14.md` +tato sekce.
- `docs/DONE.md` +sez. 44 entry.

Code total ~79 ř insert / 10 ř delete přes 4 src soubory. Žádný nový debug `console.log`. Žádný rename = žádný grep cleanup po starých identifikátorech (drobné pole — `LEAF_AUTUMN` zachováno jako index 0 paletky).

### Kudos / Censure

- **Kudos! (user → AI plán A/B/C):** „Fáze 6 batch, všechny tři" — koncízní plán-then-Go. AI rozsah Q1/Q2 v plánu adresovala sama (per strom vs. per cluster pro autumn paletu, „leaves" definice pro season modifier). Žádné intermediate stops, batch implementace 4 soubory + commit.
- **Kudos! (user → AI test feedback):** „Krása! Kudos! %END" — minimální feedback, žádné kalibrace třeba. Implementace out-of-the-box visual OK (na rozdíl od sez. 43 flower scale 1.0 → 0.5 follow-up).
- **Kudos! (AI → AI design pre-checks):** Před implementací jasné rozhodnutí (a) per strom vs. per cluster autumn (= realistic „1 druh"), (b) palm season-invariant (= botanická reálnost vs. konzistence UI), (c) createDecor traverze místo per-builder (= KISS). Všechny tři per memory `[[feedback_plan_then_go]]` pattern + DRY linka s sez. 43 FLOWER_COLORS pickem (izomorfie).
- **Censure! (AI → AI shadow flag scope question):** `createDecor` traverze nastaví `noReceiveShadow = true` na **všech meshes**, ale spec DD-49 by možná chtěl per-mesh judgment (např. snow caps = top mesh receivable, trunk = nereceivable). Bez user feedback per-mesh by byl over-engineering, ale měl jsem to explicit zmínit jako sub-prah, ne tichou design choice. **Note for future:** pokud user nahlásí „strom by měl mít přijetý stín od mostového obloukového stromu" → vyloučit specific mesh subset.

### Příště (sez. 44 → sez. 45)

**Cadence (po sez. 44):**
- `%AUDIT:CODE` — **6/8** (sez. 40+41+42+43+44 jen impl, blíží se k 8, due ~sez. 46).
- `%AUDIT:DOCS` — **5/10**.
- IDEAS/TODO pruning — **1/12** (full pass v sez. 43). Next: ~sez. 55.

**Volitelné směry (otevřeno):**

- [ ] **HSL hue shift sky/sun** (DD-48 follow-up) — fyzikálně realističtější Rayleigh při dusk (orange → blue přes purple místo RGB lerp přes desaturovanou hnědou).
- [ ] **WORLD/atmosféra zbytek** — sky/sun season tint (DD-50 follow-up plný scope), snow accumulation animace přes DAY_SPEED × SEASON, polar season variace (summer ablation), snow patches calibration, ice canvas texture, per-cluster water wave fáze.
- [ ] **LIQUID class** (DD-25 vrstva 4) — DD-47 prototype dnes single-mesh per cell, plná 1. třída + bbox clustering. Per 20×20 target marginal benefit.
- [ ] **`%AUDIT:CODE`** — 6/8, due ~sez. 46.
- [ ] **`%BEGIN` projektový addendum target use case check** (sez. 42 Censure carryover).

**Multi-instance:** Sez. 44 single-instance pokračování po sez. 41+42+43 v jednom dni. Fáze 6 close (9/9) zakončila DECOR Fáze cyklus DD-49→DD-52. Bez topic branche — single commit na main.

---

## Sezení 45

### Diskuse

**(A) `%BEGIN`.** Git sync clean, main na `76f80ff` (sez. 44 EOF). Server :8000 stále běžel (PID 33584 z předchozího sezení = několik dní running). Cadence po sez. 44: AUDIT:CODE 6/8 (sub-prah, blíží se), AUDIT:DOCS 5/10, pruning 1/12. Žádné Stale Příště (Fáze 6 close byl čistý cut). Příště ze sez. 44: (1) HSL hue shift sky/sun, (2) WORLD/atmosféra zbytek, (3) LIQUID class, (4) `%AUDIT:CODE`, (5) `%BEGIN` target use case addendum. AI doporučila začít (5) — nejmenší meta-fix, 2 sezení starý dluh. **User volba: „5, 1, 3"** = triple batch A+B+C v pořadí addendum → HSL → LIQUID.

**(B) Plán A+B+C s tradeoff flagem.** AI navrhla pořadí (nejmenší → největší = quick wins před big-think C), flagla scope risk u C (full LIQUID + bbox clustering = 2+ sezení). Doporučení: **C jen jako %THINK + skeleton** (model layer + spawn integrace, bez BFS clusteringu). User: **„Go!"** = plán-then-Go pattern.

**(C) `%BEGIN` addendum (~14 ř `docs/PROMPTS.md`).** Krok (2.5) Target use case check vložen mezi (2) Kontext a (3) Shrnutí. Conditional na perf direction (FPS optimalizace, draw call reduction, shadow opt, InstancedMesh refactor, frustum culling). Memory link [[project-target-use-case]] + [[feedback-target-use-case-check]]. Skip pokud content/feature/refactor/audit/docs. Per sez. 42 Censure carryover — dluh 2 sezení splacen.

**(D) HSL hue shift implementace.** Analýza Three.js Color API: `.getHSL()` + `.setHSL()` exist, ale native HSL lerp chybí. Helper `_lerpHsl(target, a, b, t)` ~22 ř s scratch HSL objekty (`_hslA`/`_hslB` ploché objekty pro zero per-frame alocaci, 4× call/frame). Shorter-path hue interp: pokud `|dh| > 0.5`, wrap přes 0/1 hranici. Pro `_skyDusk` (hue ~0.003 orange) → `_skyDay` (hue ~0.667 blue): diff = 0.664 > 0.5 → wrap dh = -0.336 = jde přes magenta/purple = Rayleigh-correct. Sun colors (warm peach → cream → white) jsou white-dominated, HSL≈RGB v praxi, ale držíme uniformní pipeline (DRY napříč 4 lerps = 2× sun + 2× sky).

User test po HSL: **„HSL test OK"** — visual verify ✓.

**(E) `%THINK` LIQUID class.** AI analýza: 6 sekcí (architektura / pravidla / atributy / přístupy / rizika / procedural check). Navrženy 3 přístupy: A=plná + bbox + Y-split (~150 ř), B=skeleton only (~40 ř), C=%THINK + DD-54 docs-only. Doporučení **B** (KISS + first-things-first + 20×20 target use case nepotřebuje clustering). 3 Q user: variant, vrstva v hierarchii, atributy. User: **A1=A** (skeleton B), **A2=B** (vrstva 4 pod CUBES — chybný memory wording, flagl jsem dál), **A3=A** (LEVEL/TEMPERATURE/BOUNDING_BOX/CELLS bez FLOW_DIRECTION).

**(F) Memory drift detection — Censure! (AI → AI?).** Před implementací jsem cross-checknul Q2 proti `docs/DESIGN_DECISIONS.md` DD-25. DD-25 4-vrstvá taxonomie: Bloky (BLOCKS) / Voxely (COMPOSITES) / Linie (LINES) / Objekty (ASSETS). **Vrstva 4 = Objekty = postavy/zvířata/stroje, ne tekutiny.** Memory `project_model_hierarchy` říkala „DD-25 vrstva 4 LIQUID" = drift. Před commitnutím DD-54 wording musí být correct. Re-Q1 user: A=LIQUID je **nová 5. vrstva** (paralel BLOCKS/COMPOSITES/LINES/ASSETS), B=trust memory (P2 vrstva 4 = Objekt, konceptuálně questionable), C=jiný. **User: A1A „Třeba někdy dojde na fyziku kapalin."** = schválení 5. vrstva + signal pro budoucí extension hooks (TEMPERATURE °C, FLOW_DIRECTION, VISCOSITY).

**Kudos! (AI → AI memory cross-check)** — kdyby AI Q2=B vzala literálně bez cross-checku, DD-54 by zavedlo chybnou taxonomii navždy (immutable log). Memory `[[feedback-subagent-verify]]` aplikováno na vlastní memory state před implementací.

**(G) DD-54 implementace.** ~50 ř kód napříč 2 src soubory + ~50 ř DD-54 entry + memory aktualizace.

- `src/model.js`: `class LIQUID extends CUBES` (~55 ř s docstring). Constructor signature `(id, name, x, level, z, temperature, bbox, cells, description = "")`. `super(id, name, x, level, z, description)` — `Y = level` (LEVEL je sémantický alias pro Y, oba držíme pro budoucí mesh-Y nudge offset proti z-fightu). Atributy: LEVEL, TEMPERATURE (`"frozen"`|`"liquid"` enum), BOUNDING_BOX (`{w, d}` XZ extent), CELLS (`[{x, z}, ...]`). Žádný FLOW_DIRECTION ve skeletonu (DD-29 policy „atributy jen s živým konzumentem").
- `src/main.js`: 
  - Import LIQUID z model.js.
  - `createWaterPlane(w)` → `createLiquidPlane(liquid)` (~17 ř). Čte `liquid.X/Y/Z` (z CUBES base), `liquid.TEMPERATURE` (material dispatch `_iceMat` vs. `_waterMat`), `liquid.BOUNDING_BOX.w/d` (scale). Internal materiály/`_waterGeom`/`_waterMeshes` Set zachovány (KISS = implementation locality, rename na `_liquid*` až přibude lava/oil sourozenec).
  - `buildScene` spawn loop: iteruje `terrain.water[]` raw records → `new LIQUID(id, "Tekutina", w.x, w.y, w.z, w.frozen ? "frozen" : "liquid", { w: w.w, d: w.d }, [{ x: w.x, z: w.z }])` → `createLiquidPlane(liquid)` → scene.add. ID `liquid_NNNN` zero-pad (paralel `decor_NNNN`).
- `docs/DESIGN_DECISIONS.md`: DD-54 entry ~50 ř (rozhodnutí + kontext + class signature + atributy + sez. 45 prototype scope + důvod skeleton ne plný clustering + důsledek + sub-prahy + budoucí extension hooks + references DD-25/27/47/53 + memory link + user note).
- Memory `project_model_hierarchy.md`: description sync sez. 45, ASCII tree přidán LIQUID node pod CUBES (paralel PATH `└──`), DD-54 entry v Klíčových rozhodnutích.
- Memory `MEMORY.md`: index description sync LIQUID + DD-54 + sub-prahy update (drop „DD-25 vrstva 4 LIQUID class" + „%BEGIN target use case addendum" + „HSL hue shift sky/sun" = DONE; add „BFS clustering + multi-Y split" + „fyzika kapalin extensions").

**(H) User test po C.** „Test OK, voda i led vypadají v pořádku." Skeleton = vizuálně identický s DD-47 prototype (same materiály, same per-cell rendering), jen pod LIQUID class wrapper. Conceptual debt repaid bez visual regrese.

**(I) `%END`.** Triple batch shipped + tested. Docs update + commit + push automatic per memory `[[feedback-end-implies-push]]`.

### Rozhodnutí

- **DD-54 LIQUID class jako 5. vrstva DD-25 extension (Tekutiny)** — `class LIQUID extends CUBES`, sibling BLOCKS/COMPOSITES/PATH pod CUBES (paralel PATH pattern, žádná abstract `LIQUIDS` base dokud nepřibude 2. sourozenec per DD-27 vzoru). Atributy LEVEL/TEMPERATURE/BOUNDING_BOX/CELLS. Sez. 45 prototype skeleton: single-cell instance (1 LIQUID = 1 water cell). Plný BFS clustering + multi-Y split = sub-prah.
- **Memory drift fix** — dosavadní wording „DD-25 vrstva 4 LIQUID" napříč memory + sub-prah TODO byl chybný (DD-25 vrstva 4 = Objekty/postavy/zvířata). LIQUID je nová 5. vrstva, ne vrstva 4. Memory `project_model_hierarchy` + `MEMORY.md` index + DD-54 entry explicit pojmenovaný „5. vrstva DD-25 extension".
- **Bez nového DD pro A+B** — `%BEGIN` addendum je dokumentační pravidlo, HSL lerp je DD-48 follow-up (paralel `lerpColors` semantika, jen interpolation space). DD-54 jediný nový DD ze sezení.
- **`createWaterPlane(w)` → `createLiquidPlane(liquid)`** — entry point rename na LIQUID instance. Internal materiály/`_waterMat`/`_iceMat`/`_waterGeom`/`_waterMeshes` Set **zachovány** (= implementation locality, KISS, rename až přibude lava/oil 2. sourozenec).
- **Skeleton ne plný clustering** — per memory `[[project-target-use-case]]` 20×20 dioráma per-cell rendering je visually correct + zero perf cost; DD-53 attempt sez. 42 revert lesson (bbox over varied water_y → z-fight). Plný clustering čeká na user signal („100×100 jezera blikají" / nový perf need).

### Kód

- `src/model.js` +~55 / −~1 ř. (LIQUID class + docstring; export přes existující jmenný export pattern).
- `src/main.js` +~25 / −~12 ř. (import LIQUID, `createWaterPlane → createLiquidPlane` signature change + reads, spawn loop wrap raw record → LIQUID instance, `_lerpHsl` helper + scratch HSL objekty + 4× `lerpColors → _lerpHsl` calls, komentáře).
- `docs/PROMPTS.md` +~14 ř. (krok 2.5 Target use case check, memory links).
- `docs/DESIGN_DECISIONS.md` +~55 ř. (DD-54 entry).
- `docs/TODO.md` ~−6 / +~5 ř. (drop hotové LIQUID/HSL/%BEGIN sub-prahy, add BFS clustering + fyzika kapalin DD-54 follow-up, audit cadence ticky 6→7/5→6/1→2, Recent DONE summary sez. 34-44 → 34-45).
- `docs/DIARY.md` index append k 2026-05-14 multi-session řádku (sez. 45 sekce).
- `docs/diary/2026-05-14.md` +tato sekce (~140 ř).
- `docs/DONE.md` +sez. 45 entry.
- Memory `project_model_hierarchy.md` description sync + tree LIQUID node + DD-54 entry (~10 ř insert).
- Memory `MEMORY.md` index description sync (~1 ř).

**Žádný `src/` debug** — pre-existing `console.warn` (defensive logging pro neznámé typy/textury) zachovány, žádné nové výpisy. Žádný rename grep cleanup mimo `createWaterPlane → createLiquidPlane` (jen 1 caller v `buildScene` spawn loop, fixed v sez. 45).

### Kudos / Censure

- **Kudos! (user → AI scope tradeoff flag):** AI flagla C scope risk v Plánu („full LIQUID + bbox clustering = 2+ sezení") a doporučila skeleton variantu B s explicitním tradeoff statementem („pokud po A+B zbude málo času, C může být jen %THINK výstup do TODO bez kódu"). User volba „Go!" pokrýla i scope. Žádný scope creep ve sezení.
- **Kudos! (AI → AI memory cross-check před DD-54):** User Q2=B vzata literálně by zavedla chybnou taxonomii do immutable DD logu. Cross-check proti `docs/DESIGN_DECISIONS.md` DD-25 zachytil memory drift („vrstva 4" wording), AI re-Q1 user s explicit alternativou (P1=5. vrstva vs. P2=trust memory). Memory `[[feedback-subagent-verify]]` aplikováno na vlastní memory state, ne subagent — *„memory není autoritativní, DD log je"*.
- **Kudos! (user → AI „fyzika kapalin" signal):** Minimální 4 slova v reakci na A1A volbu = jasný signal pro DD-54 budoucí extension hooks. AI explicit zapsala do DD-54 References *„Sez. 45 user note: 'Třeba někdy dojde na fyziku kapalin.' — schválení LIQUID jako samostatné vrstvy s budoucí extension hooks (TEMPERATURE °C / FLOW / VISCOSITY)."* Plán-then-Go pattern v praxi.
- **Kudos! (AI → AI HSL Rayleigh math reasoning):** Před implementací explicit spočítáno: orange hue ~0.003 → blue hue ~0.667, diff 0.664 > 0.5 → wrap shorter path -0.336 = jde přes magenta/purple. Real Rayleigh-correct. Sun colors warm-to-warm zkontrolováno: hue rotace minimální (white-dominated), uniform HSL pipeline OK (DRY).
- **Censure! (AI → AI memory drift retroaktivně):** Memory `project_model_hierarchy` description měla „DD-25 vrstva 4 LIQUID class" napříč ~3 sezení (sez. 38 zapsáno post-DD-47?, sez. 43+44 zachováno bez auditu). Kdyby `%AUDIT:DOCS` cadence běžela v sez. 44 (4/10 sub-prah), drift mohl být chycen dřív. Sez. 46 AUDIT:CODE bude důležitý — checknout, jestli jiné memory entries nemají podobný drift.
- **Censure! (AI → AI ASCII tree edit):** První Edit memory tree art zlomil `└──`/`├──` branch chars (PATH zůstal `└──` s LIQUID po něm, oba poslední = wrong art). User by si toho nevšiml (memory čte AI, ne user), ale je to nečitelné pro vlastní pozdější parsing. Fix v 2. edit. Memory edits s ASCII art vyžadují cross-check celé struktury, ne jen místo edit.

### Příště (sez. 45 → sez. 46)

**Cadence (po sez. 45):**
- `%AUDIT:CODE` — **7/8** *(sez. 40-45 jen impl/skeleton)*. **MUSÍ AUDIT v sez. 46 (threshold trigger)**.
- `%AUDIT:DOCS` — **6/10**.
- IDEAS/TODO pruning — **2/12**. Next: ~sez. 55.

**Volitelné směry (otevřeno):**

- [!] **`%AUDIT:CODE`** *(threshold trigger, mandatory v sez. 46)* — projít src/ napříč model.js + main.js + terrain.js + composites/. Zaměřit se na: terminologie konzistence po DD-54 (LIQUID napříč docs/code), dead code (post-DD-41 atlas residue, pre-DD-54 createWaterPlane references), DD-25 5. vrstva extension dokumentace, conceptual integrity.
- [ ] **WORLD/atmosféra zbytek** — sky/sun season tint (DD-50 follow-up plný scope), snow accumulation animace přes DAY_SPEED × SEASON, polar season variace (summer ablation), snow patches calibration, ice canvas texture, per-cluster water wave fáze (závisí na BFS clustering).
- [ ] **LIQUID BFS clustering + multi-Y split** *(DD-54 sub-prah)* — pokud user nahlásí signal nebo přijde 100×100 test, jinak hold. Mitigace: split components dle `(water_y, frozen)` key (drží z-fight invariant z DD-53 lesson).
- [ ] **Fyzika kapalin extensions** *(DD-54 budoucí hooks)* — TEMPERATURE numeric °C, FLOW_DIRECTION (rivers), VISCOSITY (lava/oil/acid), LEVEL animace (tide/flood). Wait pro user signal nebo nový use case.

**Multi-instance:** Sez. 45 single-instance pokračování po sez. 41+42+43+44 = 5. sezení v jednom dni (2026-05-14). Triple batch A+B+C v jednom commitu (DD-54 jediný nový DD ze sezení). Bez topic branche — single commit na main.

## Sezení 46

### Diskuse

**(A) `%BEGIN`.** Git sync clean, main na `d95264e` (sez. 45 EOF). Server :8000 stále běžel (PID 33584, ten samý ze sez. 45). Cadence po sez. 45: `%AUDIT:CODE` 7/8 — **threshold trigger, mandatory v sez. 46**, `%AUDIT:DOCS` 6/10, pruning 2/12. Žádné Stale Příště. Target use case check (krok 2.5 PROMPTS addendum ze sez. 45) **skip** — AUDIT není perf direction (jen content/refactor). AI vypsala `Příště` ze sez. 45 (4 směry: AUDIT, WORLD/atmosféra, LIQUID BFS clustering, fyzika kapalin) + doporučení AUDIT (threshold + 6-sezení dluh). User: **„%BEGIN pak %AUDIT:CODE"** = explicit chain.

**(B) `%AUDIT:CODE` setup.** AI vytvořila 7 audit oblastí přes TaskCreate (A1 terminologie + A2 architektura + A3 kvalita + A4 soulad + A5 memory drift + A6 dead files + A7 sumarizace). Paralelní průchod místo sekvenčního — Grep + Glob queries kombinované ve 2 vlnách + Read pro detail kontext. Fokus z Příště sez. 45: **LIQUID terminologie po DD-54** (drift „vrstva 4" napříč docs/code), **dead code** (post-DD-41 atlas residue, pre-DD-54 `createWaterPlane`), **DD-25 5. vrstva extension dokumentace**, **conceptual integrity**, plus **memory drift cross-check** (sez. 45 Censure carryover).

**(C) Nálezy — 4 K + 4 D + 2 S.**

**Kritické (drift z immutable rozhodnutí):**
- **K1** `src/terrain.js:138` — komentář „LIQUID 1. třída entita (DD-25 vrstva 4)" → 5. vrstva DD-25 extension (DD-54), plus „vrátí se později" zastaralé (LIQUID v src/ od sez. 45).
- **K2** `src/terrain.js:1049` — „Voda se vrátí jako LIQUID 1. třída entity" → „žije jako LIQUID 1. třída entity (DD-54)".
- **K3** `README.md:53` — drift 2 osy: vrstva 4 → 5. vrstva + status (LIQUID „Plná" sub-prah → DD-54 skeleton hotové).
- **K4** `docs/IDEAS.md:36` — vrstva 4 → 5. vrstva + DD-47 částečně → DD-54 skeleton + sub-prahy BFS clustering + fyzika kapalin extensions.

**Doporučené (sync chybí, ne drift):**
- **D1** `docs/GLOSSARY.md:3` hlavička timeline DD-32..**DD-49** → doplnit DD-50/51/52/53(revert)/54.
- **D2** `README.md:43` M8+ řádek timeline DD-32..**DD-48** → doplnit DD-49..54.
- **D3** `README.md:45` „DD-01 až **DD-52**" → DD-54.
- **D4** `README.md:47-60` „Plán (po sez. 41)" sekce — celá zastaralá (priorita sez. 42 InstancedMesh DECOR refactor downgraded; LIQUID hotové; HSL hue shift hotové). Přepis na „Plán po sez. 45" s user verdiktem close.

**Kosmetické:**
- **S1** `src/main.js:2114` + `src/main.js:2485` komentáře — „Vrácené sez. 38 jako LIQUID prototype" / „Sez. 38 LIQUID prototype" matoucí pro onboard-čtenáře po DD-54. Refresh phrasingu.
- **S2** `src/model.js:82-83` ORIENTATION komentář — „rampy/tunely" reference (TTUNELS smazaná sez. 38 audit). Drop „/tunelů".

**(D) Pozitivní zjištění.** Žádný `console.log` v src/ (jen 6× `console.warn` defensive, per sez. 45 diary explicit „pre-existing zachovány"). Žádný `createWaterPlane` v src/ (DD-54 rename clean). Žádné dead files (`.bak`/`.tmp`/`.old`/`.DS_Store`) ani empty dirs. Atlas residue v src/ = jen 2ř. historický komentář (záměrné). LIQUID sibling order v `model.js`: ř. 397 mezi BLOCKS (86) / SPRITES (235) / COMPOSITES (273) / PATH (353) pod CUBES = **conceptual integrity DD-54 v kódu**. DD-54 entry v `DESIGN_DECISIONS.md` kompletní. Memory drift cross-check: 5 memory entries s DD-49+ reference (project_model_hierarchy + MEMORY.md fixed sez. 45 + 3 nezasažené); 15/20 entries DD-49+ nezmiňují = clean.

**(E) Batch fix přes 6 souborů.** User: **„Oprav vše!"** = volba A z Q1 (K1-K4 + D1-D4 + S1-S2). 10 paralelních Edits:
- `src/terrain.js` 2× komentáře LIQUID drift.
- `src/main.js` 2× komentáře LIQUID prototype phrasing refresh.
- `src/model.js` 1× ORIENTATION drop „/tunelů" + „/ osy tunelu".
- `docs/IDEAS.md` 1× LIQUID entry full rewrite (DD-54 skeleton DONE + sub-prahy).
- `docs/GLOSSARY.md` 1× hlavička timeline doplnit DD-50/51/52/53/54.
- `README.md` 1× M8+ řádek doplnit DD-49..54 + DD-01..52 → DD-54 + Plán sekce přepis post-sez. 45.

Verify post-fix: `Grep "vrstva 4"` v src/ + README + IDEAS = **No matches found** ✓. Immutable docs (DONE/DIARY/DESIGN_DECISIONS/diary historie) nedotčené per pravidlo.

**(F) `%END`.** Cadence reset `%AUDIT:CODE` 7/8 → 0/8 (next ~sez. 54). Žádný nový DD (audit je procedural maintenance). Single commit na main.

### Rozhodnutí

- **Žádný nový DD** — audit pass je procedural maintenance, ne závazné rozhodnutí. DD-54 LIQUID class beze změny (clean v immutable logu).
- **Audit cadence reset:** `%AUDIT:CODE` 7/8 → 0/8 (threshold trigger satisfied, 10 fixů dokumentovaných). `%AUDIT:DOCS` 6/10 → 7/10 (sez. 46 tick, ne plný DOCS audit). IDEAS/TODO pruning 2/12 → 3/12 (sez. 46 přiměřený pruning v rámci AUDIT:CODE — IDEAS LIQUID + Plán sekce wipe).
- **Single commit na main** — sez. 46 = small batch maintenance, žádný topic branch. Per sez. 41-45 vzor multi-session day na main.

### Kód

- `src/terrain.js` ~6 / −5 ř. (K1+K2 komentáře LIQUID drift fix).
- `src/main.js` ~4 / −3 ř. (S1×2 komentáře LIQUID prototype phrasing refresh).
- `src/model.js` ~3 / −3 ř. (S2 ORIENTATION drop „/tunelů" + „/ osy tunelu").
- `docs/IDEAS.md` ~1 / −1 ř. (K4 LIQUID entry full rewrite — 5. vrstva DD-54 + skeleton DONE + sub-prahy).
- `docs/GLOSSARY.md` ~1 / −1 ř. (D1 hlavička sez. 39 → sez. 45 timeline doplnit DD-50/51/52/53/54).
- `README.md` ~18 / −16 ř. (D2 M8+ řádek doplnit DD-49..54, D3 DD-52 → DD-54, K3+D4 Plán sekce přepis post-sez. 45 + LIQUID entry).
- `docs/TODO.md` ~3 / −3 ř. (Recent DONE 34-45 → 34-46 + audit cadence reset 7/8 → 0/8).
- `docs/DIARY.md` +1 ř. (nový samostatný řádek sez. 46 — pivot od mega-řádku 41-45 patternu k cleanup-friendly 1-2 věty per sezení).
- `docs/diary/2026-05-14.md` + tato sekce.
- `docs/DONE.md` + sez. 46 entry.
- Memory `MEMORY.md` index description sync (sez. 45 stav → sez. 46 stav).

**Žádný `src/` debug** — audit fix-only textové komentáře. Pre-existing `console.warn` defensive zachovány.

### Kudos / Censure

- **Kudos! (user → AI audit fokus z Příště):** AI dodržela explicit fokus z Příště sez. 45 (LIQUID terminologie + dead code + DD-25 5. vrstva extension + conceptual integrity + memory drift cross-check) místo full sweep všech 7 audit oblastí naslepo. Memory `[[feedback-target-use-case-check]]` rozšířený pattern „check Příště fokus, ne implicit assumption" aplikovaný i na audit scope.
- **Kudos! (AI → AI memory drift detection):** Sez. 45 Censure retroaktivní carryover *„AI → AI memory drift — kdyby `%AUDIT:DOCS` cadence běžela v sez. 44, drift mohl být chycen dřív"* zachytil sez. 46 audit přesně typ src/docs drift, který memory cross-check sez. 45 nesahá: terrain.js × 2 + README + IDEAS měly „DD-25 vrstva 4 LIQUID" identicky jako pre-DD-54 memory wording. Memory `[[feedback-memory-drift-cross-check]]` rozšířený pattern: memory cross-check zachytí drift v memory, ale **paralel drift v src/docs vyžaduje grep**. Audit cadence je tedy load-bearing — bez pravidelné AUDIT:CODE cadence by drift se hromadil.
- **Kudos! (AI → AI parallel audit pattern):** 7 audit oblastí distribučně přes paralelní Grep + Glob + Read queries ve 2 vlnách místo sekvenčního průchodu. ~10 paralelních tool calls v jednom message, ~3-4 round-trip cykly do nálezů. Compared sez. 39 `%AUDIT:DOCS` sekvenční pattern = ~2-3× rychlejší.
- **Kudos! (AI → AI grep verify post-fix):** `Grep "vrstva 4"` napříč src/ + README + IDEAS = **No matches found** po batch fix. Memory `[[feedback-subagent-verify]]` aplikováno na vlastní changes (= „verify post-fix s gripem"), ne jen claim „10 fixů hotových".
- **Censure! (AI → AI minimal sub-scope nálezy):** Audit našel jen drift typu **textový komentář / wording**. Žádný strukturální nález (architektura DD-11, dead code, debug výpisy, dead files) → buď sez. 41-45 byly disciplinované, NEBO audit nesahal dostatečně hluboko. Pro full check by AI měla projít: (a) každý src/ soubor end-to-end (ne jen grep specifických patternů), (b) `meshByInstance` lifecycle, (c) cleanup paths v `regenerateScene`, (d) error boundary v `generateTerrain`. Sub-prah pro `%AUDIT:CODE` sez. 54: deeper file-level pass.

### Příště (sez. 46 → sez. 47)

**Cadence (po sez. 46):**
- `%AUDIT:CODE` — **0/8** *(sez. 46 RESET)*. Next: ~sez. 54.
- `%AUDIT:DOCS` — **7/10**. Next: ~sez. 49.
- IDEAS/TODO pruning — **3/12**. Next: ~sez. 55.

**Volitelné směry (otevřeno):**

- [ ] **WORLD/atmosféra zbytek** — sky/sun season tint (DD-50 follow-up plný scope), snow accumulation animace přes DAY_SPEED × SEASON, polar season variace (summer ablation), snow patches calibration, ice canvas texture, per-cluster water wave fáze (závisí na BFS clustering).
- [ ] **LIQUID BFS clustering + multi-Y split** *(DD-54 sub-prah)* — `terrain.js` Krok 6.5 post-flood-fill: visited Set + queue, split components dle `(water_y, frozen)` key. Trigger: user signal „100×100 jezera blikají" nebo perf need.
- [ ] **Fyzika kapalin extensions** *(DD-54 budoucí hooks)* — TEMPERATURE numeric °C, FLOW_DIRECTION (rivers), VISCOSITY, LEVEL animace. Wait pro user signal.
- [ ] **Deeper file-level AUDIT pass** *(sez. 46 Censure sub-prah)* — pro sez. 54 cadence trigger projít src/ end-to-end ne grep-only: meshByInstance lifecycle, cleanup paths v regenerateScene, error boundary.

**Multi-instance:** Sez. 46 6. sezení v jednom dni (2026-05-14, po sez. 41-45). Audit-only single commit na main.

## Sezení 47

### Diskuse

**(A) `%BEGIN`.** Git sync clean, main na `5f20f10` (sez. 46 EOF). Server :8000 stále běžel (PID 33584, kontinuita ze sez. 45/46). Cadence po sez. 46: `%AUDIT:CODE` 0/8 (reset), `%AUDIT:DOCS` 7/10, pruning 3/12. Žádné Stale Příště. Krok 2.5 target use case check — skip (žádný směr není perf direction).

Příště sez. 46 vypsala 4 volitelné směry (žádný explicit priority): WORLD/atmosféra zbytek, LIQUID BFS clustering (wait), fyzika kapalin (wait), deeper AUDIT pass (sub-prah sez. 54). AI doporučila WORLD/atmosféra zbytek jako jediný actionable. User: **„A"** — atmosféra.

**(B) Sky season tint (B1).** AI návrh přístupu PŘED kódem (CLAUDE.md „Discuss ideas before writing code"): pattern „base × season modifier" izomorfní s DECOR_DENSITY sezonním modifikátorem (sez. 43). Post-lerp HSL shift v `updateAtmosphere` přes nový `_seasonTint(color, season)` helper, scope `world.LATITUDE === "temperate"` (polar/tropical mají season-invariant snow/water spec, drží konzistenci sky). 4 otázky (Q1 intenzita subtle vs distinct, Q2 scope LATITUDE, Q3 sun tint také, Q4 nový DD). User: **„Go, A1=A A2=A A3=A A4=A"** = subtle deltas + temperate-only + skip sun (sub-prah) + žádný DD (DD-50 plný scope follow-up implementation).

Implementace: `SEASON_SKY_DELTA` tabulka v main.js (vedle `_lerpHsl` helper skupiny sez. 45) + `_seasonTint` helper s scratch `_hslA` reuse (zero alok) + volání v `updateAtmosphere` post-lerp před `sceneFog.color.copy` (fog automaticky picke tintovanou barvu). Summer = baseline {dh:0, ms:1, ml:1} no-op (zero regression). Subtle deltas: winter cooler+desatured+dimmer, autumn golden+oversaturated, spring mírně teplo, summer baseline.

User test: **„Je to skvělé."**

**(C) Sun season tint (A3 sub-prah → user wave 2) + sun horizon −15° (user extension).** User: **„B1 a zobrazuj slunce až do -15 stupňů pod obzorem, ať nelze pozorovat zmizení slunce ze zobrazovaných."** Dvě věci v jedné: (1) sun tint implementovat (A3 byl skip pro sub-prah), (2) `sunMesh.visible` threshold posunout pod horizont.

AI refactor `_seasonTint(color, season)` → `_applyHslShift(color, delta)` (helper o úroveň níž, season-agnostic — caller dodá delta z příslušné DELTA tabulky). DRY: sky volá s `SEASON_SKY_DELTA[season]`, sun s `SUN_SEASON_DELTA[season]`. SUN deltas mírnější (sun = eye attracter, menší plocha visual punch větší + na poledni white maskuje saturaci).

Sun horizon: `SUN_HORIZON_Y_MIN = SUN_DISTANCE * Math.sin(THREE.MathUtils.degToRad(-15))` ≈ −4.21 j. `sunMesh.visible` check z `sun.position.y > 0` → `> SUN_HORIZON_Y_MIN`. DirectionalLight intensity zůstává `Math.max(0, negCosA)` = sun pod horizontem nesvítí (= správné), jen mesh „za scénou" jako vizuální cue.

User test: **„Test OK."**

**(D) Polar season variace (B3).** Po dnešní SEASON expansion nabídla AI 4 volby (B2 slider sync, B3 polar variace, B4 ice texture, C deeper audit). Doporučení **B3** — polar je dnes jediná LATITUDE, která SEASON ignoruje (model.js:491 explicit „polar perpetually-winter"), conceptual integrity argument. User: **„B3"**.

AI návrh: 2 strukturální volby. **Volba A** (DRY/KISS): rename mode `"temperate"` → `"patches"`, sloučit polar+temperate na jeden code path s odlišnými parametry. **Volba B** (zachovat mode `"polar"` + paralel branch v Krok 3.5, ~15 ř. duplikovaného kódu). Doporučení A — mode jméno `"temperate"` popisuje *biome*, ne *algoritmus*. Kalibrace dle reálné glaciologie: Arctic sea-ice ~70 % ablation extent, Greenland surface melt 30-70 % area, Antarctica ~83 % reduction. POLAR_SNOW {spring:0.15, summer:0.40, autumn:0.15, winter:0.00} (patchThreshold inverse), POLAR_FREEZE {spring:0.7, summer:0.4, autumn:0.7, winter:1.0}. Polar winter = bit-identical s dnešním default (zero regression). User: **„A"**.

Implementace 5 souborů (terrain.js refactor + model.js komentář + GLOSSARY:65 sync). **Bug fix mid-impl**: AI si všimla, že polar `altThreshold = 0` znamená Pass 1 chytá všechny cells s `y_top ≥ 0` jako always-snowed → `patchThreshold` nemá vliv. Fix: `altThreshold = Infinity` (= žádné Pass 1 cells, vše do Pass 2 sort+rank, patchThreshold řídí procento snowed cells přesně).

User test: **„Test OK."**

**(E) Slider DAY sync (D2) + ice canvas texture (D3).** AI nabídla %END (3 hotové kusy = solidní arc), user: **„D2 a D3"**. Dvě věci paralelně:

(E1) D2 slider sync: `index.html` setInterval(250ms) v DOMContentLoaded — update `dayInput.value` + readout text z `world.DAY`. Skip pokud `world.DAY_SPEED === 0` (paused) nebo `document.activeElement === dayInput` (user mid-drag).

(E2) D3 ice texture: `makeIceCanvasTexture()` generator v main.js — 128×128 Canvas2D, base `#d9e8ec` + 30 white radial blobs (r=4..12, alpha 0.7). `_iceTexture` shared, `_iceMat.color → 0xffffff` + `map: _iceTexture`. Wrap RepeatWrapping (default 1×1).

User verdikt: **„Kudos! Parádní finalizace, zlepšení, dotažení. Já už byl s výsledkem spokojený před 5 sezeními, ale stálo to za to!"**

**(F) `%END`.** Single commit na main (sez. 47 = 6 kusů + bug fix + refactor, středně velké ale stále menší než DD-54 sez. 45 nebo DD-47 sez. 38). Per memory `[[feedback_end_implies_push]]` automaticky push.

### Rozhodnutí

- **Žádný nový DD** (A4=A) — sky tint, sun tint, polar variace = DD-50 plný scope follow-up implementation, sub-prah byl explicit v DD-50 entry. Ice texture = DD-47 follow-up. Slider sync = DD-38 kotva. Sun horizon = drobný `_visible` threshold tweak.
- **Mode refactor `"temperate"` → `"patches"`** — KISS/DRY: mode = algorithm selector, ne biome marker. Volba A z user Q1. Polar+temperate sloučeny na jeden code path.
- **Sun horizon −15° hard cutoff** (per user explicit). Smooth fade alternativa = sub-prah pokud user signal „září v zemi".
- **Single commit na main** — sez. 47 středně velké, multi-day session pattern sez. 41-46 main commits zachován.

### Kód

- `src/main.js` +~75 / −10 ř. — SEASON_SKY_DELTA + SUN_SEASON_DELTA tabulky, `_applyHslShift` helper (refactor z `_seasonTint`), volání v `updateAtmosphere` (post sky lerp) + `updateSun` (post sun piecewise lerp), `SUN_HORIZON_Y_MIN` konstanta + `sunMesh.visible` threshold, `makeIceCanvasTexture` generator + `_iceTexture` shared + `_iceMat` color/map swap.
- `src/terrain.js` +~25 / −15 ř. — POLAR_SNOW_BY_SEASON + POLAR_FREEZE_BY_SEASON tabulky, `snowSpecForLatitude` polar branch (mode "patches" + altThreshold Infinity + altBias 0.1 + season-aware patchThreshold) + temperate branch rename na "patches", `waterSpecForClimate` polar branch (POLAR_FREEZE lookup), Krok 3.5 polar `if` odstranění + komentář refresh, snowNoise + `if (snowSpec.mode === "patches")` switch.
- `src/model.js` +~10 / −7 ř. — JSDoc SEASON konzumenti rozšířit o polar season-aware + sky/sun tint, inline komentář v constructor sync.
- `index.html` +13 / 0 ř. — setInterval 4 Hz DAY sync v DOMContentLoaded.
- `docs/GLOSSARY.md` ~1 / −1 ř. — SEASON entry sync (polar season-aware + sky/sun tint).
- `docs/TODO.md` (% END update).
- `docs/DIARY.md` +1 ř.
- `docs/diary/2026-05-14.md` + tato sekce.
- `docs/DONE.md` + sez. 47 entry.
- Memory `MEMORY.md` description sync.

**Žádný `src/` debug** — 6× `console.warn` pre-existing zachovány, žádné nové. Atlas residue beze změny (jen historický komentář).

### Kudos / Censure

- **Kudos! (user → AI):** *„Parádní finalizace, zlepšení, dotažení. Já už byl s výsledkem spokojený před 5 sezeními, ale stálo to za to!"* — explicit recognition arc sez. 42-47 jako stylizovaný refinement. Sez. 42 user pivot „20×20 dioráma je real" zafixoval target use case → následující 5 sezení (43 Fáze 6 close, 44 final 3, 45 LIQUID class + HSL + addendum, 46 audit, 47 SEASON expansion) byly polish v rámci accepted scope, ne novel features.
- **Kudos! (AI → user — držení scope):** User dnes konzistentně volil A defaults (A1=A A2=A A3=A A4=A pro sky tint, „A" pro polar volbu) a držel sezení coherentně „SEASON expansion" arc. Po 6 kusech volil %END místo dalšího kusu = mature stop signal. Memory `[[feedback_plan_then_go]]` aplikováno bilateralně (rychlé „Go" + jasné EOF).
- **Kudos! (AI → AI refactor mid-impl):** Polar bug detection v `altThreshold = 0` byl AI self-review post-edit (= „let me trace patchThreshold semantiku znovu") před user test, ne reactive po user reportu visual bug. Memory `[[feedback_subagent_verify]]` rozšířený pattern: pro own changes „verify post-fix s tracingem semantiky", ne jen post-grep claim. Plus mode `"temperate"` → `"patches"` rename byla AI initiative pro DRY/KISS, ne user-asked — user souhlasil A volbou.
- **Kudos! (AI → AI parallelní 5-soubor batch):** Polar variace refactor byl 5-soubor batch (terrain.js 2× edit, model.js 2× edit, GLOSSARY.md 1× edit) provedený sekvenčně s post-batch syntax check `node --check` × 3 souborů. Cca 3 round-trip cyklů.
- **Censure! (AI → AI nadužívání Edit count):** Sezení mělo ~12 Edit volání napříč 5 souborů. Pro některé bloky by Multi-replace_all nebo větší kontext per Edit mohly zkrátit (např. JSDoc model.js update byl 2 Edits pro 2 paragraphy, mohl být 1 Edit s větším blok kontextem). Sub-prah: pre-Edit task plan = které Edits sloučit do single Edit s nadřazeným context blockem.

### Příště (sez. 47 → sez. 48)

**Cadence (po sez. 47):**
- `%AUDIT:CODE` — **1/8**. Next: ~sez. 54.
- `%AUDIT:DOCS` — **8/10**. Next: ~sez. 49.
- IDEAS/TODO pruning — **4/12**. Next: ~sez. 55.

**Volitelné směry (otevřeno):**

- [ ] **Snow accumulation animace přes DAY_SPEED × SEASON** *(DD-50 follow-up plný scope, velký kus ~150 ř.)* — temporal evolution: sníh roste v zimě, taje na jaře. Per-cell state (`accumulationLevel ∈ [0,1]`) + per-frame integrator dle `world.DAY × world.SEASON`. State persistence napříč regen problem (regen = re-init).
- [ ] **LIQUID BFS clustering + multi-Y split** *(DD-54 sub-prah)* — terrain.js Krok 6.5 post-flood-fill. Trigger: user signal „100×100 jezera blikají" nebo perf need.
- [ ] **Fyzika kapalin extensions** *(DD-54 budoucí hooks)* — TEMPERATURE numeric °C, FLOW_DIRECTION (rivers), VISCOSITY, LEVEL animace. Wait pro user signal.
- [ ] **Deeper file-level AUDIT pass** *(sez. 46 Censure sub-prah)* — pro sez. 54 cadence trigger.
- [ ] **Sky tint kalibrace** *(sez. 47 sub-prah)* — pokud user feedback „moc subtle" nebo „moc distinct", tweak `SEASON_SKY_DELTA` / `SUN_SEASON_DELTA` deltas.
- [ ] **Ice texture per-cluster variation** *(sez. 47 sub-prah)* — sdílená texture napříč všechny ice meshes = 100×100 polar = identický pattern napříč cells = tile-grid může být vidět. Pokud rušivé, per-mesh texture nebo per-cluster (závisí na BFS clustering).
- [ ] **Smooth fade sun pod horizontem** *(sez. 47 sub-prah)* — pokud user signal „sun září v zemi" (mesh `fog: false`), alpha fade na intervalu Y [0, SUN_HORIZON_Y_MIN] místo hard cutoff.

**Multi-instance:** Sez. 47 = 7. sezení dne (2026-05-14, po sez. 41-46). Single commit na main. SEASON expansion arc close — DD-50 sub-prahy plný scope hotové (sky tint, sun tint, polar variace).
