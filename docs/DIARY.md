# Diary

Index denních logů. Detaily v `docs/diary/YYYY-MM-DD.md`.

- [2026-05-13](diary/2026-05-13.md) — **Sez. 39:** **%AUDIT:DOCS sez. 39 cleanup (drift po DD-42→DD-48 + překlepy) + DD-49 kotva pro krajinné COMPOSITES.** Audit 4 K + 9 D + 6 KK + 2 S nálezy → commit `a4ed9c4` (7 souborů, 37+/32−). Hlavní fixy: GLOSSARY BLOCKS rodina TEXTURE_* drop (po DD-41), Biome list water drop (po DD-47), README Milníky doplněn DD-47/48, GLOSSARY M8+ list sez. 35-38, CLAUDE.md `%CALIBRATE` drop (PocketStory-specific, ne globální), PROMPTS.md cesta na `~/.claude/PROMPTS.md`, IDEAS „Voda ve všech skupenstvích" → DONE částečně, supersededvodu/ozhuje/LIQID/Source překlepy. **DD-49 kotva** (sez. 39 plánovací, bez impl) pro krajinné COMPOSITES: 1 generická třída `DECOR` (KIND/SEED/SCALE) + 5 procedurálních builderů (spruce/oak/bush/rock/grass_tuft) + shared toolkit (lowpoly material singleton + per-(KIND × part) geom cache + 6 paleta + mulberry32) + biome-aware density driver `DECOR_DENSITY[latitude][humidity]` (3. osa po DD-44 surface + DD-47 snow/water) + `decorate` Krok 7 v `generateTerrain` → `decorations[]`. Inspirace Quaternius Ultimate Nature Pack (vizuální reference, ne asset import). arena.ai = textová aréna (lmarena.ai), ne mesh-aréna — A3=c Claude přímo. User volby A1=a (faceted lowpoly) / A2=a (5 KIND minimum) / A4=jen DD kotva / A5=a (generická DECOR) / A6=b (biome-aware) / A7=b (shared geom cache) / A8=a (DD-49 kotva před impl) / A9=ok. **Diagnostika FPS/refresh/voda** (user „zapomeň, F12 clear"): FPS 60 @ 10×10 = vsync cap, ne regrese. Refresh pomalý = unpkg.com three.module.js 1.27 MB / 1.0 sec + 5 addons → ~1.5-2 sec při DevTools Disable cache. Voda/led v kódu bez očividného bugu. **Kudos! (user):** strukturní intuice (COMPOSITES > 3 sub-prah cesty); plán+Q→Go pattern 2× (A1-A9, A5-A9); diagnose self-correction (F12 Disable cache). **Censure! (AI → AI):** sez. 38 audit měl řešit K1 (BLOCKS TEXTURE_*) přímo, ne odložit do sub-prah; K4 GLOSSARY M8+ list mapping milníky→kód checkpoint chyběl; `%CALIBRATE` mýtus v TODO 16 sezení.
- [2026-05-13](diary/2026-05-13.md) — **Sez. 38:** **%AUDIT:CODE cleanup + DD-47 G6 climate-driven surface state (snow + drop water + LIQUID prototype) + DD-48 atmospheric color extensions.** Single-instance na main bez topic branche (multi-feature). **Audit:** K1 README docs drift (8 sezení driftu, DD-37→DD-46 milníky chyběly), K2 CLAUDE.md (4 řádky sync), D1 BLOCKS+COMPOSITES import drop, D2 TTUNELS DROP (~287 ř., model+dispatch+builder smazáno, vrátit z gitu), D3 terrain.js komentář, D4 GLOSSARY engine-internal maps. Adaptive fog `near = max(sx,sz)*0.3, far = max(sx,sz)*1.2 + 10` bug fix (fixed 30/120 byl pro 100×100, default 10×10 měl celý terén v <near zóně). **DD-47** (`snowSpecForLatitude` polar all + temperate sort+rank top 30 % s altitude bias score; `waterSpecForClimate` priority flood Wang & Liu, dry no water, freezeRatio polar 1.0 / temperate 0.3; `BLOCK_COLORS` 4→8 `_snow` varianty s TOP=`0xf5f5f5`; `BIOME_SURFACES` 12×3 drop water column, % přerozděleno fertile→grass / polar→stone+sand; `BIOME_NAMES.tropical.wet` rename "Tropický prales"; ramp snow propagace ze source cell). **DD-48** (sun color 3-keypoint piecewise `_sunColorSunrise/Mid/Noon` driver `daylight`; sky 3-keypoint piecewise `_skyNight/Dusk/Day` driver raw `negCosA`; ice materiál `0xd9e8ec` opacity 0.85 roughness 0.55 = větší zákal + menší reflexe; water wave anim sinusová `position.y += sin(t·ω)·0.04`, period 9 s; dirt color zesvětlen 0x6b4423→0x8a5e36 pro lepší hover kontrast). Calibration: sun/sky peak 1/10→3/10 (30 % blend), water wave 3 s→9 s (3× pomalejší). **Příště sez. 39:** voda cluster do bbox (1 plane / jezero), HSL hue shift pro sky lerp, WORLD.SEASON driver. **Kudos! (user):** zadání skupenství vody = 4 konkrétní pravidla najednou (basin + boundary + polar all ice + temperate proportional) konceptuálně izomorfní s sněhem → AI reusable abstraction `*SpecForClimate` pattern; volba 30 % blend pro atmospheric peak (1/10 málo, 10/10 přepálené); test OK po 2 iteracích snow algoritmu. **Censure! (AI → AI):** "Skvělé zadání" úvodní lichotka (`feedback_critic_ratio` zakazuje); první snow algoritmus byl uniform noise (žádný altitude bias) — user musel požádat; iter. 2 altitude-biased threshold zvýšil celkovou plochu (= snow všude) — sort+rank teprve řešil. **Censure! (AI → AI):** mark "TTUNEL_GEOM_CACHE_DROPPED_MARKER" placeholder místo přímého drop celé IIFE = 2 Edit kroky místo 1, drobný workflow shortcut chyba.
- [2026-05-13](diary/2026-05-13.md) — **Sez. 37:** **DD-46 G5 smoothstep bimodální heightmap pro high relief (r ≥ 6).** Topic branch `feat/terrain-bimodal`. Motiv: user vizuální test DD-45 *„posílit hřebeny+údolí"* (46×47 r6 polar/wet → ridge³ single-peak nestačil). Algoritmus: `t = smoothstep(0.4, 0.6, fbmVal); blended = lerp(VALLEY_AMP=-1, PEAK_AMP=amplitude, t)`. Bimodální distribuce — USER case Y=-1: 34 % (valley) + Y=4: 18 % (peak) + Y=1: 6 % (transition min) = 83 % cells v extrémech. Y=0 chybí (smoothstep cliff = žádaná vlastnost). DD-45 sub-prah „bimodální heightmap G5" uzavřen. Příště: voda ve všech skupenstvích (raw nápad → IDEAS). **Sez. 36:** **G3 SURFACES driver-derived per biome (DD-44) + fBm/ridge³ heightmap (DD-45).** Topic branches `feat/biome-surfaces` (G3) + `feat/terrain-noise` (DD-45 D varianta). G3 commit + merge `1c21b13`, pak D varianta. **DD-45 motiv:** user feedback po G3 *„50×50 relief 7 = neprostupný šum, nešlo by hřebenovité hory s údolími?"* — diagnóza 1-okt value-noise + high freq pro high relief = ~1.8 cells per noise uzel × full amp = vrchovinný šum. `%THINK` 5 alternativ (A re-tune / B fBm / C ridge / D blend / E domain warp), user A1=D A2=A A3=A. **Implementace:** `fbmSample(noiseFn,x,z)` 3-oktávy s persistence 0.5 + lacunarity 2 + normalize děli 1.75; `ridge(v)=1-|2v-1|` s `r³` (3 iterace: pure → ² → ³ pro lepší distribuci, plateau efekt postupně snížen z 58% → 33% → 23% top voxel cells); `ridgeWeight=max(0,(relief-4)/4)` → 0% ridge pro relief 0-4 (pure fBm rolling hills), 100% ridge pro relief 8 (full ridge horské hřebeny). `RELIEF_AMPLITUDE` idx 8: 6→8 (Alpine), ostatní beze změny (zachová `maxReliefForSize` UX — 50×50 dál relief 7). `RELIEF_FREQUENCY` dramaticky nižší pro high relief (0.45/0.55/0.65 → 0.18/0.15/0.12) — velké hřebeny místo drobných štítů. **Test:** 50×50 r7 range -1..4 single-peak dist (Y=3 30%, Y=4 23%) = lepší než dnes (Y=4 plateau 58%), ale ne true bimodální. 100×100 r10 Alpine = výborné (Y=-2..8 11 úrovní, široká dist, 5238 ramps). 10×10 r3 boot beze změny. **Sub-prah G5 bimodální heightmap** (threshold step `fbm<0.5?valley:peak` nebo dual noise continent+ridge) zapsán pro budoucí iteraci pokud user feedback bude "lepší ale ne ideální". **Censure! (AI → AI):** 3 iterace ridge (pure → ² → ³) před single-peak distribuci — well-known plateau artifact měla AI vědět z terrain literature, ne trial-and-error. **G3 (DD-44) shrnutí — hard override v UI:** `BIOME_SURFACES` 4×3 lookup (12 buněk × 4 koef.) + `surfacesForBiome(lat, hum)` helper v terrain.js; Climate slidery `change` triggerují regen, surface slidery smazány; `polar.wet` = alias `polar.mid` + rename `BIOME_NAMES.polar.wet` na "Polární tundra" (transparent fallback). DD-29 CLIMATE slot full (2 živí konzumenti: sun tilt G2 + surface mix G3). User volba Q1=A (hard override) / Q2=A (Tundra alias) / Q3=A (schválená tabulka) / Q4=A (snow surface jako G4 sub-prah). Smoke test 12/12 buněk sum=1.0 OK, alias OK, fallback temperate.mid OK. **Sez. 35:** **G1 Volba max Y + G2 Climate (DD-42 WORLD LATITUDE×HUMIDITY + sun tilt driver, DD-43 DAY mapping 0.5=poledne supersede DD-38 sun position math).** Topic branch `feat/world-climate`. Po G2 (DD-42 sez. 35) Climate atributy měly jen jednoho konzumenta (sun tilt) — `BIOME_NAMES` 4×3 tabulka byla nakreslená v UI readoutu, ale surface mix v `generateTerrain` byl pořád z UI sliderů (sez. 26). UI dovolil "Sahara s 0.8 grass" = nesoulad popisku biome a vygenerovaného terénu. **`%THINK` 3 rozhodnutí:** Q1 UI hard override vs. soft hint vs. toggle Manual (volba A hard — izomorfismus s G2 sun, KISS, ne dvojí mód); Q2 polar/wet fallback Tundra alias vs. validation error vs. snow stub (volba A Tundra alias — geo nejbližší, KISS, no special case); Q3 mix tabulka 4×3 — schválena (volba A, 12 řádků hardcoded > parametric formula). Q4 sub-prah snow surface jako G4 kandidát (A zapsat). User volba `A1A A2A A3A A4A`. **Implementace:** `src/terrain.js` `BIOME_SURFACES` export (12 buněk × 4 koef. sum=1.0, HUMIDITY → water+grass, LATITUDE → stone vs. sand), `surfacesForBiome(lat, hum)` helper s fallback temperate.mid, `BIOME_NAMES.polar.wet` rename `"—"` → `"Polární tundra"`, `polar.wet = alias polar.mid`. `src/main.js` import + `window.BIOME_SURFACES`/`window.surfacesForBiome` expose; `TERRAIN_DEFAULTS` bez hardcoded `surfaces`; `buildScene` driver-derived `surfacesForBiome(world.LATITUDE, world.HUMIDITY)`. `index.html` Surface DOM sekce **smazána** (4 slidery + auto-normalize), IIFE refactor: `SURFACE_KINDS`/`surfInputs`/`surfVals`/`normalizeSurfaces` pryč, `LATITUDE/HUMIDITY KEYS+NAMES` + Climate DOM refs přesunuto nahoru pro `readParams()`; Climate slidery `change` event triggeruje regen. **Smoke test ESM** 12/12 sum=1.0 OK, alias OK, fallback OK, generátor projde všemi 12 combiny. **DD-44** zapsán (aktivace DD-29 odložený `CLIMATE` slot full — 2 živí konzumenti). **Známý dluh:** `sand` v `polar.*` je proxy pro sníh (sub-prah TODO G4 snow surface). **Sez. 35:** **G1 Volba max Y + G2 Climate (DD-42 WORLD LATITUDE×HUMIDITY + sun tilt driver, DD-43 DAY mapping 0.5=poledne supersede DD-38 sun position math).** Topic branch `feat/world-climate`. **G1 quick win** — `maxReliefForSize` export z `terrain.js`, `relief.max` clamp dynamicky dle MIN(sx,sz) (10×10 → 0..3 Rolling, 100×100 → 0..10 Alpine). Žádný DD. **G2 návrh — user A1 matice 4×3 biomů** (Tropický deštný prales / Savana / Sahara / Vlhké subtropy / Mediteránní / Subtropická step / Listnatý les / Step Prérie / Chladná poušť / Tundra / Ledová poušť; polar/wet edge case "—"). Skutečná geografie Země místo 1D LATITUDE. **DD-42** WORLD `LATITUDE` (4 enum) + `HUMIDITY` (3 enum), `SUN_TILT_BY_LATITUDE` lookup (tropical=0 / subtropical=π/12 / temperate=π/6 / polar=π/3) supersede DD-38 fixed `SUN_TILT = π/6`, `BIOME_NAMES` 4×3 export, UI Climate sekce v `#terrainctrl` (2 slidery + biome readout). G2 MVP konzument: sun tilt (LATITUDE only) + UI biome readout. G3 plánovaný: surface mix driver-derived per biome. **DD-43** DAY mapping `0.5=poledne` (supersede DD-38 sun position math) — `updateSun()` `sin/cos` → `sin/(-cos)` parametrizace, `updateAtmosphere()` daylight `sin` → `-cos`, default `world.DAY = 0.5`. Pre-G2 micro-fix navržen userem (A2): *„DAY=0.5 by mělo být poledne, ne?"* — UX vs. code-economy trade-off. **Surface drift fix (bug fix, ne DD)** — pre-existing `normalizeSurfaces` rounding drift (sez. 26): `toFixed(2)` × 4 sliderů = ±0.02 sum worst-case drift, generátor tolerance ±0.001 → throw → tichá failure. Fix: divide-by-sum normalize v `readParams()` (4 ř.). Odhalen TEST 3 user. **Censure! (AI → AI):** generátor tolerance ±0.001 napsáno bez analýzy reálného UI rounding range (11 sezení dluh, tichá failure). Druhá censure: `SUN_TILT_BY_LATITUDE.tropical = 0` (přímý zenith) bez varování o trade-off (žádné stíny v poledne). **Kudos! (user):** A1 návrh 2D matice biomů > 1D LATITUDE návrh AI — skutečná geografie + připravuje G3 surface mix s reálnou variabilitou (12 typů místo 4). Multi-instance: single (mrkla). Topic branch `feat/world-climate` — Q při %END: dokončený? ANO → merge --no-ff do main. **Sez. 34:** **G0 Lowpoly vertex-color pipeline (DD-41 supersede DD-36 atlas).** Topic branch `feat/lowpoly-blocks`. Inspirace `jasonkneen/tiny-world-builder` (Three.js r128, `MeshLambertMaterial`, sdílený mat registry, tilt-shift). `%THINK` G0 → 3 alternativy (A vertex colors + 1 global mat / B per-kind solid mat / C atlas s 1×1 solid color), A vítěz (KISS + řeší tile pattern dluh DD-36 + připravuje G3 climate barvy). User volba `A1A' A2A A3A A4A`. **G0a TCUBES** — `BLOCK_COLORS` 3-key paleta (TOP/BOTTOM/SIDE × 4 kindy), sdílený `_lowpolyMat = MeshLambertMaterial({ vertexColors: true })`, `getTcubesKindGeom(kind)` cache BoxGeometry s color attribute (24 verts × 3 floats, sRGB→linear convert per kanál). **G0b rampy** — `RAMP_FACE_VERT_COUNTS` + `RAMP_FACE_PALETTE_KEYS` (TOP/BOTTOM/SIDE per face index), `getRampGeom(type, surface)` klonuje atlas IIFE (TRRAMP/TTRAMP/TDRAMP_GEOM_CACHE) jako raw geom + drop UV + inject color attribute; 3 typy × 3 surfaces = max 9 cached. TDRAMP SLOPE+TOP sdílí `.TOP` (lomený povrch). Test 100×100 FPS **101** (parita sez. 31 baseline 104), 13 batchů zachovaných (DD-37 stable). **User nález — šedý seam mezi sousedními voxely při slabém světle.** Hypotéza: `flatShading: true` nutí shader spočítat normálu z `dFdx/dFdy` derivatives v fragment shaderu — u **InstancedMesh** na hraně mezi 2 sousedními instancemi precision drift = tenké seam linky. Fix: drop `flatShading: true` (default false) — BoxGeometry i ramp BufferGeometries už mají per-face normály v `attributes.normal`, flat look z geom samotné. **User palette tweak** — sand více žlutá (0xe8d97a TOP / 0xd9c66a sides), stone světlejší šedá (0x9a9a9a / 0x8a8a8a). **Cleanup:** smazáno `BLOCK_TEXTURES`, `ATLAS_TILE_PX/FACE_KEYS`, `_sharedAtlasBoxGeom`+builder, `_tcubesAtlasMatCache`+builder, `RAMP_*_TEXTURES` (3 mapy), `RAMP_SURFACE_FROM_KIND`, `RAMP_ATLAS_SPECS`, `_rampsAtlasMatCache`+builder, `createTRRampFor`/`TTRampFor`/`TDRampFor` slow-path, `createMeshFor` ramp dispatch case, `createTCubeFor` slow-path material[6] array. Model.js: drop `textures` arg + `TEXTURE_*` fields z TCUBES/TRRAMPS/TTRAMPS/TDRAMP (TTUNELS si zachovává — mimo G0 scope). Net ~−250 ř. atlas garbage. **4 TODO follow-up** z tiny-world-builder inspirace: drop-in animace tiles při `regenerateScene` (z `dropAnims` queue), tilt-shift post-process (gradient blur podle screen Y), `ExtrudeGeometry` pro rampy (lazy refactor po G0), adjacency-aware re-render pattern pro budoucí PATH/FENCE. **DD-41** zapsán (immutable log, supersede DD-36). **Kudos! (user → AI flow):** plán+Q→Go pattern (`A1A' A2A A3A A4A`), `tiny-world-builder` GitHub link přesně v pravý moment (před cleanup) — AI rozšířila plán o Lambert + tilt-shift insight + 4 follow-up. **Censure! (AI → AI):** první návrh `flatShading: true` bez InstancedMesh-cross-instance pre-flight úvahy — cross-instance derivative artifact je known Three.js limitation (issue tracker), AI měla vědět nebo aspoň zvážit (per-vertex normály jsou už default v BoxGeometry, redundance). **Censure! (AI → AI):** atlas IIFE `_RAMP_RAW_GEOM_*` rename + UV strip at source byl plánovaný v DD-41 ale odložen na sub-prah cleanup — dnes IIFE pořád builduje UV + `remapU` (~50 ř. zbytečný compute) a `getRampGeom` ho stripuje v clone. Drobný KISS dluh. **Příště sez. 35:** **G1** UI slider `RELIEF_AMPLITUDE` clamp závislý na `MIN(sizeX, sizeZ)` (quick win, jednoduchá DOM změna v `#terrainctrl`). **G2** (`world.LATITUDE`/`CLIMATE` atribut) + **G3** (climate-driven biome) jsou DD-scale → vlastní topic branches v dalších sezeních. Sub-prah: ExtrudeGeom pro rampy, slider DAY sync, `.glb` import pipeline. **Sez. 33:** **IDEAS/TODO pruning (13/12→0/12) + DD-39 atmospheric lerping + DD-40 LAMP/SpotLight + cleanup.** Single-instance na main. **Pruning** (prah překročen po sez. 32): TODO drop WCUBES/INVISIBLE/CCUBES typizace (self-protiřečící), TODO add `DAY-cycle` sekce (Sky/ambient lerping, Slider DAY sync). IDEAS Voidspan ~30ř → 3ř, Performance ~25ř → ~7ř, SEASON dostal `→ TODO` značku. **Scene cleanup**: GridHelper + AxesHelper + ShadowMaterial ground plane smazány (stíny visely v prázdnotě po DD-32 wipe). **DD-39 atmospheric lerping** — `updateAtmosphere()` třetí konzument `world.DAY` (po `updateSun()`/`updateWorldTime`), per-frame lerp `scene.background.color` (lerpColors `_skyNight`↔`_skyDay`) + `sceneFog.color` (copy background) + `ambientLight.intensity` (lerp `AMBIENT_NIGHT`↔`AMBIENT_DAY`). Driver `daylight = max(0, sin(2π·DAY))` izomorfně s `updateSun()` intensity (DRY). `_skyDay = 0x1a1a2e`, `_skyNight = 0x000000` (iter. 0x05080f → 0x010207 → 0x000000 dle user feedback *„ještě tmav"*), `AMBIENT_DAY = 0.15`, `AMBIENT_NIGHT = 0.005`. Initial 2 keypointy, sunset oranžový peak roadmap TODO. **DD-40 LAMP třída + SpotLight pattern** — `LAMP extends COMPOSITES` v `model.js`, builder `buildLamp(instance)` v `main.js`: sloup 2 j (BoxGeometry dark iron) + horizontální paže 0.6 j + visící stínítko (emissive 0xffaa00) + **`SpotLight`** uvnitř (intensity 5, distance 12, angle π/5, penumbra 0.4, decay 2, castShadow 512×512, bias -0.002). Pattern: `noShadow` opt-out na stínítku (kdyby cast, blokoval by vlastní světlo dolů); `SpotLight.target = Object3D` v Group (rotuje s ORIENTATION, vertikalita zachována). Iterace PointLight (360°) → user *„pod lampou je stín, udělej ji směrovou"* → SpotLight upgrade. User inspirace `.glb` PBR Street Props balíček → KISS variantu voxelově + `.glb` import pipeline TODO M8+ (otevírá Stickman integraci). Lampa instancována na `(-3, 0, 0)` pro test, pak odebrána ze scény (třída + builder + dispatch zachovány). **UI cleanup**: popisek reliefu `<div class="tc-relief-name">` → `title` atribut inputu (native tooltip, žádný extra DOM). **Příště sez. 34:** **G0** totální předělávka zobrazení bloků/ramp od textur k lowpoly barevným objektům (DD-scale), **G1** volba max Y (závislé na MIN(sizeX,sizeZ)), **G2** severní šířka/podnební pásmo (`world.LATITUDE`/`CLIMATE`), **G3** SURFACES driver-derived per G2. **Kudos! (user → AI flow):** plán+Q→Go pattern 5×: pruning (A1B A2B A3B A4B), Sky lerping (A1A A2A A3A A4A), LAMP design Q1A Q2A, SpotLight upgrade (A1A A2A). `feedback_plan_then_go` celé sezení. **Censure! (AI → AI):** první lampa s PointLight neměla pre-flight zhodnocení vlivu sloupku — user musel upozornit *„udělej ji směrovou"*. Pattern *„světelný zdroj uvnitř meshe → mesh noShadow"* měl být v plánu. **Censure! (AI → AI):** AMBIENT_NIGHT iterace 0.04 → 0.015 → 0.005, user dvakrát žádal *„ještě tmav"* — AI mohla nabídnout 0.005 hned (s warning *„noc bez lampy bude téměř černá"*). **Sez. 32:** **Audit follow-up F5/F6/F10/F11/F14 + DD-38 WORLD re-introduce.** Single-instance na main. Bod 1 (audit cleanup): F11 `_b` mrtvý scratch + `_dir` komentář; F14 RELIEF SSoT (`RELIEF_AMPLITUDE/FREQUENCY` zkráceny na 0..8, clamp+warn drží jako záměr); F10 `disposeHoverClones(root)` helper + volání ve `regenerateScene` při staleWater remove; F5 shared `_namedTextureCache` + `getNamedTexture(name)` helper — 4 call-sites (slow path + 2 atlas) přepojeny, PATH `_pathTexCache` s wrap-mode ponechán; F6 doc komentář u `_faceMaterialCache` o 3 paralelních cache (klíče, fáze pipeline, proč ne refactor); atlas dva mají 1-řádkový odkaz. Bod 2 (BatchedMesh) user volba — AI oponoval (diminishing returns po DD-37, r160→r167+ wide risk surface, hover pipeline čerstvá ze sez. 31), user přijal A1 = 1+3 (audit + sun anim). Bod 3 (DD-38): `%THINK` zvážil architekturu (re-introduce WORLD bez X/Y/Z, DAY+DAY_SPEED, math sferická dráha s 30° náklonem); 4 mikro-Q (atributy A=DAY+SPEED, default speed A=0 paused, intensity lerp A=ano noc tmavá, UI A=slidery v #settings). Implementace: `WORLD extends OBJECTS` v `model.js` +24 ř.; `main.js` instance + `window.world` expose + `SUN_BASE_INTENSITY`/`SUN_DISTANCE = √(100+64+100) ≈ 16.25`/`SUN_TILT = π/6` konstanty; `updateWorldTime(dt)` (skip při speed=0, `% 1` modulo s neg-safe wrap); `updateSun()` math `sun.position.set(cos·dist, sin·dist·cos(tilt), sin·dist·sin(tilt))` + intensity `0.8 * max(0, sin)` + sun mesh follow + `_sunUserVisible && y > 0` visibility; render loop `updateAnimations → updateWorldTime → updateSun → updateBubbleTails`. Settings API rozšířeno `setDay`/`setDaySpeed`, `setSun` přepracován na `_sunUserVisible` flag. `#settings` panel +2 slidery + CSS `.slider-row` izomorfně s `#terrainctrl .tc-row`. Wire script `input` event s `toFixed(2/3)` label update. **DD-38** zapsán: kontext (DD-29 zaniknul sez. 29 F4 audit, WORLD bez konzumenta po DD-32 wipe tree_sway), rozhodnutí (atributy + math s tilt + UI), důvody (DAY = první časový atribut mimo TIME singleton, sun direction render-derived), známá omezení (sklon fixed bez SEASON, sky/ambient konstantní, slider DAY nesync při auto-advance, shadow frustum reactive jen na size). User test OK. **Kudos! (user → AI flow):** plán+Q → Go pattern třikrát (bod 1 strukturovaný 5 task list, F6 = doc komentář volba KISS, %THINK + 4 Q pro bod 3). **Censure! (AI → user):** návrh 1+2 (BatchedMesh) byl rozumný k oponování — sez. 32 by se naplnil refactorem r160→r167+ s marginal payoff. AI držela 80/20 oponování per CLAUDE.md tonu. **Censure! (AI → AI):** Slider DAY nesync je known UX limitation, mohl být zachycen v Q před implementací (např. Q5 — slider periodic sync z value vs. statický). Nakonec zapsán jako známé omezení v DD-38 — DDocs OK ale Q5 by ušetřil "test → discovery". **Sez. 31:** **InstancedMesh refactor (DD-37) + sun mesh + post-process (DOF/fog) + settings panel.** Single-instance na main (KISS user volba Q3=B). `%THINK` zvážil 4 alternativy (InstancedMesh / BatchedMesh r167+ / web worker generace / mesh merge per cell) → InstancedMesh stable A. **6 Editů main.js core** (`_terrainBatches: Map<key, InstancedMesh>` + `createTerrainBatch`/`pushInstanceToBatch` helpery, `spawnTerrain` 3-pass count→allocate→fill, `regenerateScene` batch-dispose, `meshByInstance` discriminated union `{batch,idx}|Object3D`, `setHoverHighlight` batch case + albedo overbright, `resolveInstanceFromHit` raycaster). **Hover regrese kompenzace**: emissive boost (DD-25) nepřenositelný do batche bez custom shader, `HOVER_TINT_COLOR=(1.6,0.8,0.2)` overbright trik (`Float32Array` v `InstancedBufferAttribute` nemá clamp → values>1.0 = albedo overbright, sytá oranžová). User po prvním testu *„není patrný"* → sytost zvýšena z (1,1,0.55) na (1.6,0.8,0.2). **Výsledky seed 42:** 100×100 FPS 7→**104** (14.9×), calls 47642→**1016** (46.9×); 30×30 FPS 123→~140. Predikce v %THINK 150+ FPS realita 104 (GPU bound s 549k tri). **DD-37** zapsán. **Slepá ulička** — Stage 2 TDRAMP→TTRAMP refactor `terrain.js` (user feedback *„na -7,0,9 by mělo být TTRAMP"*, analýza: 100×100 cell (-7,9) y_top=−1, highDirs[S,W], diag SW y_top=1 = +2, ne +1 → Stage 1 fail → Stage 2 L-shape TDRAMP). Vrátil Stage 2 na TTRAMP (sez. 26 raná iterace, TTRAMPS 0 new walls vs. TDRAMP 1.0). Verify 100×100 seed 42: TDRAMP 848→783, TTRAMPS 548→613 (+65 cells). User vizuální verdict *„špatně, revert"* → revert. **Sun mesh** — drobná bílá koule (Q1=A realistická, radius 1.5, `sun.position × 5` = (-50,40,50)) `MeshBasicMaterial` unlit s `fog: false` override. Q2=A statická, později DAY/SUN_ANGLE reactive. **Atmospheric fog + DOF** (BokehPass) — user volba Q1=C obojí, Q2=A center focus. `scene.fog = new THREE.Fog(SKY_COLOR, 30, 120)` lineární mlha matchne barvu pozadí. `EffectComposer` + 3 passy (`RenderPass` → `BokehPass` focus dynamic / aperture 0.0005 / maxblur 0.005 pixel-art friendly → `OutputPass` sRGB color-space). Render loop `composer.render()` místo `renderer.render()`. Resize composer.setSize. **Settings panel** `#settings` levý dolní roh (izomorfně s #hud/#perfhud/#terrainctrl coral header + tyrkysový accent), 3 checkbox toggles (DOF/Fog/Slunce, default ON). `window.settings = { setDOF, setFog, setSun }` API + wire script `change` event. **Cleanup** `lastHoveredMesh` dead code smazán (psáno ale nečteno po refactoru). **Cadence reset:** AUDIT:CODE 2/8, AUDIT:DOCS 2/10, IDEAS/TODO pruning 12/12. **Kudos! (user):** plán+Q→Go pattern několikrát (InstancedMesh A1A A2B A3B, sun A1A A2A, DOF A1C A2A, settings A1C A2A) — `feedback_plan_then_go` v praxi. Realistická vizuální revize ramp refactoru (1 minute vs. ego). **Censure! (AI → AI):** %BEGIN navrhl Stage 2 TDRAMP→TTRAMP bez pre-flight vizuálního zhodnocení (per `feedback_subagent_verify` měl ukázat side-by-side render, ne jen counts). Implementace 2 Edity + Node verify, ale ne render diff — user musel rozhodovat *po* commit-ready stavu. **Sez. 30:** **Rampy atlas refactor (TRRAMPS/TTRAMPS/TDRAMP) + F9/F12 fix + size 100×100 unlock.** Single-instance na main bez topic branche (user volba). Začalo PROMPTS.md `%END` (3.) Git update — topic-branch workflow + `--no-ff` merge (governance dluh sez. 27, sub-prah). Pak rampy atlas izomorfně s DD-36: 3× IIFE geom transform na atlas-UV (1/N-tice U-axis, single material, no groups), `addQuad/addTri` helpers `materialIndex` → `faceIdx` přejmenovány + `remapU` inline; generický `getRampAtlasMaterial(type, surface)` DRY napříč 3 typy, klíč `${type}_${surface}`, `RAMP_ATLAS_SPECS` per typ keys+texTable; `RAMP_SURFACE_FROM_KIND` lookup (instance.KIND `ramp_grass` → `"grass"`); slow path `faceMaterialFor` pro rampy odstraněn (user volba — žádný producer non-fixed surfaces); TTUNELS skipnut (žádný producer, model existuje od sez. 16 ale terrain ho negeneruje). **Výsledky 30×30 seed 42:** FPS 92 → **123** (+34 %), calls ~5050 → 4290 (−14 %). Odhad TODO ~17 % incremental → reálných 14 % matchne. **F9** (sez. 29 follow-up): perf HUD `mat` = součet 3 cache místo jen `_faceMaterialCache.size` (po atlas refactoru by jinak ukazovala 0); reálně 7 @ 30×30 (4 TCUBES + 3 ramp atlas). **F12** (sez. 29 kosmetické): `updateShadowFrustum(maxDim)` helper derivuje `half = ceil(maxDim/2) + 4`, volaný z `spawnTerrain` po každém regen; module-load default ±8 zachován jako fallback. **Size 100×100 unlock** — user žádost, slidery `tc-size-x` + `tc-size-z` max 30 → 100, žádná `terrain.js` úprava (open-ended). User stress test 100×100: FPS **7**, calls **47642**, tri **549k**, `geom: 8`, `mat: 7`. **Atlas pipeline exhausted** — sdílení optimální (8 geom + 7 mat constant), ale 1 Mesh = 1 draw call neumí překročit. Bottleneck CPU draw call validation. **Roadmap:** InstancedMesh per atlas material (~13 batchů celkem) → ~13 calls → predikce **~150+ FPS** @ 100×100. **2 commity:** `2f6ac79` (rampy atlas + F9 + PROMPTS.md, +143/−67) + sez. 30 close (size 100 + F12 + docs). **Kudos! (user → AI flow):** plán+Q→Go pattern dvakrát (rampy atlas Q3 + 100×100 bez Q). Realistický odhad FPS 100×100 (lineární ~11) blízko reálných 7, drift dán CPU/GPU bound switchem nečekaný. **Censure! (AI → AI):** %BEGIN nepoložil pre-flight ramp atlas otázku „kolik unique combos a kolik draw calls per ramp" — bez té analýzy by Q1/Q2/Q3 byly hypotetické. **Sez. 29:** **`%AUDIT:CODE` + F1-F4 cleanup** (`feat/audit-29-cleanup` větev). Pre-flight TCUBES atlas vizuální parita ✓ (user *parita OK*, 2 podotázky o tile pattern a slow-path divergence dokumentované). 3 paralelní auditní agenti (terminologie/DD/struktura, kvalita/dead code, struktura/dead files) → 36 raw nálezů, po dedup **14 silných**: 4 KRITICKÉ + 6 DOPORUČENÉ + 4 KOSMETICKÉ. User volba (C) všechny kritické v jednom rolu + AskUserQuestion na F3/F4 dispozici → smazat oba. **F4 smaz `WORLD`** (singleton bez konzumenta po DD-32 wipe `tree_sway` + `productionTick`): `model.js` −33 ř., `main.js` −7 ř. + `window.world` expose. **F3 smaz `VOXEL_MODEL` infra** (dormant po sez. 25 wipe): `model.js` −33 ř. (VOXEL_MODEL třída), `main.js` −67 ř. (OBJLoader/MTLLoader importy, `buildVoxelModel`, `createCompositeFor` dispatch, COMPOSITES dispatch v `createMeshFor`), `tools/` (3 mjs, −385 ř.), `assets/` (2 .vox). **F1 GLOSSARY rewrite** sez. 22 → sez. 28: smazány factory toy sekce (FACILITY rodina, RESOURCES_DEF/RECIPES_DEF/FACILITY_DEF, Event Log, tick model), pixel-voxel COMPOSITES (TREE/GRASS_TUFT/ROCK_PIXEL/LOG), VOXEL_MODEL pipeline, MagicaVoxel workflow, shape × surface separation, WORLD sekce; přidán Terrain generator sekce (parametry, biome kindy, ramp smoothing), DD-36 atlas pipeline detail, `:sand` do `:named-texture` list. **F2 DD-36 napsán** — TCUBES atlas pipeline jako architektonický záznam (sez. 28 perf refactor neměl DD, agent A nález #4 doporučil zápis). Rozhodnutí: shared geom + per-kind atlas mat, fast/slow path dispatch, `_faceMaterialCache` memoizace, perf HUD permanent. **README + CLAUDE.md update**: hierarchie modelu bez COMPOSITES potomka + WORLD, milestones DD-33..36, plán otevřené body, Key Files bez `tools/`. **Diff stat:** 11 souborů, +166/−743 = **−577 ř.** celkem. **Topic branch `feat/audit-29-cleanup`** per sez. 21+27 workflow. Audit cadence reset 0/8 + 0/10. **TODO follow-up:** F5 (atlas/cache divergence), F6 (paralelní cache izomorfismus), F9 (perf HUD metrika), F10 (hover-clone leak), F11/F12/F14 kosmetické. **Kudos! (user → AI flow):** plán+Q→C+sub-decisions→Go pattern (`feedback_plan_then_go` v praxi). Rychlý batch 4 kritických nálezů v jednom rolu. **Censure! (AI → AI):** Agent A v auditu tvrdil „TDRAMP chybí v GLOSSARY", ale TDRAMP tam byl (řádek 21) — nedostatečná kontrola agent reportu před prezentací user. **Sez. 28:** **TCUBES atlas refactor** (`feat/terrain-perf`). %THINK performance ze sez. 27 priority: diagnose-first vs. rovný refactor. Doporučeno diagnose. **3 instrumenty:** `faceMaterialFor` memoizace (Map cache, ~16k allocs → 4 unique), `console.time` v `spawnTerrain` (3 fáze), perf HUD pravý horní roh (FPS + `renderer.info`, throttle 1×/s). **Baseline 30×30 odhalil:** FPS 15, calls 25k, `tri/call ≈ 2` = smoking gun (`material[6]` rozsekl mesh na 6 material groups = 6× draw calls per box). Generace 2.5ms (5 % regen) = worker dead. **Atlas refactor:** `getSharedAtlasBoxGeom` (1 BoxGeometry pro všechny TCUBES s UVs přepsanými na 1/6-tice atlasu) + `getTcubesKindMaterial(kind)` (4 lazy atlases = 96×16 px canvas, 6 facelets slepené horizontálně, `NearestFilter` + `SRGBColorSpace`) + `createTCubeFor` fast/slow path dispatch. **Výsledek 30×30:** FPS 15 → **92** (6.1×), calls 25k → ~5k (5×), regen 43ms → 9ms (4.8×), `[Violation]` rAF handlers pryč. 20×20 FPS 32 → 142, 10×10 FPS 175 (parita). Geom z ~5500 → 8 (sdílená box geom). **Rampy atlas odloženo** sez. 29 (256 rampů @ 30×30 = ~17 % incremental, diminishing return; 4× repeat pattern přes TRRAMPS/TTRAMPS/TDRAMP/TTUNELS). **Žádný DD** (perf optimalizace, ne arch). **Censure! (AI → AI):** `%BEGIN` ukázal pull 9 commitů, ale propásl signál „2 nové remote větve" v `git fetch` výstupu — user musel hlásit *„na druhém stroji vidím něco jiného"*. Plus Q2 v %THINK bez explicit 1/2 labels = user „2" bylo dvojznačné. **Kudos! (user → AI flow):** plán+Q→Jasně→Go, 2 round-tripy, žádné prokrastinování (`feedback_plan_then_go` memory v praxi). Diff stat: main.js +135/−25, index.html +30/−0. **Sez. 27:** **Merge `feat/terrain` → `main`** (`b1295ea`, --no-ff, 12 files +1557/−1709, `src/terrain.js` +411 ř.). Žádný kód. `%BEGIN` cadence: `%AUDIT:CODE` 8/8 ✓, `%AUDIT:DOCS` 10/10 ✓, performance 30×30 priorita ze sez. 26. User detekoval 7 nepushnutých commitů (`d027001`..`dec4d67`) v `main..feat/terrain` — topic branch byla pushnutá do `origin/feat/terrain`, ale merge nikdy. Tři varianty (A merge teď + nová `feat/terrain-perf` / B pokračovat dokud performance / C + TODO PROMPTS update); user (A). Žádný DD. **Censure! (AI → AI):** `%END` sez. 26 nepoložil otázku merge strategie; `%BEGIN` sez. 27 neukázal `main..feat/terrain` v standardní diagnostice — user musel zachytit sám. **Censure! (PROMPTS.md):** `%END` (3.) text *„Commit na větev `main`"* zastaralý od sez. 21 (DD-30 zavedl topic branches `feat/factory`+`feat/terrain`). TODO update PROMPTS.md `%END` o topic-branch workflow. **Kudos! (user):** rychlá detekce zachránila multi-instance discipline (přechod hejna → mrkla by jinak pullnul main bez terrain MVP). Příště sez. 28 (mrkla): `feat/terrain-perf` + performance %THINK.
- [2026-05-12](diary/2026-05-12.md) — **Sez. 26:** **DD-32 Fáze 3 + ramp smoothing layer** (DD-33+34+35). **UI panel `#terrainctrl`** (220 ř. inline + 30 ř. `regenerateScene`): pravý dolní roh, slidery size sx/sz (3..30), relief (0..10) s názvy stupňů, 4 surface (auto-normalize), seed input. Trigger `change` event (rozhodnutí po `%THINK`: `input` 60Hz drag = 30000+ mesh ops/s catastrofické, on-click button = friction pro experimenty, debounce = invisible mode; `change` release dragy plynule, regen 1× per dotažení). Auto-normalize: programmatic `input.value = …` HTML5 spec neemituje event → no loop. **Ramp smoothing 9-iter empirická validace přes user feedback:** (1) MVP TRRAMPS edge + TTRAMPS corner naive 3-cell peak → user Censure EDGE_ORIENT prohozené o 180° + CORNER_ORIENT klíč „NE"→„EN" (sorted alfa bug). (2) User: *„TTRAMPS apex = bod, ne hrana — geometricky nepřístupný"* — refactor: TRRAMPS greedy criticality, TTRAMPS jen isolated diag peak, compatibility filter (TRRAMPS A→B drop pokud B s jinou orient = bok rampy). (3) User návrh nového typu **TDRAMP (DoubleTriangle)** — čtvercová podstava + trojúhelníková horní podstava + lomená rampa 2 trojúhelníky. AI mikro-`%THINK` geometrie 7 vrcholů / 7 faces / 5 material groups (SLOPE+TOP+BOTTOM+WALL_FULL+WALL_TRI), severská konvence pro grass. **DD-35** založeno. (4) TDRAMP orient bug +90°, fix `TDRAMP_PEAK_ORIENT = { EN: 0, ES: 90, SW: 180, NW: 270 }`. (5) User: *„TDRAMP patř ještě na tři místa: (1,4), (2,2), (2,-2)"* — analýza ukázala **L-shape** topologii (2 sousední direct vyšší **bez** diag peak); refactor **2-stage detekce** (Stage 1: 3-cell convex peak s diag, Stage 2: plain L-shape), prio rozhodne. Plus (0,3) jako 3-cell peak NW (algoritmus skipl kvůli highDirs.length=3 ≠ 2). 7 TDRAMP. (6) **Dedup shared apex** pár (1,1)+(2,2) v (1.5,1.5,1.5) a (1,-1)+(2,-2) v (1.5,1.5,-1.5) — AI implementoval dedup, loser fallback na TRRAMPS edge. User: *„Na 1,1,-1 a 1,1,1 se hodí více TTRAMP — zakryjí boky 2,1,-1 a 2,1,1 a nevytváří další obnažené boky"* → dedup loser fallback změněn z TRRAMPS edge na **TTRAMPS apex k L-corneru**. (7) AI bilance pro user: kvantitativní tabulka odkrytých svislých ploch per ramp type na (1,-1) — všechny 3 typy redukují baseline 2.0 → 1.0 (=−1.0 stejně), ale distribuce odlišná (TTRAMPS 0 new walls, TRRAMPS 0.5 new, TDRAMP 1.0 new). (8) User finální: *„TDRAMP na místech jako 1,1,-1 vypadat nejlépe"* — odstranění dedup, všechny L-shape cells dostanou TDRAMP, double-tent apex pár OK. **Final stat seed 42 default:** 20 TRRAMPS + 7 TTRAMPS + 7 TDRAMP = 34 ramps, 92% pokrytí přístupů. **DD-33** ramp smoothing layer, **DD-34** orient mapování (centralizace po 3 oddělených bug iteracích), **DD-35** TDRAMP class. **Censure! (AI → AI):** 3 oddělené orient bugy — mělo se chytit single zdrojový derivation + verifikace ne-default orient před commitem. Plus dedup pre-emptive bez ukázání stavu bez ní. **Kudos! (user feedback loop):** 9 iterací empirickou validací produktivnější než AI single-shot design; user identifikoval TTRAMPS apex bodový fakt + navrhl TDRAMP. **User performance feedback:** *„30×30 vykreslování pomalé, poladíme později"* — sez. 27 priorita (instancing / BatchedMesh / mesh merge / web worker). **Sez. 25:** **DD-32 implementace end-to-end (`feat/terrain` větev).** User pivot z factory toy zpět k procedurálnímu terrain sandbox. `%THINK` o `generateTerrain` API (4 mikro-rozhodnutí: relief 0..1 spojitě → user upgrade na 0..10 integer s 11 pojmenovanými stupni Flat/Level/Gently undulating/Rolling/Hilly/Uneven/Rugged/Craggy/Mountainous/Heavily dissected/Alpine; surfaces biome klastry; náhrada SCENE_LAYOUT; voda jako plane), zapsáno do TODO. Pak full implementace 3 fází: **Fáze 0 kotvení** (`d027001`) — DD-32 (factory toy → terrain pivot, DD-30/31 immutable jako historie), README/CLAUDE.md identitní update, merge `feat/factory` → `main --no-ff`, nová `feat/terrain`. **Fáze 1 wipe** (`010d7ac`) — `model.js` 705 → 450 ř., `main.js` 3252 → 1981 ř. (−1271 ř., smazán FACTORY TOY ENGINE + TREE/GRASS/ROCK/LOG buildery + severská dioráma + spawn helpery + animateTreeSway + TIME_SCALE slider listener + render loop ticky), `index.html` 161 → 85 ř. **Celkem −1602 ř. (−39 %)** z 4118 na 2516. **Fáze 2 generátor MVP** (`845c0d2`) — nový `src/terrain.js` 173 řádků: mulberry32 + makeValueNoise (grid sampling + bilineární smoothstep + wrap-around) + `generateTerrain({ size, relief, surfaces, seed })` → `{ blocks, water }` (heightmap dle relief amp×freq lookup; biome map low-freq noise → sort+exact-match thresholds = souvislé klastry; Y modifier sand−1/water−2; sloupcové vyplnění top voxel dle biome + dirt middle + stone yBottom; water cells → 1×1 plane(y) na y_top+0.55); validace fail-fast; graceful degradation relief 9..10 → clamp 8 + warn. `main.js` integrace: `:sand` textura, `BLOCK_TEXTURES` per kind, `createBlock` + `createWaterPlane` helpery, `TERRAIN_DEFAULTS` (10×10, relief 3, grass80/stone10/sand5/water5, seed 42), `buildScene` volá generator + spawne 321 bloků + 5 water planes. Smoke test Node ESM OK; node --check OK; server :8000 vrací 200; user browser test *„rovinatá krajina s jedním kaňonem s vodou na dně"* — funkce (biome klastrování + water Y−2 = depression), ne bug. Příště sez. 26: Fáze 3 UI panel `#terrainctrl` (relief slider 0..10, size sx/sz, 4 surface slidery, seed, Generate button, regenerateScene funkce). **Kudos:** AskUserQuestion před wipe (4 otázky) prevent silent pivot; user upgrade reliéfové škály z 0..1 na 11-stupňovou pojmenovanou škálu sémanticky bohatší než AI původní návrh. **Censure! (AI → paralelní AI sez. 24):** paralelní instance si přivlastnila „Sez. 24" pro meta-diskusi a moji práci popsala jako sub-event s nepřesnostmi (terrain.js 210 ř. místo 173, main.js wipe −301 ř. místo −1271). **Sez. 24 (paralelní instance, meta + multi-instance discipline):** Tato instance meta-sezení o **multi-instance discipline** — paralelní instance Claude nad stejným repem hrozí (1) file-level race v pracovním stromu, (2) `%BEGIN` stash race (paralelní `git stash`/`pull`/`pop` se rozpadnou), (3) docs race (DIARY/TODO/MEMORY Write přepíše bez merge), (4) session numbering collision (`## Sezení 24` ×2), (5) port 8000 (bezpečné). Doporučení: **`git worktree add ../TheCubes-<topic> feat/<topic>`** per instance (nejčistší — vlastní strom + větev, konflikty až mergem); kompromis „dirigent" pattern (jedna instance docs + %BEGIN/%END + commits, ostatní jen kód v izolovaném souboru); minimum: single instance pro %BEGIN/%END a Edit operace. **Live demonstrace:** mezi `%BEGIN` a `%END` paralelní instance commitla DD-32 Fáze 1 (`010d7ac` wipe — model.js -258 ř., main.js -301 ř.; smazány FACILITY/registry/engine/HUD/severská dioráma/COMPOSITES/tree_sway) + Fáze 2 (`845c0d2` `src/terrain.js` 210 ř. — value-noise heightmap + biome map MVP). Mezi dvěma `git status` v této instanci se strom přepnul z `M main.js + ?? terrain.js` → `clean` (přesně diskutovaný case naživo). Tahle instance: žádný kód, jen docs a `git push -u origin feat/terrain` pro koordinaci. **Žádný DD** (workflow recommendation, kandidát na sekci „Multi-instance discipline" v globálním `~/.claude/CLAUDE.md` při příštím `%CALIBRATE`). **Censure! (AI → AI):** `%BEGIN` měl chytit signál „uncommitted -568 ř. wipe, který tahle instance nezačala" → existuje paralelní instance → varovat *před* návrhem programu, ne až po user otázce. **Censure! (paralelní AI → AI):** wipe + terrain MVP commit bez upstream push + bez varování v diáři → ostatní instance nevěděly. Příště sez. 25: `buildScene` integrace + HUD panel `#terrainctrl` (relief/size/surface/seed). **Sez. 23:** **Steady-state polish (bod #3 z Příště sez. 22).** Forest rate 0.5 → 1.0 logs/s — 1:1 matchne sawmill recipe (1 log/cyklus × 1.0/s), source-starve a PAUS/RSUM oscilace pryč. Conveyor THROUGHPUT=2 ks/s zůstává nad rate. **Meta cleanup CLAUDE.md slim** (user noticed redundance — *„v lokální CLAUDE vidím navíc, co bych spíš čekal v README.md"*): diagnostika tabulkou (Project / Status / scéna / hierarchie modelu / sezení changelog patřily do README+DIARY, ne CLAUDE.md), lokální `CLAUDE.md` 150 → 32 řádků (78% redukce). Zůstává jen skutečný AI overlay: Code Style nuance (user-learner), `%THINK` bod 5 (sandbox/factory mechanika), Makra odkaz, Key Files mapa, kde najít co. **Governance rozšíření (globální `~/.claude/CLAUDE.md`):** `%CALIBRATE` sekce A o **„Role discipline řídících dokumentů"** + sub-prah nezávislý na sezení-count *„nárůst projektového CLAUDE.md o ≥ 50 % od posledního CALIBRATE"*. Plus **bariéra v `%DOCS`** — 4 explicitní pravidla, kam se *nikdy* nepíše (changelog → DIARY, status → README, DD → DESIGN_DECISIONS, CLAUDE.md jen pro AI pravidla). Důvod governance: AUDIT:DOCS cílí na content kvalitu (překlepy, broken links), zatímco role-confusion mezi řídícím a content dokumentem je process-level, doména CALIBRATE A. **Žádný nový DD** (data tweak + meta governance, ne arch rozhodnutí). **Censure! (AI → AI):** nárůst CLAUDE.md o 4 odstavce za sez. 20+21+22 měla AI registrovat při každém `%DOCS` — princip „role discipline" měla držet bez explicit pravidla; bariéra v `%DOCS` je sice systémový fix, ideálně by tu nemusela být.
- [2026-05-11](diary/2026-05-11.md) — **Sez. 22:** **Fáze B PATH transport** (`feat/factory` větev). Body 2+3+5+6 z Příště: **`PATH` rozšíření** o `SOURCE`/`SINK`/`RESOURCE`/`THROUGHPUT` (default null, backward compat se sez. 17 dekorativní cestou) — RESOURCE **explicit** (ne derived ze SOURCE — storage drží libovolné suroviny, derive by byl nejednoznačný); **`pathTick(dt)` engine** + `transferOnPath` (clamp `min(throughput*dt, source_have, sink_free)`, mutace bufferů, log DRN/PROD přes existující floor-crossing helpery = stejná 1-event-per-whole-unit granularita); render loop volá `pathTick` **po** `productionTick` (pořadí důležité: tick = nejprve produkce, pak distribuce); **boot-time validace category × KIND** v `createPathFor` (`fluid → pipeline`, `solid → conveyor` warn na neshodu); **TIME_SCALE slider** v HUD (`#simctrl` divovec s `pointer-events: auto` override, range 0..3 step 0.1, `input` listener mutuje `world.TIME_SCALE` + label `1.0×`, accent-color `#66ccdd` sladěno s evt-rsum cyan); **migrace test scény** — smazán `sawmill.BUFFER.logs = 50` pre-stock, přidány 2 conveyor PATH (forest→sawmill, sawmill→storage, throughput 2 ks/s default). Diff stat: model.js +24/−6, main.js +124/−8, index.html +26/0, docs ~+250. Test 1-4 OK: cesty viditelné, čítače rostou, eventy se logují, slider funguje live. **Odhalený polish problem** — PAUS/RSUM oscilace sawmillu při source-limited supply (forest 0.5 logs/s vs. recipe 1/s spotřeba = trvale input-starved, 30 Hz spam v event log). 3 možnosti řešení sez. 23: druhý forest (data), ladění rates (data), `setPaused` debounce (engine, min interval 200 ms) — třetí je nejčistší. **Žádný nový DD** (sez. 22 implementuje plán DD-31 ze sez. 21 dle specifikace; jediná drobná divergence — explicit RESOURCE atribut — je v rámci otevřenosti DD-31, žádná aktualizace). **Kudos!** AskUserQuestion před kódováním ušetřilo 2 round-tripy (RESOURCE design + scope). **Censure! (AI → AI):** slider šel do `#hud` aniž by ukázala konkurenční umístění (bottom panel se #eventlog jako „observer dashboard" páreček) — drobnost, ale příště systematizovat při dalších UI elementech (build mode toggle, KIND palette). **Sez. 21:** **DD-30 pivot na 3D factory-observer toy** (Voidspan inspirace, ne dogma — selektivní převzetí: Resource registry, Recipe matrix, Material gate, Event Log 4-znakové verbs; odmítnuto: 2D Belt topologie, kolonisté, Protocol, Coin, TS stack). Identita projektu se mění z „meta-sandbox" na **„model-first factory-observer toy s OOP modelem jako runtime"**. **DD-31 Resource model & FACILITY** — 6 surovin (`logs/planks/stone/gravel/water/coal`), **lokální buffery** per fasilita (model B vs. globální pool A — pro „hezký resource řetězec" je B nutné, jinak je PATH dekorativní), **fixed-KIND transformery** (izomorfie s TREE.KIND — `sawmill`/`crusher` jsou KIND, ne data atribut), PATH `KIND ∈ "conveyor"|"pipeline"` s `SOURCE`/`SINK`/`THROUGHPUT` (Phase B). Tick: **1 wall s = 1 tick**, `productionTick(dt × world.TIME_SCALE)` v render loopu, clamp `dt ≤ 0.1 s`. **Soft revize DD-29** (sez. 20): `world.RESOURCES` agregát + `world.TIME_SCALE` jako noví konzumenti — `WIND_STRENGTH` zůstává platný, po Phase D cleanup ztratí `tree_sway` a migruje do IDEAS. **Boční větev `feat/factory` fáze A** — model.js +160/−15 (RESOURCES_DEF/RECIPES_DEF/FACILITY_DEF registry + FACILITY/GENERATOR/TRANSFORMER/STORAGE třídy + WORLD rozšíření), main.js +230/−5 (FACTORY TOY ENGINE sekce: `productionTick`, `generatorTick`/`transformerTick` s material gate, `logEvent`/`renderEventLog` ring buffer 100 events/visible 5, floor-crossing PROD/DRN emit jen na whole-unit, state-change PAUS/RSUM, `aggregateResources` přepočítá `world.RESOURCES` + DOM `#res-<r>` čítače), index.html +50 (`#resources` panel 6 řádků + `#eventlog` severity-colored). Test scéna 3 fasility (forest/sawmill pre-stocked 50 logs/storage) bez PATH zatím. **Otevírací %THINK** debatoval „dopamin z koukání na vlaky" — AI 75 % souhlas s 3 výhradami (autorský loop > pasivní pohled; čitelnost > krása; nepojmenovaný pivot rizikový). User „zavalil jsem se nápady", chce **vizuálně couvnout** (Phase D queue: TREE 10 KIND, GRASS_TUFT, ROCK_PIXEL, LOG, populateNorthernScene, tree_sway, `:grass-top` proc. textura). 4-fázový TODO plán (A model+tick, B PATH transport, C editor MVP, D cleanup). Cíl projektu: **pozorování ukazatelů** (observer game, Voidspan Perpetual Observer Simulation axiom), ne win/loss. Soviet Republic resource řetězec inspirace pro Phase 2 vlnu (bricks/cement/steel + multi-input recepty). **Kudos:** strukturovaná %THINK debata před zápisem (mikro-rozhodnutí pojmenována, ne v běhu); Voidspan filter přísný (žádný drift k „přeber všechno"); User reframing pivotem z psychologie šetří cleanup. **Censure! (AI → AI):** identitní pivot měla AI nabídnout sama při „4 dříve diskutovaných směrech BUILDING/TRACK/biome/editor uvnitř sandbox identity", ne čekat na user prompt.
- [2026-05-10](diary/2026-05-10.md) — **Sez. 20:** **DD-29 WORLD singleton (Light DO).** User otevřel dvěma KISS návrhy z Glossary: sjednotit OBJECTS s CUBES (X/Y/Z na všechno) a BLOCKS s CCUBES — AI **Censure!** odmítla oba jako demix konceptů (DD-01 separace vizuální/modelová entita; CCUBES je listový potomek, ne rodič; BLOCKS drží sdílený `ORIENTATION` + `:named-textures` infrastrukturu). Místo toho přesměrováno na DO/DROP rozhodnutí o WORLD (5× v Příště, překročen stale práh). Z navrhovaných atributů (`WIND { strength, direction }`, `SUN { angle }`, `CLIMATE`, `SEASON`, `DAY`) má **jen `WIND.strength` aktivního konzumenta** — `tree_sway` má hardcoded amplitudu. `WIND.direction` by ani nešel použít (`tree_sway` je izotropní, X+Z nesoudělné fáze). **Light DO** kompromis: nová třída `WORLD extends OBJECTS` (bez X/Y/Z — demonstruje DD-01) s `WIND_STRENGTH = 1.0`, instance `world` v `main.js`, dev exposure `window.world`, `tree_sway` násobí amplitudu. **DD-29** zafixoval politiku „atribut přibude jen s živým konzumentem" — 5 odložených atributů (`SUN_ANGLE`, `CLIMATE`, `SEASON`, `DAY`, `WIND_DIRECTION`) v IDEAS.md s konzumenty. Plochá struktura (`WIND_STRENGTH`, ne nested `WIND.strength`) — KISS pro infotip, refaktor na nested až při ≥3 atributech kategorie. Test v konzoli: `world.WIND_STRENGTH = 0/1/2` → bezvětří/default/bouře. **Sez. 19:** Meta-sezení (žádný kód). **`docs/DONE.md` vytvořen** — 215 řádků archiv hotových úkolů (M1–M7 kompletně, M8+ Hotové, Sezení 14–18 + „Příště — Hotové" podsekce). `docs/TODO.md` zkrácen z 240 → 25 řádků (jen `[ ]` z M8+ + aktivní „Sez. 18 → 19 Příště"). DROP položky `~~strikethrough~~` v DONE s anotací DD/cleanup důvodu. **Prefix metapromptů `@` → `%`** — důvod: `@` v Claude Code otevírá file-picker (UI rušení). Diskuse oponovala „stačí velká písmena" (false-positive trigger v Czech prose: „uprav DOCS", „udělej AUDIT funkce"). Volný `%`: Shift+5, vizuálně výrazný, nekoliduje s `AUDIT:CODE` dvojtečkou (na rozdíl od `:` prefixu). Bulk PowerShell rename (`[System.IO.File]::WriteAllText` + UTF8 NoBOM kvůli diakritikám): **138 souborů z 1770 zkontrolovaných** napříč 7 projekty (TheCubes, PocketStory, Stickman, Voidspan, Pixelodynamics, Isometric, JePEK) + globální `~/.claude/CLAUDE.md` + `~/.claude/PROMPTS.md` + 5 projektových memory adresářů. Token sada: `%AUDIT:CODE`, `%AUDIT:DOCS`, `%BEGIN`, `%END`, `%THINK`, `%DOCS`, `%CALIBRATE`. Verifikace grepem — 0 zbývajících `@`-prefixovaných maker. Žádný projektový DD (operační změna mimo model). README.md doplněn o `docs/DONE.md`. **Sez. 18:** **`%AUDIT:CODE`** — 12 nálezů (1 kritický, 5 doporučených, 4 kosmetické) napříč `src/` (3413 řádků) + GLOSSARY + DESIGN_DECISIONS + struktura. **DD-28 sjednocená Y konvence** — dvě sémantiky napříč CUBES potomky: BLOCKS (TCUBES, TRRAMPS, TTRAMPS, TTUNELS) = grid Y voxelu (mesh center, snap-to-int), VOXEL_MODEL + pixel-voxel COMPOSITES (TREE, GRASS_TUFT, ROCK_PIXEL, LOG) = world Y surface (mesh bottom). Předtím tři konvence — pixel-voxel měl group origin posunutý o 0.5 nad surface kvůli `treeVoxel` lokálnímu offsetu `-0.5 + (gy + 0.5) * TREE_PX`. Migrace: `treeVoxel` zjednodušen na `(gy + 0.5) * TREE_PX`, populate `instY = t.y + 0.5` (cancellation = žádná vizuální regrese). DD-22 ponechán v platnosti jako sub-konvence. **F2** zombie `GRASS_TUFT.tall` smazán (default → `"short"`), **F3** docstringy GRASS_TUFT/ROCK_PIXEL/VOXEL_MODEL aktualizovány, **F4** GLOSSARY hlavička, **F5** matoucí komentář v `buildScene` smazán, **F6** `temp/` cleanup + `.gitignore`, **F7** historické komentáře zkráceny, **F8/F9** asset templates dokumentace, **F10** `castShadow` duplicita v `treeVoxel`. Test OK ihned po fix-batch — cancellation Y offset držela vizuální stav.
- [2026-05-06](diary/2026-05-06.md) — **Sez. 17:** **DD-26 sjednocená `ORIENTATION`** napříč BLOCKS i COMPOSITES (float [0, 360) ve stupních; engine `* π/180`). Refaktor: BLOCKS enum 0..3 → stupně (migrace `ramp_0` z 1 → 90), `LOG/VOXEL_MODEL.ROTATION_Y` (rad) smazán, `BLOCKS` + `COMPOSITES` base třídy explicit constructor s default 0. Populate dá random ORIENTATION každé dekoraci → organičtější vzhled. **DD-27 PATH** (1. potomek vrstvy 3 LINES) — `extends CUBES` s `KIND` + `POINTS`, engine Catmull-Rom curve + plochý strip mesh (BufferGeometry, šířka 0.5 j, Y offset +0.005). `:path-dirt` textura — kropenatý šum 240 záplat 1-2 px šedých kamínků. Cesta z tunnel_0 (-3.5, -3) ven přes východní hranu (4.5, 1) — 5 bodů (rovný start podél X, inflexní bod, rovný konec podél X) využívajících Three.js reflection fallback tangenty (P[1] − P[0] když P[0].Z = P[1].Z). `pathOccupiedCells` integruje s populate (cesta blokuje dekorace). **Procedurální severská dekorace:** 3 nové třídy COMPOSITES — `GRASS_TUFT` (micro/short/fern), `ROCK_PIXEL` (micro/small/medium/mossy), `LOG` (stump/birch/pine). `populateNorthernScene(scene, pathBlocked)` — mulberry32(42) deterministická RNG, severský mix (smrk dominuje, bříza akcent, suchý strom občas) + micro vrstva 1×1×1 voxely. Zjednodušení **grass blok** (boky/spodek `:dirt` místo `:grass-side` 14+2 px gradient strip) — pravidlo BLOCKS rodiny „vrch grass, jinak dirt" napříč grass blok / TRRAMPS / TTRAMPS / TTUNELS; smazán `:grass-side` factory + `makeGrassSideTexture` + `GRASS_STRIP_PX`. **Voda** (TCUBES `:water-top` + `:water-side`, transparent + opacity 0.7, `noShadow` heuristika v `createTCubeFor`) implementována jako 2×2 jezírko a po user „nejsem si jistý" smazána (test cleanup ~70 řádků). **Q/E camera rotation fix** — explicitní `_kbWorldY = (0,1,0)` místo `camera.up` + `camera.lookAt(target)` po mutaci pozice (eliminuje shift). **`buildSceneTwo` → `buildScene`** rename po sez. 15 cleanup propásnut (censure). **Y konvence fix** v populate — pixel-voxel COMPOSITES `instance.Y = t.y + 1` (ne +0.5 z DD-22 pro VOXEL_MODEL — DD-22 popisuje auto-center loader, pixel větev má vlastní `treeVoxel` offset -0.5; otevřený koncepční dluh). Infotip POINTS uzávorkován `(x,y,z) → (x,y,z) → ...`. **Sez. 16:** **DD-25 4-vrstvá taxonomie scény** (Bloky / Voxely / Linie / Objekty) — reviduje DD-23. **`BLOCKS` abstract rodina** pod CUBES: TCUBES + nové **TRRAMPS** (trojboký hranol = klín, 5 faces), **TTRAMPS** (trojboký jehlan = trirectangular tetrahedron, 4 faces), **TTUNELS** (klenutý tunel skrz 1C blok, 4 faces, „kvádr − válec − kvádr"). Procedurální `BufferGeometry` v engine s per-face material groups + `faceMaterialFor` dispatch. **`ROTATION_Y` → `ORIENTATION` enum** (integer 0..3 = 90° CCW rotace). **Hover highlight** přepis ze žlutých edge linií na **emissive boost na celém objektu** s lazy clone-on-first-hover materiálů — sdílené materials (TREE `_treeMatCache`) zůstávají nedotčené. **VOXEL_MODEL `tunel-grass` + `ramp-grass` instance smazány** a nahrazeny TTUNELS (vstupy pro vlak) + TRRAMPS. Asset soubory `tunel-grass.*`, `ramp-grass.*`, `ramp-triangle-grass.*` smazány; v `assets/` zbývá jen `cube-grass.vox` (template DD-24). **`%AUDIT:DOCS`** — 7 nálezů (F1 SCENE2.md smazán, F2 GLOSSARY rewrite, F3 README Status, F4 IDEAS anotace, F5 DD-22 typo, F6 DD setříděno, F7 v F2). **Sez. 15:** Velký pivot **DD-23 „Kostičky = jen voxely"** — smazány všechny non-voxel třídy (BALLOON, HOUSE, CLOUD, ROCK, TUNNEL_ARCH, WAREHOUSE, TRAIN), Scéna 1 + scene switcher, LIT system, balloon_bob, classic TREE varianta, cylinderBetween helpers, click handler. **~720 řádků smazáno** (`main.js` 2596 → 1919). Před tím: kompletní pixel-voxel TREE.KIND systém s 10 druhy (`spruce`/`oak`/`birch`/`palm`/`bush`/`cypress`/`willow`/`bonsai`/`dead`/`maple`) + dispatch přes `TREE_BUILDERS`. **Polymorfní `tree_sway`** (classic větev smazána, pixel větev = per-children height-weighted s desync fází + nesoudělnými periody, amplituda 0.16). **DD-22** — pevné měřítko (1 TC voxel = 1 m, 1 MV voxel = 1/16 TC = 6.25 cm, scale 0.625 default) + Y konvence (`instance.Y = gy + 0.5`). **DD-24** — shape × surface separation (1 shape s abstract paletou 4 indexů × N surfaces JSON → pre-built kombinace), sjednocení pojmenování `<shape>-<surface>`. Rename: `grass-cube` → `cube-grass`, `grass-ramp.*` → `ramp-grass.*` (vč. `.mtl` `map_Kd` reference). User import: `tunel-grass.vox` 48³ MV (3×3×3 TC) + `ramp-grass.vox` (1×1×1 TC, stoupá v −X směru, position (-4,-0.5,0) spojuje úrovně grass(-4,-1,0) → grass(-5,0,0)). Drobné: `export-grass-vox.mjs` přepsán na povrchový mapping textur (TOP grass-top, sides grass-side 14+2 px, BOTTOM dirt). **TODO bagr/jelen** debata o měřítku (postava 8 MV vysoká minimum pro válcové detaily).
- [2026-05-05](diary/2026-05-05.md) — **Sez. 14:** Velký pivot — STICKMAN se stal samostatným projektem (`./source/Stickman` vlastní layered MVC + IK + Inspector), všichni humanoidi smazáni z TheCubes (~1170 řádků, commit `7679fef`). Pak velká iterace na Scéně 2: 10×10 voxelová dioráma (`SCENE2_LAYOUT` ~145 kostek) postavena interaktivně přes builder (LMB stavět, RMB bourat, 3 typy + Export do clipboardu s pravidlem „překryté = hlína/skála") — builder pak odstraněn, layout hardcoded. Klávesnice WASD/QE/YX (W/A/S/D pan + Q/E rotate + Y/X zoom geometrický per-frame). GridHelper+AxesHelper pryč, slunce úhel 30°, shadow fix peter-panning (`normalBias` 0.02→0, PCFSoftShadowMap→PCFShadowMap). 4 nové třídy: TUNNEL_ARCH (half-torus rotated), WAREHOUSE+TRAIN (připraveny pro fáze 2/3), VOXEL_MODEL (DD-21 generická). **MagicaVoxel pipeline (DD-21):** OBJLoader+MTLLoader async, auto-center XZ + bottom snap Y, NearestFilter pixel-art. První import: 2 auta. Reverse pipeline: `tools/export-grass-vox.mjs` (TheCubes paleta → `.vox`), `export-scene-palette-vox.mjs` (12 swatch barev), `export-cars-vox.mjs` (sedan + truck simplified), `dump-png-palette.mjs` utility. **DD-21 zafixoval hybrid politiku:** parametrizované entity → procedurální COMPOSITES, statická dekorace → VOXEL_MODEL default. GLOSSARY rozšířen o sekci „Vizuální zdroje" + tabulku workflow. **Censure (uživatel → AI):** „nech voxely hranaté" argument se opíral jen o Minecraft, ignoroval izomorfismus s celkovou scénou (TREE/BALLOON/CLOUD jsou hladké). **Kudos:** fázování vlakové scény (mountain + tunnels + rails + warehouse + train + cargo) na 3 fáze podle složitosti; první VOXEL_MODEL integrace („Jsou tam!").
- [2026-04-27](diary/2026-04-27.md) — **Sez. 13:** Polish STICKMANa (WORK_POSE sign fix axe-style chop, hlava `0.18 → 0.144` + segments `(8,4) → (16,12)`, trup Z `0.24 → 0.16`, **face plane** 3 výrazy `:)` `:|` `:(` s `faceUpdaters[]` registry — třetí engine-derived vedle bubbleTails a litEntities). **Scene switcher** (URL reload `?scene=N` dispatch, `buildSceneOne`/`buildSceneTwo`, HUD tlačítka v pravém horním rohu — reload eliminuje cleanup-debt z F4 auditů). **Scéna 2 louka** (procedurální `makeGrassTexture` 256×256 s 32×32 buňkami, paleta 5 zelených + žlutá, `RepeatWrapping`, plane 10×10). **GLTF export** přes `window.exportStickman()` (dynamic import GLTFExporter z three/addons → `.glb` do Downloads). **Slepá ulička:** ranní `pose_cycle` attempt s 8 statickými pózami (stand/sit_normal/sit_turkish/kneel/lie_back/lie_stomach/lie_side/walk_in_place) + target/ease pattern (~0.4 s konvergence) — uživatel „není to hezké", celý systém odstraněn. **Pivot přes Gemini referenci:** uživatel poslal `gemini-code-...html` s vlastním pokusem na stejné zadání (3 stickmany walk/run/squat). Klíčový idiom převzat: `Math.max(0, Math.sin(...))` = jednosměrný kloub (anatomická constraint elegantně řešená matematicky). Implementovány tři gait animátory `walk_idle`, `run_idle`, `squat_lift` s pojmenovanými konstantami. SCENE2.md prompt vytvořen pro fresh AI workflow (self-contained brief, anatomická pravidla, acceptance criteria). **Censure!** (uživatel → AI) git pull skip ve `%BEGIN` recidiva po sez. 11 — pre-pull lokální `PROMPTS.md` měl starou „prozatím skip" verzi, paměťová bariéra `feedback_git_sync_mandatory` chyběla (vznikla sez. 11 na jiném stroji). Neměl jsem si všimnout `gitStatus banner` v systémovém promptu.
- [2026-04-24](diary/2026-04-24.md) — **Sez. 12:** Humanoidní varianty — **NOODLE** (plastelínová: 2× CapsuleGeometry + 4× TubeGeometry končetiny podél `CatmullRomCurve3`, walk cycle mutuje kontrolní body per-frame; paže 4 uzly s lehce pokrčeným loktem, nohy 3 uzly; `char_0001` přehozen z CHARACTER) + **STICKMAN** (blokový low-poly podle user spec: BoxGeometry trup, 8×4 SphereGeometry hlava, 6-seg CylinderGeometry končetiny, kostkové ruce/chodidla; plná 3-segmentová kostra s animovaným zápěstím/kotníkem přes rozšířený `applyWalkCycle` s `if (p.leftWrist)` backward compat; `char_0004` na jižní straně scény). DD-20 zafixoval `poseFns` dispatch (CHARACTER/NOODLE/STICKMAN sdílejí `ANIMATE` mode slot přes `group.userData.poseFns` callback mapu — animátory dispatchují přes ni, nové varianty = nová build fn + poseFns bez úpravy animátorů). Iterativní doladění NOODLE přes user fixy F1–F4 (hlava zabořená do těla, průměry na polovinu, nohy/ruce zabořené, paže 45° ven-dolů) a F1-F2 druhá vlna (ramena výš, 4 uzly s loketním ohybem). **Sez. 11:** %AUDIT:CODE po DD-18/19 (3 paralelní agenti → 25 raw findings → triáž na 7 silných), opravy F1–F6. F1: CHARACTER docstring v `model.js` zkrácen (engine detaily odstraněny, DD-11 čistota). F2: magic konstanty pojmenované (`COLLISION_RADII`, `WANDER_TIMERS`, `WALK_PARAMS`/`RUN_PARAMS`, `WORK_POSE`, `LIE_GROUND_Y_DEFAULT`) + destrukturalizace ve walk/run bloku. F3+F4: `applyLiePose` parametrizovaný `groundY` + komentář o záměrné divergenci signatury (lež je whole-body transform, ne kloubová póza). F5: `isBlocked` komentář dokumentuje first-come-first-served collision prioritu (pořadí `animators[]`, cesta k two-pass fixu pro editor fázi 2). F6: CHARACTER docstring doplněn o `sit`/`lie`/`wander`. F7 (STATE atribut pro wander substate visibility) zapsán jako kandidát DD-20. **Sezení začalo censure** — AI skipla git sync kvůli zastaralé poznámce v `PROMPTS.md`, repo bylo 2 commity pozadu (sez. 8+9 měla hotové ROCK/SPEAKER/TIMER/COUNTER, sez. 10 CHARACTER/kolize); navíc AI doporučila ROCK v plánu, aniž by věděla, že je hotový. Oprava: `PROMPTS.md` %BEGIN git sync povýšen na povinný krok + memory `feedback_git_sync_mandatory.md`. Mezitím koncepční diskuse *„nevynalézáme kolo postavičky?"* — Mixamo/Quaternius rig **nevzít** (stylová konzistence + cíl projektu + DD-11 separation), inventář vzít jako inspirační seznam budoucích `ANIMATE.kind`ů (`kneel`, `carry`, `throw`, `wave`).
- [2026-04-19](diary/2026-04-19.md) — Sez. 1: IDEA fáze + M1 kompletní. Hierarchie `OBJECTS → CUBES`, statický svět s hodinami, šachovnicová textura (DD-07), infotip panel (DD-08), osvětlení (DD-10 nahrazuje DD-09). PROMPTS.md přizpůsobeno (server v `%BEGIN`).
- [2026-04-23](diary/2026-04-23.md) — **Sez. 10:** %AUDIT:CODE fixy (F1–F4) + **CHARACTER** (DD-18: dvoudílné končetiny, ANIMATE jako „mode slot" pro statické i dynamické pózy, `walk`/`sit`/`lie` animátory) + **wander** stavový automat uvnitř animátoru (6 stavů: walk/run/stand/sit/lie/work; work = approach→perform mining/harvesting) + **2D kolizní systém** (DD-19: kruhy v XZ, dispatch-by-type radii, stop & transition bez slidingu/pathfindingu). Vedlejší fixy: edge highlight teď `depthTest=true` + `polygonOffset` (jen viditelné hrany); balloon light 3× intensity, 1.67× distance, 2×² shadow map. Sdílené pose primitives (`applyWalkCycle/SitPose/LiePose/WorkPose`, `resetCharBase`, `moveTowards`) vytaženy pro DRY mezi samostatnými animátory a wander. Tři instance: `char_0001` (Poutník modrý, wander), `char_0002` (Sedící, na severní hraně oranžové dlaždice − 2/3 stehna dozadu), `char_0003` (Poutník zelený, wander). **Sez. 9:** TIMER + BALLOON lantern (DD-17) + COUNTER + edge highlight. `TIMER { INTERVAL, ACTION }` jako první nevizuální potomek OBJECTS — engine `tickHandlers[]` + `ACTIONS` dispatch (`toggle`, `set`). `BALLOON.LIT` bool + PointLight uvnitř vaku (druhá shadow mapa vedle slunce) + emissive — engine fade watcher (exp. lerp ~0.5 s). Toggle přes TIMER nebo click na vak (oba konvergují na stejný stav). `COUNTER { VALUE, INCREMENT }` druhý nevizuální potomek — dynamický HUD řádek (createElement + textContent, XSS-safe). Edge highlight na hover (EdgesGeometry + X-ray LineSegments) jako příprava pro editor. DD-17 formalizuje diskrétní ACTION dispatch a kategorii „více ovladačů téhož stavu". **Sez. 8:** ROCK + dynamický 3D ocásek SPRITES (DD-16) + `%AUDIT:CODE` fix všech 6 findings. ROCK jako pátý COMPOSITES (shluk 5 flat-shaded icosahedronů, sdílené materiály 3 odstínů). Ocásek odstraněn z canvas textury, nahrazen tenkým 4-segmentovým jehlanem mimo sprite (`Group` sprite + tail). Atributy `SPRITES.SPEAKER` (instance ref nebo `{x,y,z}` literál, duck-typing dispatch) + `SPEAKER_OFFSET_Y` (default 0.5). `meshByInstance` map umožňuje tracking pohybujících se cílů přes `object3d.position` (ne jen `instance.X/Y/Z`) — `dialog_0002` live sleduje orbitující `tbox_0002`. DD-16 formalizuje SPEAKER dispatch a kategorii „engine-derived behavior" (precedent pro druhý per-frame registry vedle `animators`). Audit fixy: SPEAKER_OFFSET_Y atribut (F1), DD-16 + GLOSSARY (F2), clarifying komentáře u `bubbleTails`/`meshByInstance` (F3/F4), sdílené materiály v `buildRock` (F5), `userData.noShadow` flag pro bubble tail (F6).
- [2026-04-20](diary/2026-04-20.md) — Sez. 2: M2 kompletní. GridHelper + AxesHelper, třída `TERRAIN` s atributem `COLOR`, 3×3 duhová růžice kolem mateřské CUBES, dispatch v enginu (DD-11), infotip formátuje `COLOR` jako `#rrggbb`. Sez. 3: M3 + M4 kompletní. Terminologie potomků CUBES (DD-13: `CCUBES`, `TCUBES`, `SPRITES`, `COMPOSITES`), refactor `TERRAIN → CCUBES`, jednotný float souřadný systém (DD-12 nahrazuje DD-03), `TREE` (3D strom z primitivů), `BALLOON` (COMPOSITES mimo grid, float pozice) + kompletní stínovací systém (shadow map, PCF soft, ShadowMaterial ground, self-shadow přes snížení ambient). Sez. 4: M5 + M6 kompletní. `SPRITES` s canvas-generovanou dialogovou bublinou + komix ocáskem („Ahoj! Jsem mluvící strom." nad TREE), `TCUBES` s per-face emoji texturami (krabice TOP🌳/BOTTOM🪵/sides📦 + hvězda s fallback šachovnicí na 5 stranách). DD-14 zafixoval dispatch podle typu atributu (sdílený SPRITES/TCUBES pattern). Sez. 5: M7 kompletní — chování v čase přes atribut `ANIMATE` na `OBJECTS` (DD-15, data-driven dispatch izomorfní s DD-14). Implementace: animators registry, `updateAnimations(performance.now() / 1000)` v render loopu, dva `kind`y — `balloon_bob` (vak + koš sinusově s různými periodami, lana přepočítávána přes refaktorovaný `cylinderBetween` s unit-height geometrií) a `tree_sway` (3 kužely koruny eliptickým pohybem, amplituda škálovaná výškou). Plus bubble fix: `makeBubbleTexture` sloučen na jednu cestu → žádný černý proužek okraje přes ocásek. Sez. 6: simplify review + M8 start. Refactor M7: scratch vektory (`_up`/`_dir`/`_a`/`_b`) místo per-frame Vector3 alokací v render loopu (~720 alokací/s → 0), `oscPhase(t, period, offset)` helper (DRY 4× duplicitní `2π·t/P`), `ANIMATORS` lookup tabulka (místo `switch` — s boot-time warnem na neznámý `kind`), sémantické rope konstanty (`ropeStartY`/`ropeEndY` místo magic offsets), filter `ANIMATE: null` v infotipu, cleanup commit-narativ komentářů. Dva nové generické animátory: `rotate` (přímá mutace `object3d.rotation`, `tbox_0001` točící krabice) a `orbit_stadium` (oválná dráha 2 rovné + 2 půlkruhy, `tbox_0002` „hvězda" jako auto na trati — heading sleduje tečnu dráhy). `registerAnimator` zavedl snapshot `object3d.userData.base` jako sdílený kontrakt pro transformační animátory. IDEAS: sekce „Mobilita napříč výškami" (rampy, schody, heightmap, z-levels, splines, logický graf, anti-gravity). Sez. 7: dokončení matice ANIMATE + nové COMPOSITES. `pulse` animátor (mutace `material.emissiveIntensity` — třetí osa mutace po dílech a transformacích) s volitelným rozšířením na `material.opacity` ve stejné fázi (lazy `transparent=true`, „dýchá"). Červená dlaždice pulsuje silně (period 2 s, max 0.9), tyrkysová slabě + průsvitně (3.5 s, max 0.6, opacity 0.25→1.0). Nové COMPOSITES: `HOUSE` (kvádr + jehlanová střecha ConeGeometry 4-segment, atribut `COLOR` na stěny) a `CLOUD` (shluk 5 koulí, bez atributů). Pátý `kind` `drift` — lineární pohyb po jedné ose s wrap-around (první ne-periodický animátor; `phase = (t·speed) mod range`). Mrak letí po obloze (axis X, speed 0.6, range 16). Trio základních dekorací COMPOSITES (TREE + BALLOON + HOUSE) hotové.

- [2026-05-14](diary/2026-05-14.md) — **Sez. 46:** `%AUDIT:CODE` threshold trigger (7/8) — audit src/ + docs + memory, fokus na LIQUID terminologii po DD-54 + dead code + DD-25 5. vrstva extension dokumentaci + memory drift cross-check. 10 nálezů (4 kritické + 4 doporučené + 2 kosmetické): drift `DD-25 vrstva 4 LIQUID` v `src/terrain.js` × 2 + `README.md` + `docs/IDEAS.md` (paralel sez. 45 memory cross-check zachytil drift v memory; src/docs byly nedotčené pre-audit), `docs/GLOSSARY.md` hlavička sync sez. 39 → 45 (doplnit DD-50/51/52/53/54), `README.md` M8+ milestones řádek sync DD-48 → DD-54 + Plán sekce přepis post-sez. 45 (user verdikt close), `src/main.js` LIQUID prototype komentáře refresh, `src/model.js` ORIENTATION „rampy/tunely" drop (TTUNELS smazaná sez. 38). Batch fix přes 6 souborů. Pozitivní: žádný `console.log` v src/, žádné dead files, atlas residue jen 2ř. historický komentář, LIQUID sibling order v `model.js` ř. 397 = conceptual integrity DD-54 v kódu. Cadence reset `%AUDIT:CODE` 7/8 → 0/8.
- [2026-05-14](diary/2026-05-14.md) — **Sez. 47:** SEASON expansion 6-pack (DD-50 plný scope). (A) sky season tint v `updateAtmosphere` přes `_applyHslShift` helper + `SEASON_SKY_DELTA` tabulka, scope temperate. (B) sun season tint v `updateSun` po piecewise lerp + `SUN_SEASON_DELTA` (mírnější deltas). (C) sun horizon extend do −15° (`SUN_HORIZON_Y_MIN ≈ −4.21 j`) — vyhne se „skokovému zmizení" mesh na sunset/sunrise. (D) polar season variace v `snowSpecForLatitude` + `waterSpecForClimate` (POLAR_SNOW/FREEZE_BY_SEASON tabulky, Arctic ablation kalibrace) + **refactor mode `"temperate"` → `"patches"`** (DRY: polar+temperate sloučeny na jeden code path v terrain.js Krok 3.5) + bug fix polar `altThreshold = 0` → `Infinity` (Pass 1 by jinak chytal většinu cells jako always-snowed). (E) slider DAY sync (D2 — DD-38 kotva): setInterval 4 Hz update z `world.DAY`, skip pokud user drag nebo paused. (F) ice canvas texture (D3 — DD-47 follow-up): `makeIceCanvasTexture()` 128×128 Canvas2D + 30 white radial blobs, `_iceMat.map = _iceTexture` + color → 0xffffff. User verdikt: „Parádní finalizace, zlepšení, dotažení." Žádný nový DD (= DD-50 plný scope follow-up). Single commit na main, 7. sezení dne (po sez. 41-46).
- [2026-05-14](diary/2026-05-14.md) — **Sez. 48:** **M-Genesis cleanup arc start.** User vize: *„první klíčový milník — základní iterace, ve které nic nechybí a nic overthinked nelze odebrat."* `%THINK` analýza → 5-fázový plán (audit code → audit docs → pruning → CALIBRATE → ceremonie + git tag v1.0). User volby: Q1 label „M-Genesis", Q2 ANO k 5 fázím, Q3 brain-dump A3 (FindPath multi-modal ground/air/water + generátor/transformátor/konzument supply chain) jako post-close arc do IDEAS, Q4 ceremonie v Fázi 5. **Fáze 1: `%AUDIT:CODE`** (user-driven trigger, ne cadence): K1 nálezy = 6 dispatchových vrstev bez živého konzumenta v terrain scope (SPRITES/PATH/TIMER/COUNTER/TIME/ANIMATE) + D1 atlas IIFE compute waste (TODO ř. 70 known) + D2 atlas komentář drift. User volby: PLNÝ K1 cut + teď. Cleanup K1a-K1e + D1 + D2 + UI Sun toggle drop (user follow-up *„Slunce vždy ON"*). Velikost: `src/main.js` −877 ř. (−29.5 %), `src/model.js` −128 ř., `src/time.js` smazán, `index.html` −5 ř. Celkem **−1040 ř. (−25.7 %)** JS+HTML. Model 13 → 8 tříd (OBJECTS / CUBES → BLOCKS / COMPOSITES → LAMP+DECOR / LIQUID / WORLD). **DD-55 M-Genesis cleanup scope locked** (immutable). Browser smoke test OK. Single commit `46f686d` na main + push. **Fáze 2-4 follow-up:** Commit `8a88aa7` AUDIT:DOCS (9 souborů sync + DD-55 + diary), `c88aa4c` regrese fix TAU (Censure! naivní smoke test), `b28cbd2` Fáze 3 pruning (2 MOVE → DONE + 1 DROP + IDEAS post-close arc brain-dump FindPath + supply chain), `2144ae9` Fáze 4 %CALIBRATE definice port z PocketStory + TheCubes adaptace, `ab281fc` Fáze 4 K+D impl (memory rewrite + 2 nové/updatované memory + cadence policy + %BEGIN-FAST sub-prah + permissions cleanup). **6 commits dnes** (rekord dne), 8. sezení 2026-05-14. Fáze 5 ceremonie (README v1.0 + git tag `v1.0-terrain` + close summary) plánovaná sez. 49 zítra.
- [2026-05-13](diary/2026-05-13.md) — **Sez. 40:** DD-49 plná implementace (Fáze 1-5) + DD-50 SEASON driver (minimal scope) + 7 user kalibrací (snow caps, decor scale 0.5×, DAY_SPEED 0.001, BIOME_SURFACES mid+dry rebalance, bílá tráva snowed, ramp Y fix, bush Y proporční, wet density 80%→20% škála). 2 nové soubory `src/composites/{toolkit,builders}.js`, single commit na main.
- [2026-05-14](diary/2026-05-14.md) — **Sez. 41:** DD-51 seasonal foliage cycle (LEAF_AUTUMN paleta oak/bush v autumn + winter defoliation: oak.snowed = kmen-only, bush.snowed = skip entity, spruce season-invariant) + DD-52 slope-aware decor Y na rampách (`ramps[].slopeDir` unit vector low→high + lineární `slopeT` interp v `decorate()`) + 2 kalibrace (decor scale 0.5→1/3, oak/spruce density ×2). Multi-decor `MAX_ATTEMPTS=2` zkoušen + rolled back (20k DECOR Object3D = ~8 FPS regrese). InstancedMesh DECOR refactor = priorita pro sez. 42. **Sez. 42:** perf diagnostic dive (Krok 1 shadow tighten = counterintuitive regression kvůli denser texel density, revert; Krok 2 mapSize 1024 = invalid measurement, revert; Krok 3 perf HUD fix = drop legacy `mat:` field + `renderer.info.autoReset = false` + avg per-frame stats) + DD-53 water bbox cluster attempt (implementováno + reverted po user pivot). **Klíčový insight:** user explicitně instructed "20×20 dioráma je real workflow, 100×100 = academic stress, ať zobrazení neořežeme". 20×20 test = 60 FPS + visual OK out of the box. InstancedMesh refactor downgrade `[!]` → `[ ]`. Jediný persistent kódový change = Krok 3 perf HUD fix. **Sez. 43:** Full IDEAS/TODO pruning cadence trigger (4 passes: markery refresh + ledger drop + Speculative konsolidace + section restructure 14 → 6, TODO −34 % řádků, 49 → 46 open items) + Fáze 6 DECOR KIND extension (Skupina A palm + cactus, Skupina B flower + stump + log, Skupina C `_dead` postfix + Density UI control = 5 nových KINDs + 1 meta feature). TTRAMPS skip fix v `decorate()` — corner ramp half-coverage = decor floating (math analýza: projected NW triangle ~1/4 cellu), KISS resolution skipnout corner cells. User reportoval palm float po Skupina A → fix. User kalibrace flower scale 1.0 → 0.5. Bez nového DD (Fáze 6 = pre-approved TODO impl). **Sez. 44:** Fáze 6 close — final 3 položky batch v jednom commitu: (A) receive shadow opt-out flag (`userData.noReceiveShadow` v `createDecor` traverse + respect v `createMeshFor` traverze, cast stín zachován), (B) LEAF_AUTUMN 4-color paleta AUTUMN_PALETTE (ORANGE/YELLOW/RED/BROWN), RNG pick per strom přes mulberry32 seed = monochromatická koruna, sousedi různý odstín, (C) sezonní density modifier `seasonalDensityMult(kind, season)` v `decorate()` (autumn oak/bush/flower ×0.8, winter ×0.5 + rock ×1.2; jehličnan/tropical/dry season-invariant). 79 ř. přes 4 soubory, Fáze 6 close 9/9. Bez nového DD. **Sez. 45:** Triple batch A+B+C. (A) `%BEGIN` projektový addendum krok (2.5) Target use case check v `docs/PROMPTS.md` — sez. 42 Censure carryover close (před nabídnutím perf směru se AI zeptá na real grid size / climate priority). (B) HSL hue shift sky/sun — `_lerpHsl(target, a, b, t)` helper v `main.js` s scratch HSL objekty + shorter-path hue interp (wrap přes 0/1 hranici pro |dh|>0.5), nahrazena 4× `target.lerpColors(a, b, t)` v `updateSun()` (sun color piecewise) + `updateAtmosphere()` (sky 3-keypoint). Pro orange→blue (`_skyDusk → _skyDay`) wrap přes magenta/purple = Rayleigh-correct, místo RGB lerp přes desaturovanou hnědou. DD-48 follow-up, žádný nový DD. (C) **DD-54 LIQUID class** jako **5. vrstva DD-25 extension: Tekutiny** — `class LIQUID extends CUBES` v `model.js` (sibling BLOCKS/COMPOSITES/PATH pod CUBES, paralel PATH pattern bez abstract LIQUIDS base), atributy LEVEL/TEMPERATURE/BOUNDING_BOX/CELLS. Sez. 45 prototype skeleton = single-cell instance (1 LIQUID = 1 water cell, BOUNDING_BOX={w:1,d:1}). `terrain.water[]` raw records → spawn loop v `buildScene` wrappuje do `new LIQUID(...)` + `createLiquidPlane(liquid)` (renamed `createWaterPlane(w)`). Plný BFS clustering + multi-Y split = sub-prah (DD-53 attempt + revert lesson, per target use case per-cell visually correct). **Memory drift fix**: dosavadní wording „DD-25 vrstva 4 LIQUID" byl chybný (vrstva 4 = Objekty/postavy), LIQUID je nová 5. vrstva. User signal „fyzika kapalin" → DD-54 budoucí extension hooks (TEMPERATURE °C, FLOW_DIRECTION pro řeky, VISCOSITY, LEVEL animace).
