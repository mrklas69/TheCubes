# Diary

Index denních logů. Detaily v `docs/diary/YYYY-MM-DD.md`.

- [2026-04-19](diary/2026-04-19.md) — Sez. 1: IDEA fáze + M1 kompletní. Hierarchie `OBJECTS → CUBES`, statický svět s hodinami, šachovnicová textura (DD-07), infotip panel (DD-08), osvětlení (DD-10 nahrazuje DD-09). PROMPTS.md přizpůsobeno (server v `@BEGIN`).
- [2026-04-20](diary/2026-04-20.md) — Sez. 2: M2 kompletní. GridHelper + AxesHelper, třída `TERRAIN` s atributem `COLOR`, 3×3 duhová růžice kolem mateřské CUBES, dispatch v enginu (DD-11), infotip formátuje `COLOR` jako `#rrggbb`. Sez. 3: M3 + M4 kompletní. Terminologie potomků CUBES (DD-13: `CCUBES`, `TCUBES`, `SPRITES`, `COMPOSITES`), refactor `TERRAIN → CCUBES`, jednotný float souřadný systém (DD-12 nahrazuje DD-03), `TREE` (3D strom z primitivů), `BALLOON` (COMPOSITES mimo grid, float pozice) + kompletní stínovací systém (shadow map, PCF soft, ShadowMaterial ground, self-shadow přes snížení ambient). Sez. 4: M5 + M6 kompletní. `SPRITES` s canvas-generovanou dialogovou bublinou + komix ocáskem („Ahoj! Jsem mluvící strom." nad TREE), `TCUBES` s per-face emoji texturami (krabice TOP🌳/BOTTOM🪵/sides📦 + hvězda s fallback šachovnicí na 5 stranách). DD-14 zafixoval dispatch podle typu atributu (sdílený SPRITES/TCUBES pattern).
